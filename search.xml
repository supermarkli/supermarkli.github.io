<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>2.4G 无线通信笔记</title>
    <url>/posts/a25d78f8/</url>
    <content><![CDATA[<h2 id="什么是2.4g无线通信">什么是2.4G无线通信？</h2>
<p>之前嵌入式project的通信中用到了2.4G模块，借此机会来了解一下这项技术。</p>
<span id="more"></span>
<p>2.4G无线通信是一种工作在2.4GHz频段的无线通信技术，具体频率范围是2.400GHz到2.4835GHz。这个频段属于ISM（工业、科学、医疗）频段，全球免授权使用，因此成为了各类无线通信技术的"必争之地"。</p>
<figure>
<img src="/assets/2.4G-image.png" alt="alt text" />
<figcaption aria-hidden="true">alt text</figcaption>
</figure>
<h3 id="g是怎么工作的">2.4G是怎么工作的？</h3>
<p>想象一下收音机调频的过程，2.4G通信的原理也类似：</p>
<ol type="1">
<li><strong>基本原理</strong>
<ul>
<li>发射端将数据调制到2.4GHz的载波信号上</li>
<li>通过天线将调制后的射频信号发射出去</li>
<li>接收端捕获射频信号并解调出原始数据</li>
<li>数据传输速率可达几Mbps到几百Mbps不等</li>
</ul></li>
<li><strong>为什么选择2.4GHz？</strong>
<ul>
<li>频率较高，可实现较大带宽</li>
<li>天线尺寸合适，适合便携设备</li>
<li>全球统一免费频段，使用成本低</li>
<li>穿透能力和传输距离较为均衡</li>
</ul></li>
</ol>
<blockquote>
<p><strong>调制技术详解</strong></p>
<p>就像把声音变成无线电波传输：</p>
<ol type="1">
<li><strong>常用调制方式</strong>
<ul>
<li>GFSK：高斯频移键控，常用于蓝牙</li>
<li>O-QPSK：正交相移键控，用于ZigBee</li>
<li>OFDM：正交频分复用，WiFi采用</li>
</ul></li>
<li><strong>为什么需要不同调制方式？</strong>
<ul>
<li>不同应用场景对速率要求不同</li>
<li>需要在抗干扰和功耗间权衡</li>
<li>复杂调制提供更高速率但耗电更多</li>
</ul></li>
<li><strong>信道划分</strong>
<ul>
<li>2.4G频段通常分为多个信道</li>
<li>WiFi使用11个主要信道</li>
<li>蓝牙采用跳频技术使用79个信道</li>
</ul></li>
</ol>
</blockquote>
<h3 id="g能做什么">2.4G能做什么？</h3>
<p>2.4G无线通信有多种协议和应用场景：</p>
<ol type="1">
<li><strong>WiFi（IEEE 802.11）</strong>
<ul>
<li>高速无线网络接入</li>
<li>视频流传输</li>
<li>智能家居网络</li>
</ul></li>
<li><strong>蓝牙（Bluetooth）</strong>
<ul>
<li>音频传输</li>
<li>外设连接</li>
<li>低功耗数据传输</li>
</ul></li>
<li><strong>ZigBee</strong>
<ul>
<li>传感器网络</li>
<li>智能照明</li>
<li>家庭自动化</li>
</ul></li>
<li><strong>私有协议</strong>
<ul>
<li>遥控玩具</li>
<li>无线鼠标键盘</li>
<li>工业控制</li>
</ul></li>
</ol>
<h3 id="g与wifi蓝牙的区别">2.4G与WiFi、蓝牙的区别</h3>
<p>想象2.4G频段就像一条高速公路，WiFi和蓝牙就像在这条公路上行驶的不同类型的车辆。让我们从几个方面来理解它们的区别和关系：</p>
<ol type="1">
<li><p><strong>基础概念关系</strong></p>
<ul>
<li><strong>2.4G是物理层概念</strong>：
<ul>
<li>就像高速公路本身</li>
<li>只代表通信使用的频率范围（2.400GHz-2.4835GHz）</li>
<li>是最基础的物理传输媒介</li>
</ul></li>
<li><strong>WiFi/蓝牙是协议层概念</strong>：
<ul>
<li>就像高速公路上的不同交通规则</li>
<li>在2.4G频段上构建的完整通信协议</li>
<li>包含了更复杂的通信规则和机制</li>
</ul></li>
</ul></li>
<li><p><strong>协议标准差异</strong></p>
<ul>
<li><strong>WiFi</strong>：
<ul>
<li>IEEE 802.11标准</li>
<li>面向高速数据传输</li>
<li>典型速率：几十Mbps到几Gbps</li>
</ul></li>
<li><strong>蓝牙</strong>：
<ul>
<li>IEEE 802.15.1标准</li>
<li>面向低功耗短距离通信</li>
<li>典型速率：1-3Mbps（经典蓝牙）</li>
</ul></li>
<li><strong>普通2.4G</strong>：
<ul>
<li>私有协议（如nRF24L01）</li>
<li>简单灵活的协议栈</li>
<li>典型速率：250Kbps-2Mbps</li>
</ul></li>
</ul></li>
<li><p><strong>技术特点对比</strong> | 特性 | WiFi | 蓝牙 | 普通2.4G |
|------|------|------|-----------| | 功耗 | 高 | 低（特别是BLE） | 中等
| | 距离 | 50-100m | 10-50m | 10-100m | | 组网 | 星型 | 点对点/微微网 |
灵活多样 | | 成本 | 较高 | 中等 | 较低 | | 延迟 | 较高 | 中等 | 较低
|</p></li>
<li><p><strong>应用场景对比</strong></p>
<ul>
<li><strong>WiFi适合</strong>：
<ul>
<li>高速网络接入</li>
<li>视频流传输</li>
<li>智能家居网络</li>
</ul></li>
<li><strong>蓝牙适合</strong>：
<ul>
<li>音频传输</li>
<li>可穿戴设备</li>
<li>低功耗设备连接</li>
</ul></li>
<li><strong>普通2.4G适合</strong>：
<ul>
<li>遥控遥测</li>
<li>实时控制</li>
<li>简单数据传输</li>
</ul></li>
</ul></li>
</ol>
<blockquote>
<p><strong>技术选型建议</strong></p>
<p>根据实际需求选择合适的技术： 1. 需要接入互联网 → 选择WiFi 2.
追求低功耗 → 选择蓝牙（特别是BLE） 3. 要求低延迟、简单可靠 →
选择普通2.4G 4. 成本敏感 → 普通2.4G最经济</p>
</blockquote>
<h3 id="g的特点">2.4G的特点</h3>
<ol type="1">
<li><strong>优势</strong>
<ul>
<li>全球通用免费频段</li>
<li>硬件成本低</li>
<li>技术成熟可靠</li>
<li>应用生态丰富</li>
</ul></li>
<li><strong>局限性</strong>
<ul>
<li>频段拥挤，易受干扰</li>
<li>穿墙能力一般</li>
<li>功耗相对较高</li>
<li>安全性需要额外保障</li>
</ul></li>
<li><strong>关键技术指标</strong>
<ul>
<li>传输距离：室内10-100米</li>
<li>功耗：mW到W级别</li>
<li>速率：几Kbps到几百Mbps</li>
<li>延迟：ms级别</li>
</ul></li>
</ol>
<h2 id="总结">总结</h2>
<p>2.4G无线通信凭借其免授权、低成本、技术成熟等优势，已经成为目前最普及的短距离无线通信技术之一。尽管面临着日益拥挤的频谱环境，但通过合理的系统设计和优化，仍然可以实现稳定可靠的无线通信。随着物联网的发展，2.4G技术将继续在各类应用场景中发挥重要作用。</p>
]]></content>
      <categories>
        <category>通信协议</category>
      </categories>
      <tags>
        <tag>2.4G</tag>
        <tag>无线通信</tag>
        <tag>WiFi</tag>
      </tags>
  </entry>
  <entry>
    <title>CSS Note</title>
    <url>/posts/752f9918/</url>
    <content><![CDATA[<h2 id="css心得">CSS心得</h2>
<p>介绍了之前在手搓前端时遇到的一些css问题以及解决办法</p>
<span id="more"></span>
<p>body最好设为<strong>overflow:hidden;</strong>这样就不会出现最大页面的滑动框</p>
<p>color才是字体颜色</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">body</span> &#123;</span><br><span class="line">    <span class="attribute">margin</span>:<span class="number">0px</span>;</span><br><span class="line">    <span class="attribute">display</span>: flex; <span class="comment">/* 使用flex布局，使子元素水平排列 */</span></span><br><span class="line">    <span class="attribute">font-family</span>: Arial, sans-serif; <span class="comment">/* 设置页面的默认字体 */</span></span><br><span class="line">    <span class="attribute">height</span>: <span class="number">100vh</span>;</span><br><span class="line">    <span class="attribute">overflow</span>:hidden;</span><br><span class="line">    <span class="attribute">color</span>:<span class="number">#404040</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
<h3 id="常见css">常见css</h3>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="attribute">text-align</span>: center;  <span class="comment">/* 文本居中对齐 */</span></span><br><span class="line"><span class="attribute">border</span>: <span class="number">1px</span> solid <span class="number">#ddd</span>; <span class="comment">/* 添加边框 */</span></span><br><span class="line"><span class="attribute">box-shadow</span>: <span class="number">0</span> <span class="number">4px</span> <span class="number">6px</span> <span class="built_in">rgba</span>(<span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0.1</span>); <span class="comment">/* 添加阴影效果 */</span></span><br><span class="line"><span class="attribute">border-radius</span>: <span class="number">8px</span>; <span class="comment">/* 添加圆角 */</span></span><br><span class="line"><span class="attribute">font-size</span>: <span class="number">14px</span>;    <span class="comment">/* 设置文字大小 */</span></span><br><span class="line"><span class="attribute">color</span>: <span class="number">#444444</span>;     <span class="comment">/* 设置文字颜色 */</span></span><br><span class="line"><span class="attribute">font-weight</span>: bold;  <span class="comment">/* 设置文字加粗 */</span></span><br><span class="line"><span class="attribute">font-weight</span>: normal; <span class="comment">/* 确保表头字体不加粗 */</span></span><br><span class="line"><span class="attribute">border</span>: none; <span class="comment">/* 移除按钮边框 */</span></span><br><span class="line"><span class="attribute">cursor</span>: pointer; <span class="comment">/* 鼠标悬停时显示为指针 */</span></span><br><span class="line"><span class="attribute">transition</span>: background-color <span class="number">0.3s</span>; <span class="comment">/* 设置背景颜色的过渡效果 */</span></span><br><span class="line"><span class="attribute">white-space</span>: nowrap;<span class="comment">/* 不允许换行 */</span></span><br><span class="line"><span class="attribute">user-select</span>: none;<span class="comment">/* 不允许选择文字 */</span></span><br><span class="line"><span class="attribute">top</span>: <span class="number">20%</span> <span class="meta">!important</span>;<span class="comment">/* 与容器顶部的距离 */</span></span><br><span class="line"><span class="attribute">overflow</span>: auto;<span class="comment">/* 允许滚动，建议父容器为hidden，且子容器要设置宽度 */</span></span><br><span class="line"><span class="attribute">table-layout</span>:fixed;</span><br><span class="line"> <span class="attribute">z-index</span>: <span class="number">1000</span>; <span class="comment">/* 页面最上面 */</span></span><br></pre></td></tr></table></figure>
<h3 id="常见flex属性">常见flex属性</h3>
<p>flex: 1；可以搭配overflow：hidden；使用</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="attribute">flex-direction</span>: column; <span class="comment">/* 子元素垂直排列 */</span></span><br><span class="line"><span class="attribute">align-items</span>: center; <span class="comment">/* 子元素水平居中 */</span></span><br><span class="line"><span class="attribute">flex-shrink</span>: <span class="number">0</span>;   <span class="comment">/* 元素不会缩小 */</span> </span><br><span class="line"><span class="selector-class">.filter-button</span><span class="selector-pseudo">:hover</span> &#123;</span><br><span class="line">    <span class="attribute">background-color</span>: <span class="number">#edf9f7</span>; <span class="comment">/* 鼠标悬停时改变背景颜色 */</span></span><br><span class="line">    <span class="attribute">border-bottom-color</span>: <span class="number">#2d6253</span>; </span><br><span class="line">&#125;</span><br><span class="line"><span class="attribute">flex</span>: <span class="number">1</span> ; <span class="comment">/* 占据剩余的宽度 */</span></span><br><span class="line"><span class="attribute">justify-content</span>: space-between;<span class="comment">/* 占满一行 */</span></span><br><span class="line"><span class="attribute">gap</span>: <span class="number">10px</span> <span class="number">20px</span>; <span class="comment">/* 设置行间距为10px，列间距为20px */</span></span><br></pre></td></tr></table></figure>
<h3 id="技巧可以搭配js实现宽度变化">技巧：可以搭配js实现宽度变化</h3>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.sidebar-container</span><span class="selector-class">.expanded</span> &#123;<span class="attribute">width</span>: <span class="number">16%</span>; <span class="comment">/* 扩展后的宽度 */</span>&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&#x27;filterButton&#x27;</span>).<span class="title function_">addEventListener</span>(<span class="string">&#x27;click&#x27;</span>, <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">            sidebar.<span class="property">classList</span>.<span class="title function_">toggle</span>(<span class="string">&#x27;expanded&#x27;</span>);</span><br><span class="line">            tb1.<span class="property">classList</span>.<span class="title function_">toggle</span>(<span class="string">&#x27;expanded&#x27;</span>);</span><br><span class="line">            <span class="variable language_">this</span>.<span class="property">textContent</span> = <span class="variable language_">this</span>.<span class="property">textContent</span> === <span class="string">&#x27;»&#x27;</span> ? <span class="string">&#x27;«&#x27;</span> : <span class="string">&#x27;»&#x27;</span>;</span><br><span class="line">        &#125;);</span><br></pre></td></tr></table></figure>
<h3 id="技巧改变列宽">技巧：改变列宽</h3>
<p>将单元格设为如下</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="attribute">overflow</span>: hidden;</span><br><span class="line"><span class="attribute">text-overflow</span>: ellipsis; <span class="comment">/* ... */</span></span><br></pre></td></tr></table></figure>
<p>想要改变列宽必须设为fixed；<code>table-layout</code>
是一个控制表格布局算法的 CSS 属性。它有两个主要值：</p>
<ol type="1">
<li><strong>auto</strong>（默认值）：表格及其列的宽度根据内容动态调整。</li>
<li><strong>fixed</strong>：表格及其列的宽度根据表格的宽度和列的宽度定义来确定，不考虑内容。</li>
</ol>
<p>由于 <code>table-layout: fixed;</code>
的布局计算仅依赖于表格和列的宽度定义，而不依赖于内容，因此它能够显著加快表格的渲染速度，特别是在大数据量的表格中。</p>
<p>这里的技巧是color:white;这样不会影响表格的美观</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">th</span> <span class="selector-class">.resize-handle</span> &#123;</span><br><span class="line">    <span class="attribute">position</span>: absolute;</span><br><span class="line">    <span class="attribute">right</span>: <span class="number">0</span>;</span><br><span class="line">    <span class="attribute">bottom</span>: <span class="number">0</span>;</span><br><span class="line">    <span class="attribute">width</span>: <span class="number">5px</span>;</span><br><span class="line">    <span class="attribute">height</span>: <span class="number">100%</span>;</span><br><span class="line">    <span class="attribute">cursor</span>: col-resize;</span><br><span class="line">    <span class="attribute">z-index</span>: <span class="number">1</span>;</span><br><span class="line">    <span class="attribute">color</span>:white;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="技巧表格置顶">技巧：表格置顶</h3>
<p>outline是为了修复滑动时失去边界的bug</p>
<p>当元素使用 <code>position: sticky</code> 时，它的表现如下：</p>
<ul>
<li>元素在容器内是相对定位的，当滚动到某个阈值时，它变成固定定位。</li>
<li>该元素会在父元素的特定位置（由 <code>top</code>, <code>right</code>,
<code>bottom</code>, <code>left</code> 属性定义）开始“粘滞”。</li>
</ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">th</span>&#123;</span><br><span class="line">    <span class="attribute">position</span>:sticky;</span><br><span class="line">    <span class="attribute">top</span>:<span class="number">0</span>;</span><br><span class="line">    <span class="attribute">border</span>: none;</span><br><span class="line">    <span class="attribute">outline-color</span>: <span class="number">#ddd</span>;</span><br><span class="line">    <span class="attribute">outline-style</span>: solid;</span><br><span class="line">    <span class="attribute">outline-width</span>: <span class="number">1px</span>;</span><br><span class="line">    <span class="attribute">background-color</span>: white; <span class="comment">/* 设置表头背景颜色 */</span></span><br><span class="line">    <span class="attribute">user-select</span>: none; <span class="comment">/* 标准语法 */</span></span><br><span class="line">    <span class="attribute">z-index</span>: <span class="number">1</span>;  <span class="comment">/* 确保表头在内容上方 */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>CSS</category>
      </categories>
      <tags>
        <tag>CSS</tag>
      </tags>
  </entry>
  <entry>
    <title>Anaconda Note</title>
    <url>/posts/7066aaf6/</url>
    <content><![CDATA[<h2 id="anaconda">Anaconda</h2>
<p>Anaconda 是一个强大的数据科学和机器学习工具包，简化了 Python
环境和包的管理。</p>
<span id="more"></span>
<p>梳理一下 <code>conda</code>、<code>Anaconda</code> 和
<code>Python</code> 之间的关系：</p>
<ol type="1">
<li><strong>Python</strong>:
<ul>
<li>Python
是一种广泛使用的高级编程语言，以其简洁易读的语法和强大的库支持著称。Python
的核心功能是提供编程语言基础，供开发者用来编写各种应用程序。</li>
</ul></li>
<li><strong>Conda</strong>:
<ul>
<li>Conda
是一个开源的<strong>包管理和环境管理系统</strong>。它能够安装、更新和卸载软件包，同时管理多个项目的环境，确保不同项目的依赖不会互相干扰。Conda
可以管理任何编程语言的软件包，但在 Python 的上下文中，它主要用于 Python
包和环境的管理。</li>
<li>Conda 与 Python 的关系是，<strong>Conda 可以用来安装 Python
及其库，并且能够创建隔离的 Python
环境</strong>。这样，你可以在同一台计算机上维护多个 Python
版本和不同版本的库。</li>
</ul></li>
<li><strong>Anaconda</strong>:
<ul>
<li>Anaconda 是一个包含 Python 和 Conda
的开源发行版。它提供了一个包含大量数据科学和机器学习库的环境，预装了
Conda、Python 以及许多常用的数据科学工具（如
NumPy、Pandas、Matplotlib、SciPy 等）。</li>
<li>Anaconda
的目标是简化科学计算和数据分析的过程，提供一个即开即用的开发环境，免去手动安装各种工具和库的麻烦。</li>
</ul></li>
</ol>
<h3 id="基本命令"><strong>基本命令</strong></h3>
<ol type="1">
<li><strong>创建和管理环境</strong>：
<ul>
<li>创建新环境：<code>conda create --name myenv python=3.9</code></li>
<li>激活环境：<code>conda activate myenv</code></li>
<li>退出环境：<code>conda deactivate</code></li>
<li>列出环境：<code>conda env list</code></li>
<li>删除环境：<code>conda remove --name myenv --all</code></li>
</ul></li>
<li><strong>包管理</strong>：
<ul>
<li>安装包：<code>conda install package_name</code></li>
<li>更新包：<code>conda update package_name</code></li>
<li>卸载包：<code>conda remove package_name</code></li>
<li>列出安装的包：<code>conda list</code></li>
</ul></li>
<li><strong>环境导出和导入</strong>：
<ul>
<li>导出环境：<code>conda env export &gt; environment.yml</code></li>
<li>导入环境：<code>conda env create -f environment.yml</code></li>
</ul></li>
</ol>
]]></content>
      <categories>
        <category>Python</category>
      </categories>
      <tags>
        <tag>conda</tag>
        <tag>Python</tag>
      </tags>
  </entry>
  <entry>
    <title>NFC Note</title>
    <url>/posts/78289205/</url>
    <content><![CDATA[<h2 id="什么是nfc">什么是NFC？</h2>
<p>今天听实验室师兄的分享时提到了NFC技术，自己对这个技术也是一知半解，所以来记录一下。</p>
<span id="more"></span>
<p>NFC（Near Field
Communication，近场通信）是一种短距离无线通信技术，工作频率为13.56MHz，最大通信距离通常在10厘米以内。这项技术继承了RFID（射频识别）的核心原理，但针对移动设备进行了优化和扩展。</p>
<figure>
<img src="/assets/NFC-image.png" alt="alt text" />
<figcaption aria-hidden="true">alt text</figcaption>
</figure>
<h3 id="nfc是怎么工作的">NFC是怎么工作的？</h3>
<p>想象一下，两个磁铁靠近时会相互影响。NFC的工作原理也差不多：</p>
<ol type="1">
<li><strong>基本原理</strong>
<ul>
<li>主动设备（比如刷卡机）通过天线产生13.56MHz的高频交变磁场</li>
<li>被动设备（比如NFC标签）进入磁场后，通过电磁感应获得能量</li>
<li>两个设备通过调制解调技术（ASK、PSK等）在磁场中编码和解码数据</li>
<li>数据传输速率可达424Kbps</li>
</ul></li>
<li><strong>为什么要靠得这么近？</strong>
<ul>
<li>磁场强度与距离的平方成反比，超过10cm信号就急剧减弱</li>
<li>近场通信使用感应耦合（而不是电磁波传播）</li>
<li>这种物理特性自然形成了安全距离，防止远距离窃听</li>
</ul></li>
</ol>
<blockquote>
<p><strong>调制解调技术</strong></p>
<p>想象用手电筒给远处的朋友发信号：</p>
<ol type="1">
<li><strong>什么是调制？</strong>
<ul>
<li>就像用手电筒发信号时，通过开关灯来传递信息</li>
<li>NFC中主要使用两种调制方式：
<ul>
<li>ASK（幅度调制）：类似于调整手电筒的亮度</li>
<li>PSK（相位调制）：类似于按固定节奏闪烁手电筒</li>
</ul></li>
</ul></li>
<li><strong>为什么要调制？</strong>
<ul>
<li>原始数据（比如1和0）不能直接传输</li>
<li>需要把数据"搭载"在13.56MHz的载波信号上</li>
<li>就像把信息写在纸飞机上才能传递给别人</li>
</ul></li>
<li><strong>解调是什么？</strong>
<ul>
<li>接收方需要从收到的信号中"提取"出原始数据</li>
<li>就像接收方要从纸飞机上读取信息</li>
<li>或者从手电筒的闪烁中理解传递的含义</li>
</ul></li>
</ol>
</blockquote>
<h3 id="nfc能做什么">NFC能做什么？</h3>
<p>NFC有三种常见的使用方式：</p>
<ol type="1">
<li><strong>读写模式</strong>
<ul>
<li>就像用手机碰一碰地铁站的时刻表海报</li>
<li>或者用手机读取商品上的电子标签</li>
</ul></li>
<li><strong>手机支付模式</strong>
<ul>
<li>手机变身成公交卡、银行卡</li>
<li>碰一碰就能付款</li>
</ul></li>
<li><strong>设备互联模式</strong>
<ul>
<li>两个手机之间传照片</li>
<li>新耳机和手机快速配对</li>
</ul></li>
</ol>
<h3 id="nfc的特点">NFC的特点</h3>
<ol type="1">
<li><strong>超级方便</strong>
<ul>
<li>采用即时配对（Peer-to-Peer）技术，无需复杂配对流程</li>
<li>支持双向通信，设备可以自动切换读写角色</li>
</ul></li>
<li><strong>很安全</strong>
<ul>
<li>物理层：通信距离限制</li>
<li>协议层：支持AES-128等加密算法</li>
<li>应用层：可集成安全单元（SE）存储敏感数据</li>
</ul></li>
<li><strong>省电</strong>
<ul>
<li>被动设备（如NFC标签）利用电磁感应供电，无需电池</li>
<li>主动设备仅在通信时开启RF场，待机功耗极低</li>
<li>典型NFC操作仅需几毫秒，耗电量可忽略不计</li>
</ul></li>
</ol>
<h3 id="生活中的nfc应用">生活中的NFC应用</h3>
<ol type="1">
<li><strong>支付场景</strong>
<ul>
<li>手机付款（Apple Pay、微信/支付宝碰一碰）</li>
<li>刷公交地铁</li>
<li>超市购物</li>
</ul></li>
<li><strong>智能家居</strong>
<ul>
<li>碰一碰开门</li>
<li>碰一碰控制家电</li>
<li>碰一碰自动设置手机模式（比如睡觉模式）</li>
</ul></li>
<li><strong>便捷生活</strong>
<ul>
<li>门禁卡</li>
<li>打卡签到</li>
<li>快速连接蓝牙设备</li>
</ul></li>
</ol>
<h2 id="总结">总结</h2>
<p>NFC作为一种成熟的近距离无线通信技术，已经在我们的日常生活中扮演着越来越重要的角色。它的高安全性、便捷性和广泛的应用场景使其成为移动支付和智能设备交互的首选技术之一。随着物联网技术的发展，NFC的应用领域将会进一步扩大。</p>
]]></content>
      <categories>
        <category>通信协议</category>
      </categories>
      <tags>
        <tag>无线通信</tag>
        <tag>NFC</tag>
        <tag>RFID</tag>
      </tags>
  </entry>
  <entry>
    <title>TDX Note</title>
    <url>/posts/cbeff3a9/</url>
    <content><![CDATA[<h2 id="intel-tdx-demystified-a-top-down-approach">Intel TDX
Demystified: A Top-Down Approach</h2>
<p><strong>摘要</strong>：Intel Trust Domain Extensions (TDX)
是一种在第四代 Intel Xeon Scalable
处理器中引入的架构扩展，支持保密计算。TDX 允许在安全仲裁模式 (SEAM)
中部署虚拟机 (VM)，提供加密的 CPU
状态和内存、完整性保护以及远程认证。</p>
<span id="more"></span>
<blockquote>
<p><strong>安全仲裁模式（Secure Arbitration Mode, SEAM）</strong>
是英特尔在其处理器架构中引入的一种硬件安全机制，它允许在处理器中隔离一个由
SEAM 模块（SEAM
Module）控制的安全环境，这个模块用于处理一些高度敏感的任务和数据。</p>
</blockquote>
<p><strong>简介</strong>：将计算迁移到云基础设施中可以降低成本，但受监管的行业对于将敏感数据移交给第三方云服务提供商仍存在担忧。</p>
<p>保密计算旨在通过最小化处理器及其供应商的信任根来提供端到端的保护。所有数据必须在整个生命周期中受到保护，从离开其所有者的设备到进入<strong>经过认证的
CPU 封装</strong>。</p>
<p>TDX
的威胁模型假设特权软件（如<strong>虚拟机管理程序或主机操作系统</strong>）可能是不可信的或具有对抗性的。</p>
<hr />
<h2 id="tdx-的概述">TDX 的概述</h2>
<p>TDX 的目标是保护指定信任域 (TD) 的 CPU
状态和内存的机密性和完整性，并使 TD 所有者能够验证远程平台的真实性。</p>
<p>TDX 基于一系列技术构建，包括虚拟化技术 (VT)、<strong>多密钥全内存加密
(MKTME)</strong> 和 TDX 模块 (TDX Module)。TDX 还依赖于软件保护扩展
(SGX) 和<strong>数据中心认证原语 (DCAP)</strong> 来进行远程认证。</p>
<blockquote>
<ul>
<li>MKTME
提供了一种机制，使得处理器能够利用多个加密密钥，对内存数据进行加密和解密。</li>
<li>DCAP 提供了一种机制，使得远程用户或服务可以验证 TDX
环境的可信状态。</li>
</ul>
</blockquote>
<h2 id="安全原则">安全原则</h2>
<p>消除了对不可信/特权主机软件的层级依赖，并<strong>将虚拟机管理程序和云操作员排除在可信计算基
(TCB)
之外</strong>，从而允许租户以信任的方式安全部署和运行其计算任务。</p>
<p>TDX 保障 TD 的内存和虚拟 CPU
状态的机密性和完整性，确保这些数据不会被同一台机器上运行的其他安全域访问或篡改。它通过以下机制来实现这一点：</p>
<ol type="1">
<li><strong>内存访问控制</strong></li>
<li><strong>运行时内存加密</strong></li>
<li><strong>由 Intel 签署的 TDX 模块，负责处理涉及安全敏感的 TD
管理操作</strong></li>
</ol>
<p>此外，远程认证为租户提供了 TD 在真实的 TDX 支持的 Intel
处理器上运行的证明。</p>
<h3 id="内存机密性">内存机密性</h3>
<p>TD
的数据在处理器封装内以明文形式存储，但当数据从处理器卸载到主存储器时，处理器使用<strong>仅处理器已知的
TD 专用密钥</strong>对其进行加密。</p>
<p>加密操作在<strong>缓存行粒度</strong>上执行，这使得外围设备无法读取或篡改
TD
的私有内存而不被检测到。当处理器从主存储器加载数据时，处理器<strong>可以检测到任何篡改行为</strong>。</p>
<blockquote>
<p>一个缓存行是 CPU
从主存储器<strong>读取或写入数据的基本单位</strong>，通常为 64
字节。这样做是为了提高加密和解密操作的效率，因为 CPU
的数据处理与缓存行的操作直接相关。</p>
</blockquote>
<h3 id="cpu-状态机密性">CPU 状态机密性</h3>
<p>TDX 通过在安全域之间进行上下文切换时<strong>管理 TD 的虚拟 CPU
状态</strong>来保护并发进程的机密性。</p>
<p>这些状态存储在 <strong>TD 的元数据</strong>中，并在主存储器中通过 TD
的密钥进行保护。上下文切换期间，TDX 会清除或隔离处理器内部的与 TD
相关的特定状态，如翻译后援缓冲 (TLB) 条目或分支预测缓冲，以保持 TD
信息的保护。</p>
<blockquote>
<p><strong>元数据（Metadata）</strong>
是指描述数据的数据。它提供了有关某一数据的结构、属性、特征等信息，帮助我们理解、组织和管理这些数据。</p>
</blockquote>
<h3 id="执行完整性">执行完整性</h3>
<p>TDX 保护 TD 执行的完整性，防止主机干扰，确保在预期的状态下恢复 TD
的计算。它能够检测到虚拟 CPU
状态中的恶意变化，以及位于私有内存中的指令的插入、修改或删除。</p>
<p>然而，TDX 不提供额外的控制流完整性保证。</p>
<p>TD
所有者有责任使用现有的基于编译或硬件辅助的控制流完整性执行技术，例如控制流强制技术
(CET)。</p>
<blockquote>
<p>意味着，如果 TD
的<strong>代码本身有漏洞</strong>，攻击者仍然可以通过某种方式来影响其控制流。</p>
<p>CET
是一种由英特尔提供的硬件安全特性，用于检测和阻止控制流劫持攻击。</p>
</blockquote>
<h3 id="io-保护">I/O 保护</h3>
<p>外围设备或加速器位于 TD 信任边界之外，不应被允许访问 TD
的私有内存。为了支持虚拟化 I/O，TD
可以选择<strong>显式共享内存</strong>以进行数据传输。</p>
<p>但是，TDX
<strong>不对位于共享内存区域中的数据提供机密性和完整性保护</strong>。TD
所有者有责任实现适当的机制，例如使用传输层安全协议 (TLS)
等安全通信通道，<strong>以保护离开 TD 信任边界的数据</strong>。</p>
<p>在未来，TDX 2.0 计划包括 TDX Connect 以解决受信 I/O 问题。</p>
<blockquote>
<p><strong>TLS（Transport Layer Security）</strong>
是一种用于在计算机网络上提供安全通信的加密协议。它通过加密、数据完整性验证和身份认证来保护数据的传输，防止信息泄露和篡改。</p>
</blockquote>
<hr />
<h2 id="威胁模型">威胁模型</h2>
<p>TDX
假设对手可能具有<strong>物理或远程访问计算机</strong>的能力，并能够<strong>控制启动固件、系统管理模式
(SMM)、主机操作系统 (OS)、虚拟机管理程序 (Hypervisor)
和外围设备</strong>。</p>
<p>这些对手的主要目标是获取机密数据或干扰 TD 的执行。需要注意的是，TDX
不能保证可用性，因为对手<strong>可以控制分配给 TD
的所有计算资源</strong>，并发起拒绝服务 (DoS) 攻击。</p>
<blockquote>
<p>这里的"可用性"是指虚拟机（TD）能够可靠、持续地执行和运行的能力</p>
<p><strong>拒绝服务攻击（Denial of Service，简称 DoS）</strong>
是一种网络攻击，它的目的是使目标系统或服务无法正常提供服务。攻击者通过向目标发送大量请求或恶意数据，耗尽目标的资源（如带宽、CPU、内存等），导致合法用户无法访问或使用目标系统或服务。</p>
</blockquote>
<p>因此，TDX 的设计必须防止对手采取任何可能破坏 TDX
安全保证的行为。下面，我们总结了对手的能力，并确定了潜在的攻击向量和场景。</p>
<p>对手可以通过其主机侧接口函数与 TDX
模块进行交互，这些接口函数允许它们构建、初始化、测量和销毁
TD。对手可以按照任意顺序调用这些接口函数，并提供语义上和语法上合法或非法的输入。</p>
<p>对手可以控制分配给 TD
的计算资源，包括物理内存页、处理器时间和物理/虚拟设备。它们可以在任意时刻中断
TD，并尝试读取和写入任意内存位置，还可以重新配置输入/输出内存管理单元
(IOMMU)。</p>
<p>对手可以操纵 TD 的输入数据，包括高级配置和电源接口 (ACPI)
表、外围组件互连 (PCI) 配置、特定型号寄存器 (MSR)、内存映射输入/输出
(MMIO)、直接内存访问
(DMA)、模拟设备、主机处理的超调用、随机源和时间概念。</p>
<p>对手可以进行物理和硬件攻击，例如通过探测总线或通过恶意 DMA
访问主存储器。对于回滚任意内存区域的物理攻击，没有防御措施。然而，<strong>对手不可能提取嵌入处理器芯片中的密钥材料</strong>。威胁模型不包括故障注入或侧信道攻击，如电源扰动、时序和电力分析。</p>
<p>攻击 TDX
认证在模型范围之内，因为它破坏了信任模型，并可能使对手伪造虚假的
TEE，以从租户处收集机密信息。</p>
<h3 id="可信计算基-tcb">可信计算基 (TCB)</h3>
<p>TDX 的 TCB 由<strong>支持 TDX 的 Intel
处理器</strong>及其内置技术（如 VT、MKTME 和 SGX）组成。TCB 还包括由
Intel 签署的软件模块，如 TDX 模块、NP/P-SEAM 加载器，以及用于远程认证的
SGX 架构飞地 (enclaves)。</p>
<p>在 TD 内运行的软件栈由租户拥有，并被视为 TCB 的一部分。TDX
中使用的加密原语被认为是可靠的，其实现也是安全的，包括随机数的生成和侧信道攻击（如时间攻击）的防护。</p>
<p>租户必须信任处理器制造商 Intel，以开发、制造、构建和签署用于 TDX
的硬件/软件组件。TDX 模块、NP/P-SEAM 加载器以及 DCAP
的源代码是公开可审计的，允许租户评估其可信度。然而，租户还必须信任 Intel
签署的版本与他们审查的版本一致，这涉及在编译过程中对供应链攻击的保护。</p>
<p>此外，租户还需要信任 Intel 的供应证书服务 (PCS) 进行远程认证。PCS
最初支持 SGX 认证，现已扩展至包括检索供应证书密钥 (PCK) 证书、撤销列表和
TCB 信息。</p>
<hr />
<h2 id="保密计算技术的比较">保密计算技术的比较</h2>
<p>保密计算技术的共同目标是保护在不可信第三方基础设施上托管的敏感数据和计算，防止未授权访问、篡改和披露。</p>
<p>主要处理器供应商正在竞争将保密计算功能集成到其芯片中。尽管在实现和术语上存在差异，但这些技术共享基本的安全原则，并具有类似的系统设计，如引入新的执行模式或特权级别，将虚拟机管理功能迁移到经过认证的固件/软件中，确保受信或测量的可信组件的启动，实施内存访问控制，并提供内存加密保护。</p>
<p>除了 Intel TDX 外，我们还简要介绍了来自其他供应商的保密计算技术，包括
AMD 安全加密虚拟化 (SEV)、IBM 安全执行和受保护执行设施 (PEF)、Arm
保密计算架构 (CCA) 和 RISC-V 保密虚拟机扩展 (CoVE)。</p>
<figure>
<img src="/assets/1111.png" alt="已上传的图片" />
<figcaption aria-hidden="true">已上传的图片</figcaption>
</figure>
<h3 id="amd-sev">AMD SEV</h3>
<p>SEV（安全加密虚拟化）是 AMD EPYC
处理器中的一项保密计算功能。它通过在多租户云环境中对虚拟机（VM）内存进行加密来保护敏感数据，防止特权软件或管理员的访问。</p>
<p>SEV 依赖于 AMD 安全内存加密 (SME) 和 AMD 虚拟化 (AMD-V)
技术，通过强制执行 VM 与虚拟机管理程序之间的加密隔离来确保安全。每个 VM
都分配有一个唯一的临时高级加密标准（AES）密钥，用于运行时的内存加密。</p>
<p>内存控制器上的 AES 引擎负责加密或解密写入或读取主存储器的数据。每个
VM 的密钥由 <strong>AMD 平台安全处理器 (PSP)
管理</strong>，该处理器是集成在 AMD 系统芯片 (SoC) 内的 32 位 Arm
Cortex-A5 微控制器。物理地址的 C 位（第 47
位）决定了内存页面的加密。</p>
<p>SEV 还提供了远程认证机制，允许 VM 所有者验证 VM 启动测量的可信度及
SEV 平台的可靠性。PSP 生成由 AMD 认证密钥签名的认证报告。VM
所有者可以验证认证报告的真实性及其嵌入的平台/来宾测量信息。</p>
<p>AMD 已经发布了 SEV 的三代版本。第一代 SEV 仅保护 VM
内存的机密性。第二代 SEV-ES（加密状态）增加了在虚拟机管理程序切换期间对
CPU 寄存器状态的保护，而第三代
SEV-SNP（安全嵌套分页）增加了完整性保护，以防止内存篡改、重放和重映射攻击。</p>
<p>特别是，SEV-SNP 使用反向映射表 (RMP) 进行内存完整性保护。RMP
追踪每个页面的所有权和权限，以防止未经授权的访问。SEV-SNP
还通过将来宾地址空间划分为四个级别并在 VM
内部提供额外的安全隔离，来引入<strong>虚拟机权限级别 (VMPL)</strong>
功能。</p>
<h3 id="ibm-保密计算">IBM 保密计算</h3>
<p>IBM 对保密计算的早期探索可以追溯到对 SecureBlue++ 的研究，该研究在
Mambo CPU 模拟器上运行了一个模拟的 POWER 处理器。今天，IBM
系统支持两种架构的保密计算：IBM Z 和 LinuxONE 上提供的安全执行 (Secure
Execution) 以及 OpenPOWER 系统上发布的受保护执行设施 (PEF)。</p>
<p><strong>IBM 安全执行 (Secure Execution)</strong><br />
IBM 安全执行自 IBM Z15 和 LinuxONE III 以来提供了对在隔离 TEE
中运行的安全虚拟机 (SVM) 的支持。</p>
<p>安全执行保护 SVM
中代码和数据的机密性、完整性和真实性，防止任何未经授权的访问和篡改。安全执行利用被称为
Ultravisor 的可信固件执行安全敏感任务来引导和运行 SVM。Ultravisor
在上下文切换期间保护 SVM
的内存和状态，并防止潜在的恶意虚拟机管理程序的入侵。</p>
<p><strong>IBM PEF (受保护执行设施)</strong><br />
PEF 通过对 IBM Power 指令集架构 (ISA) 的扩展来提供基于 VM 的 TEE。PEF
固件、用于准备 SVM 的工具和操作系统扩展都是作为开源软件发布的。</p>
<p>为了保护敏感数据和代码，PEF 引入了一个受信固件——受保护执行
Ultravisor（Ultravisor），该固件通过 CPU 架构变化来屏蔽 SVM
执行并强制执行安全保障。PEF
依赖于系统的安全和受信启动，并在一个新的、最高特权的 CPU 状态（称为
Secure State）中执行 Ultravisor。</p>
<p>虚拟机管理程序启动 VM，然后调用 Ultravisor 来切换到 SVM。Ultravisor
通过将其移动到受保护的内存来将 VM 转换为
SVM，从而确保这些内存对于不受信任的代码是不可访问的。</p>
<h3 id="arm-cca">Arm CCA</h3>
<p>CCA（保密计算架构）是 Armv9 架构中引入的一项技术。传统上，Arm
TrustZone 通过将"普通世界"（Normal World）和"安全世界"（Secure
World）分隔开来，实现了安全执行。TrustZone
防止普通世界的软件访问安全世界中的数据。</p>
<p>CCA 引入了"领域管理扩展"（Realm Management
Extension，RME），通过添加两个新的世界：领域世界（Realm
World）和根世界（Root World），进一步扩展了这种分隔。</p>
<p>领域世界（Realm World）为保密虚拟机（Confidential
VMs）提供了相互不信任的执行环境，隔离工作负载，使其不受其他安全域（包括主机操作系统、虚拟机管理程序、其他领域和
TrustZone）的干扰。</p>
<p>为了实施地址空间的隔离，CCA
使用了一种扩展的页面表结构——"粒度保护表"（Granule Protection
Table，GPT），该结构用于跟踪每个页面的所有权，并将其归属于不同的世界。</p>
<p>根世界（Root World）中的"监控器"（Monitor）负责创建和管理
GPT，防止虚拟机管理程序或操作系统直接更改它。监控器可以通过更新 GPT
动态地在不同世界之间移动物理内存。CCA 还支持对 CCA
平台及其领域的初始状态进行认证，以确保其安全性。</p>
<h3 id="risc-v-cove">RISC-V CoVE</h3>
<p>CoVE（保密虚拟机扩展）是 RISC-V
的一个参考保密计算架构，其受保护的实例被称为 TEE
虚拟机（TVM）。该架构引入了 TEE 安全管理器（TSM）驱动，这是一个 M
模式（RISC-V
中的最高特权级别）固件组件，用于在保密环境和非保密环境之间切换。TSM
驱动通过内存跟踪表（Memory Tracking Table，MTT）跟踪 TVM
的内存页面分配。</p>
<p>TSM 驱动测量并加载 TSM，它是虚拟机管理程序与 TVM 之间的受信中介。CoVE
定义了一个应用程序二进制接口（ABI），供虚拟机管理程序从 TSM
请求虚拟机管理服务。</p>
<p>CoVE 采用分层认证架构，从硬件开始，逐层向上加载、测量并认证 TSM
驱动、TSM 和
TVM。每一层都由上一层加载、测量并签署认证。这种方法提供了一个安全的信任链，可以用于验证系统的完整性。TVM
可以从 TSM 获取一个包含从硬件根源认证的证书，为 TVM
和其所运行的软件的真实性验证提供了机制。</p>
<hr />
<h2 id="tdx-的构建模块">TDX 的构建模块</h2>
<p>TDX 依赖于一系列现有的 Intel 技术，包括虚拟化技术 (VT)、全内存加密
(TME)/多密钥全内存加密 (MKTME) 和软件保护扩展 (SGX)。</p>
<figure>
<img src="/assets/QQ_1730016861906.png" alt="QQ_1730016861906" />
<figcaption aria-hidden="true">QQ_1730016861906</figcaption>
</figure>
<h3 id="intel-vt">Intel VT</h3>
<p>Intel VT（虚拟化技术）是一组由 Intel
处理器提供的<strong>硬件辅助虚拟化</strong>功能。使用
VT，虚拟机监控程序（VMM）或虚拟机管理程序可以获得比基于软件的虚拟化更好的性能、隔离和安全性。Intel
的 VT 产品组合包括 CPU、内存和 I/O 的虚拟化。</p>
<p>支持 VT-x
技术的处理器具有一组称为虚拟机扩展（VMX）的特殊指令集，它可以控制虚拟化功能的实现。支持
VT-x 的处理器可以在两种模式下运行：VMX 根模式和 VMX 非根模式。</p>
<p>虚拟机管理程序运行在 VMX 根模式，而来宾 VM 运行在 VMX 非根模式。VT-x
定义了两种新转换方式：虚拟机进入（VM Entry）和虚拟机退出（VM
Exit），用于在来宾和虚拟机管理程序之间切换。</p>
<p>VMX
的切换信息存储在虚拟机控制结构（VMCS）中，用于记录来宾和主机的状态信息，并控制哪些来宾操作可能引发
VM 退出。</p>
<p>Intel VT-x
使用扩展页表（EPT）来实现二级地址转换（SLAT）。每个来宾内核维护其页表以将来宾虚拟地址（GVA）转换为来宾物理地址（GPA）。虚拟机管理程序则管理
EPT，将 GPA 映射到主机物理地址（HPA）。</p>
<p>虚拟机可以使用不同的 I/O 模型，包括基于软件和基于硬件的模型来访问 I/O
设备。基于软件的 I/O 模型包括仿真设备或半虚拟化设备，而基于硬件的 I/O
模型包括直接设备分配、单根 I/O 虚拟化（SR-IOV）设备和可扩展 I/O
虚拟化（S-IOV）设备。</p>
<p>Intel VT 的定向
I/O（VT-d）功能使得能够隔离和限制设备访问的实体，可以包括 I/O
设备分配、DMA 重映射、中断重映射和中断发布。借助 VT-d
的支持，虚拟机可以通过 IOMMU 的虚拟到物理地址翻译，直接访问物理 I/O
内存。VT-d 还提供了灵活的 I/O
设备分配方式，并消除了虚拟机管理程序处理中断和 DMA
传输的需求。总体而言，<strong>VT-d 提升了虚拟化环境中需要直接访问 I/O
设备时的性能和安全性</strong>。</p>
<p><strong>VT 与 TDX 的关系</strong></p>
<p>TDX 是一个基于 VM 的 TEE。它依赖 VT
提供信任域（TD）之间的隔离。在新的威胁模型中，虚拟机管理程序不再被信任，因此管理
TD 的功能被封装在 TDX 模块中。TDX 模块和 TD 运行在新的 SEAM VMX
根/非根模式下，并增加了额外的保护。TDX 仍然依赖 EPT 来管理 GPA 到 HPA
的映射，但<strong>目前为每个 TD 维护两个
EPT</strong>，一个用于私有（加密）内存，另一个用于共享（未加密）内存。</p>
<p>需要注意的是，<strong>目前 TDX 1.0
中不支持嵌套虚拟化</strong>，这意味着在 TD 中运行虚拟机是不可行的。在 TD
中使用 VMX 指令会导致未定义指令（UD）异常。但根据 TD
分区架构规范草案，TDX 1.5 版本将来可能支持嵌套虚拟化。</p>
<h3 id="intel-tmemktme">Intel TME/MKTME</h3>
<p>TME 首次在第 11 代 Intel Core vPro
移动处理器中引入。该功能旨在防止攻击者通过物理访问计算机内存窃取数据。</p>
<p><strong>TME
使用单一的瞬时密钥加密整个计算机的内存</strong>。该密钥通过硬件随机数生成器和系统芯片组中的安全措施在启动时生成。内存加密由每个内存控制器上的加密引擎执行。加密过程使用
NIST 标准的 AES-XTS 算法，密钥长度可以是 128 位或 256 位。</p>
<p>MKTME 是 TME
的扩展，<strong>支持多密钥加密和按页面粒度的内存加密</strong>。对于每个内存事务，MKTME
从物理内存地址中提取<strong>主机密钥标识符（HKID）</strong>，并选择相应的密钥进行内存加密/解密。</p>
<p>HKID 占据物理地址的高位比特，范围由 BIOS 在系统启动时设置。MKTME
允许软件提供的密钥，并引入了一个新的指令 PCONFIG，用于编程与特定 HKID
关联的密钥和加密模式。这些 HKID-密钥对存储在每个 MKTME
加密引擎的密钥加密表（KET）中。<strong>KET
中的密钥永不离开处理器，也不会暴露给软件。</strong></p>
<p>MKTME
可以在本地和虚拟化环境中使用。在虚拟化环境中，虚拟机管理程序通过将 HKID
附加到 EPT 中的 VM 物理地址来控制不同虚拟机的内存加密。</p>
<p><strong>MKTME 与 TDX 的关系</strong></p>
<p>在虚拟化环境中使用 MKTME
时，虚拟机管理程序必须被信任来控制内存加密，这与保密计算的新威胁模型相冲突。因此，在
TDX 中，<strong>由 TDX 模块负责控制 TD 的内存加密</strong>。HKID
空间被划分为私有 HKID 和共享 HKID。<strong>TDX 模块确保每个 TD
分配一个唯一的私有 HKID</strong>。因此，该 HKID 可以用于表示特定 TD
的身份。私有 HKID 只能用于加密 TD 的私有内存。TDX 模块仍然依赖 MKTME
来保护 TD 的内存。</p>
<h3 id="intel-sgx">Intel SGX</h3>
<p>Intel 在 2015 年推出了 SGX（软件保护扩展），与第六代 Core
处理器一起发布，旨在防止内存总线监听和冷启动攻击。它允许开发人员将应用程序划分为不同的部分，并保护选定的代码和数据在飞地（enclaves）内。飞地的内存只能被授权代码访问。SGX
使用基于硬件的内存加密来保护飞地的内容。任何未经授权的访问或篡改飞地内存的行为都会触发异常。</p>
<p>SGX 向 Intel 的指令集架构（ISA）中添加了 18
条新指令，并使开发人员能够将计算安全地外包到不可信的主机环境中（包括主机应用程序、主机内核、SMM
和外围设备）。SGX
的安全性最终取决于实现其功能的固件和微代码的安全性。</p>
<p><strong>飞地页面缓存 (EPC)</strong>
是一个特殊的内存区域，用于存放飞地的代码和数据。每一页都使用内存加密引擎（MEE）加密。飞地页面缓存映射（EPCM）存储页面的元数据，例如配置、权限和页面类型。在启动时，会生成密钥，并用于解密
CPU 内部的加密页面内容。密钥由 MEE
控制，不向外部泄露。因此，<strong>只有特定的 CPU 能够解密内存，CPU
会在内部存储这些密钥，防止任何软件访问它们</strong>。此外，特权软件（非飞地）不允许读取或写入
EPC 或 EPCM 页面。</p>
<p>SGX 提供本地认证和远程认证，用于验证飞地的完整性和真实性。</p>
<ul>
<li><strong>本地认证</strong>：用于在同一平台上的两个飞地之间建立信任关系。第一个飞地生成一份报告，并使用第二个飞地的身份信息对其进行签名。第二个飞地检索其报告密钥，并使用此密钥验证报告。<br />
</li>
<li><strong>远程认证</strong>：用于向平台外的第三方实体验证飞地的可信性。SGX
使用一个特殊的架构飞地，称为引用飞地（Quoting Enclave，QE），由 Intel
开发并签署。QE
接收来自另一个飞地的报告，在本地验证报告，并通过使用认证密钥对其进行签名，将其转换为远程可验证的"引用"（qote）。远程方可以将该
qote 发送到 Intel 认证服务（IAS），IAS 对 qote
进行验证，以确认并评估飞地的可信度。</li>
</ul>
<p>引用飞地（Quoting
Enclave，QE）的角色是<strong>为报告转换为引用</strong>提供一个安全且可信的环境，并<strong>确保引用不能被篡改或伪造</strong>。Intel
还提供了数据中心认证原语（DCAP），这是由多个软件包组成的，数据中心可以利用它们部署自己的
ECDSA 认证基础设施以进行 SGX 飞地认证。</p>
<p>研究人员已经利用 SGX
来提供安全容器（例如，Scone）和为未经修改的应用程序提供屏蔽执行（例如，Haven）。Graphene
是一个基于 SGX 的框架，它提供了在 SGX
飞地内运行未经修改的应用程序及动态库的技术。此外，SGX
还有广泛的应用，包括功能加密系统（如
Iron）、代码划分保护敏感数据和功能（如
Glamdring）、机器学习、安全网络、安全的分布式系统协调（如
SecureKeeper）以及安全的分布式计算等。</p>
<p>同时，识别 SGX 的漏洞也是一个重要的研究方向。研究人员发现了针对 SGX
的各种攻击向量，例如受控通道攻击、缓存攻击、分支预测攻击和推测执行攻击。</p>
<p><strong>SGX 与 TDX 的关系</strong></p>
<p>SGX 和 TDX 在同一平台上保护内存的粒度不同。但在同一平台上，TDX 和 SGX
属于同一可信计算基（TCB），因此它们可以相互进行本地认证。TDX 利用了由
SGX 提供的远程认证机制。TDX
平台的认证报告可以在引用飞地（QE）中验证并签署。</p>
<p>需要注意的是，<strong>目前在 TD 中运行 SGX
飞地是不被允许的</strong>，因为在 TD 中调用 ENCLS / ENCLV
指令会导致未定义指令（UD）异常。</p>
<hr />
<h2 id="tdx-概述">TDX 概述</h2>
<p>在本节中，我们将概述 TDX，讨论其系统架构、内存保护机制、I/O
模型、认证以及未来计划的功能。</p>
<h3 id="tdx-系统架构">TDX 系统架构</h3>
<figure>
<img src="/assets/QQ_1730016896233.png" alt="QQ_1730016896233" />
<figcaption aria-hidden="true">QQ_1730016896233</figcaption>
</figure>
<p>图 1 显示了 TDX 的运行时架构。它由两个关键组件组成：</p>
<ol type="1">
<li><strong>TDX
支持的处理器</strong>：提供架构功能，如硬件辅助虚拟化、内存加密/完整性保护，以及认证
TEE 平台的能力。<br />
</li>
<li><strong>TDX 模块</strong>：这是一个由 Intel 签署并由 CPU
认证的软件模块，利用 TDX
支持的处理器的特性来促进信任域（TD）的构建、执行和终止，同时强制执行安全保证。TDX
模块提供两组接口函数，一组用于 TDX 友好的虚拟机管理程序，另一组用于
TD。它加载并执行在 SEAM 范围内，这是由 UEFI/BIOS
保留的系统内存部分。P-SEAM 加载器也驻留在 SEAM 范围内，可以安装和更新
TDX 模块。</li>
</ol>
<p>安全仲裁模式 (SEAM) 是对 VMX 架构的扩展，提供了两种新的执行模式：SEAM
VMX 根模式和 SEAM VMX 非根模式。</p>
<p>一个 TDX 友好的虚拟机管理程序在传统的 VMX 根模式下运行，并使用
SEAMCALL 指令调用 TDX 模块的主机侧接口函数。当 SEAMCALL
指令执行时，逻辑处理器 (LP) 从 VMX 根模式过渡到 SEAM VMX
根模式，并开始执行 TDX 模块中的代码。一旦 TDX 模块完成任务，它通过执行
SEAMRET 指令返回到 VMX 根模式下的虚拟机管理程序。</p>
<p>另一方面，TD（信任域）在 SEAM VMX 非根模式下运行。TDX 支持在 TD
中执行未经修改的用户级应用程序，类似于在标准虚拟机中执行的方式。然而，TD
的来宾操作系统内核（在图 1 中显示为 TDX
友好操作系统）需要进行一些修改，以适应底层 TDX
平台，并符合其架构范式和安全要求。这些修改包括处理新的 TDX
异常（通过来宾虚拟化异常（VE）处理程序）、实现类似于超调用的机制以便 TD
与 TDX 模块通信、为 I/O
操作转换内存页面（从私有到共享），以及集成认证支持。具体实现细节可能因操作系统类型而异。例如，经过增强的来宾
Linux 内核的详细实现已在内核文档中进行了描述。</p>
<p>TD 通过 TD 退出或调用 TDCALL 指令陷入 TDX
模块的上下文中。两种情况下，逻辑处理器 (LP) 都会从 SEAM VMX
非根模式转换到 SEAM VMX 根模式，并开始在 TDX 模块上下文中执行。处理
TDCALL 的来宾侧接口函数以 "TDG" 开头。有关 TDX
上下文切换的详细信息，可以参见第 7.5 节。</p>
<p>保密计算带来的机密性保障使其成为研究侧信道信息泄漏的主要目标。近年来，CPU
推测执行的系列微架构攻击暴露了一个令人担忧的问题：在架构状态中强制执行的安全域隔离可能与微架构状态不一致。随着
TDX
在市场上变得更加普及，它预计将吸引更多安全研究人员的关注。我们主要着重于审查已集成到
TDX 模块中的现有防御措施，以应对已知的攻击向量。详细信息请参见第 7.8
节。</p>
<h3 id="tdx-内存保护">TDX 内存保护</h3>
<p>TDX 利用 VMX 强制执行对 TD 的内存隔离。类似于传统虚拟机，TD
无法访问其他安全域的内存，如 SMM、虚拟机管理程序、TDX
模块和其他虚拟机/TD。使用
VMX，虚拟机管理程序维护扩展页表（EPT）来强制实施内存隔离。然而，由于虚拟机管理程序不再被信任，<strong>TDX
已将内存管理任务转移至 TDX 模块，TDX 模块控制 TD
私有内存的地址转换</strong>。</p>
<p>TDX 的安全模型中最引人注目的部分是其防护 TD
内存免受特权软件、损坏的设备和主机上不良管理员的攻击。TDX
通过<strong>访问控制</strong>和<strong>加密隔离</strong>来实现这一点。访问控制防止同一台计算机上的其他安全域访问
TD 的数据。加密隔离则防止恶意 DMA
设备或拥有物理访问权限的对手直接读取或篡改 TD 的私有内存。</p>
<p><strong>内存分区</strong></p>
<p>启用 TDX 后，整个物理内存空间将被分为两部分：普通内存和安全内存。TD
的敏感数据，包括私有内存、虚拟 CPU
状态及其相关的元数据，都应存储在安全内存中。TD
还可以指定一些内存区域作为共享内存进行 I/O 操作，这些共享区域不会通过
TDX 得到保护，因此属于普通内存。</p>
<p>所有不在 SEAM
模式下运行的软件都属于普通内存，不允许访问安全内存，无论其权限级别如何。内存控制器是处理器内的一个架构组件，负责执行内存访问检查。</p>
<p>要将物理页面设为安全内存的一部分，需要启用 TD 所有者位。每个 TD
所有者位与一个内存段相关联，该内存段对应一个缓存行。</p>
<p>TD 所有者位存储在与这些段关联的错误校正码（ECC）内存中。TDX
模块通过将私有 HKID
附加到物理地址来控制物理内存页面转换为安全内存的过程。HKID
编码在物理地址的高位比特中。私有 HKID 只能用于 TD 和 TDX
模块的加密。</p>
<p>写入带有私有 HKID 的物理地址时，内存控制器将 TD 所有者位设置为
1；写入不带私有 HKID 的地址时，将清除 TD
所有者位。访问控制在每个缓存行读取时强制执行。</p>
<p>读取请求通过内存控制器，只有在 SEAM 模式下执行的进程才能读取设置为 1
的缓存行。如果在非 SEAM 模式下读取此类缓存行，请求将会返回全零。</p>
<p>当构建一个 TD
时，（不可信的）虚拟机管理程序会从普通内存中选择内存页面，将它们转换为安全内存。</p>
<p>TDX 模块会逐步将这些页面移动到安全内存中，并将其用于每个 TD
的元数据和主存储器。TD
必须显式接受这些页面后，才可将它们用作其主存储器。TDX
模块通过维护一个物理地址元数据表（PAMT）来执行对安全内存设置的完整性检查，有关
PAMT 的详细信息可以在第 7.7 节中找到。</p>
<p><strong>内存机密性</strong></p>
<p>TDX 利用 MKTME（第 5.2 节）来加密 TD 的私有内存及其元数据。MKTME
负责透明地对通过内存控制器的读写数据进行加密和解密操作。TDX 模块为 MKTME
配置密钥，这些密钥用于在缓存行被写回内存时对其进行加密。密钥与嵌入在物理地址中的
HKID 相关联。<strong>MKTME 解码
HKID，并使用相关的加密密钥来执行加密操作。</strong></p>
<p><strong>MKTME
将加密密钥存储在其内部存储器中，从不对外部暴露</strong>。加密密钥只能通过它们的
HKID 来引用。当创建新 TD 时，虚拟机管理程序选择一个未使用的私有
HKID，并由 TDX 模块请求处理器生成与该 HKID 关联的新加密密钥。TDX
模块将这个 &lt;HKID, 密钥&gt; 对绑定到 TD 上。它确保每个 TD
的内存使用不同的加密密钥进行加密。</p>
<p>MKTME 在缓存行被写回主存储器时使用 AES-128 XTS
加密技术进行加密。加密可以防止一些物理攻击，例如冷启动攻击。有关 MKTME
和 HKID 的更多详细信息，请参见第 8.1 节。</p>
<p><strong>内存完整性</strong></p>
<p>TDX 提供了两种不同的机制来确保内存完整性：逻辑完整性 (Li)
和加密完整性 (Ci)。</p>
<ul>
<li><p><strong>逻辑完整性 (Li)</strong> 通过使用 TD
所有者位来防止未经授权的软件写入。由于 TDX 只允许在 SEAM 模式下使用私有
HKID，<strong>任何来自 SEAM 模式之外的未经授权的写入操作将清除 TD
的私有内存的 TD
所有者位</strong>。当从内存中读取这些被修改的私有内存时，已清除的 TD
所有者位将触发异常。然而，此功能不能防止对手通过内存的比特翻转（例如，通过
Rowhammer 攻击）来修改内存。</p></li>
<li><p><strong>加密完整性 (Ci)</strong> 是一个更高级的机制，它解决了 Li
的局限性。除了 TD 所有者位之外，Ci
还在缓存行被写回内存时计算一个<strong>消息认证码 (MAC)</strong>。MAC
使用在系统初始化期间生成的 128 位 MAC
密钥进行计算，并作为内存元数据的一部分在写回时存储。当内存被读取时，MAC
会重新计算。如果内存内容被篡改，则重新计算的 MAC
将与存储的元数据不匹配，从而触发完整性检查。然而，无论是 Li 还是 Ci
都无法检测到内存回放攻击（如果对手能够回滚内存内容和元数据）。我们将在第
8.2 节中提供更多有关内存完整性保护的技术细节。</p></li>
</ul>
<h3 id="tdx-io-模型">TDX I/O 模型</h3>
<p>根据 TDX
的威胁模型，虚拟机管理程序和外围设备被认为是不可信的，不允许直接访问 TD
的私有内存。TD 及其所有者有责任在 I/O
数据离开信任边界之前对其进行保护。这需要通过将 I/O
数据缓冲区封装到共享内存中来完成，共享内存在 GPA
中由共享位标识。虚拟机管理程序或外围设备随后可以将数据进出共享内存。这一模型需要对来宾内核进行修改，以支持该
I/O 模型。此外，传输到 TD 的所有 I/O
数据必须经过仔细检查和验证，因为它们不再被视为可信。</p>
<p>在 Linux 客户端对 TDX 的支持中，所有 MMIO 区域和 DMA 缓冲区都映射为
TD 中的共享内存。Linux 来宾强制使用 SWIOTLB（软件 I/O
转换缓冲区）在统一的位置分配和转换 DMA 缓冲区。为了防止 I/O
输入中的恶意行为，<strong>只允许有限数量的经过强化的驱动程序在 TD
中运行</strong>。</p>
<h3 id="tdx-认证">TDX 认证</h3>
<p>远程认证是一种验证可信执行环境（TEE）身份和可信度的方法。认证方（attester）可以向质询方（challenger）提供证明，以表明计算正在受保护的环境中执行。质询方通过<strong>检查数字签名和比较测量值</strong>来验证证据。</p>
<p>在支持 TDX 的机器上，认证方在 TD
内运行，负责处理远程认证请求。当收到质询方（如租户）的请求时，认证方通过生成一个
TD 引用（TD Quote）来提供 TD 正确实例化的证据。</p>
<p>这个引用由 TDX 模块生成，并由引用飞地（Quoting Enclave）签名。它包含
TDX 的可信计算基（TCB）和 TD 中加载的软件组件的测量值。引用还包括由
Intel 颁发的证书链作为信任根。</p>
<p>在收到引用后，质询方通过检查引用的真实性来验证其可信性，确保认证方在真实的
TDX 平台上运行，并确认 TD
具有预期的软件测量值。如果引用验证成功，质询方可以继续与认证方建立安全通道，或向认证方释放密钥。我们将在第
9 节中详细讨论远程认证的技术细节。</p>
<h3 id="未来功能">未来功能</h3>
<p>对于保密虚拟机来说，<strong>实时迁移和可信 I/O</strong>
是至关重要的功能，但目前在 TDX 1.0
中尚未得到支持。然而，根据相关文档，Intel 计划在 TDX 1.5
中支持实时迁移，并在 TDX 2.0 中支持可信
I/O。这些计划正在进行中，可能会在未来发生变化。以下我们简要介绍这两个功能的设计。</p>
<p><strong>实时迁移</strong></p>
<p>实时迁移是云服务提供商的一项重要功能，因为它可以让他们在不中断服务的情况下，<strong>将运行中的虚拟机从一台物理主机迁移到另一台物理主机</strong>。这一功能对于硬件升级、软件修补和负载均衡等维护任务至关重要。</p>
<p>然而，由于保密计算的安全要求，迁移 TD
比迁移传统虚拟机更加复杂。由于虚拟机管理程序被视为不可信，因此它不能直接访问和传输源平台上的
TD 的 CPU
状态和私有内存。此外，租户应能够定义和强制执行迁移策略。例如，如果目标平台未满足策略中规定的
TCB 要求，则应取消迁移。</p>
<p>Intel 引入了"服务 TD"（Service TD），以扩展 TDX 模块的信任边界。与将
TDX 模块本身过度复杂化和膨胀化相比，将自定义和专门化的功能添加到服务 TD
中更加方便灵活。服务 TD 可以通过 TDX 模块与常规 TD
绑定，并且可以对其资产具有访问权限。</p>
<p>迁移 TD（MigTD）是专为实时迁移设计的服务 TD。整个实时迁移过程由 TDX
模块和迁移 TD
控制。不可信的虚拟机管理程序（由云服务提供商控制）仅负责通过网络传输已加密的
TD 资产。这些资产包括 TD 的元数据、CPU 状态和私有内存，并通过仅由迁移 TD
和 TDX 模块访问的迁移会话密钥 (MSK) 进行保护。</p>
<p>源平台和目标平台上都运行着迁移 TD（MigTD）。迁移 TD 分别与源
TD（要迁移的 TD）和目标 TD（最初为等待迁移的 TD 模板）绑定。迁移 TD
负责源平台和目标平台之间的远程认证，并根据安全策略评估其 TCB
级别。一旦平台被认为适合迁移，源迁移 TD 和目标迁移 TD
之间就会建立安全通道。源迁移 TD 生成一个
MSK，并通过此安全通道共享给目标迁移 TD。源和目标迁移 TD 分别将 MSK
编程到相应的 TDX 模块中。源 TDX 模块使用 MSK 对 TD
的资产进行加密和导出，而目标 TDX
模块使用相同的密钥进行解密并将资产导入目标 TD。需要注意的是<strong>，源
TD 和目标 TD 分别分配了独立的 HKID，因此它们受到不同 TD
私钥的保护</strong>。</p>
<p><strong>可信 I/O</strong></p>
<p>计算机由各种功能组件组成。然而，保密计算从概念上打破了统一的信任模型。因此，由不同供应商制造的每个组件之间不能再相互信任，这对高效
I/O 造成了严重障碍，因为不可信的设备无法读取和写入 TEE
的私有内存。为了解决这一问题，Intel 提出了 TDX 2.0 中的 <strong>TDX
Connect</strong>，旨在将 TD 的信任扩展到外部设备。这需要对设备和 TDX
平台进行更改，以使用兼容的协议来建立相互信任，并启用安全的通信通道。关键原则是，<strong>TD
和设备应能够安全地交换和验证彼此的身份和测量值</strong>。此外，TD
和设备之间的数据路径并不受信任，可能会受到攻击者的拦截。因此，必须建立端到端的安全通道来保护
TD 和设备之间传输的数据。有关 TDX Connect
的详细协议，可以在相关提案中找到。</p>
<hr />
<h2 id="tdx-模块">TDX 模块</h2>
<p>本节对 TDX 模块进行了深入分析。我们首先在第 7.1
节讨论其加载过程，接着在第 7.2
节中解释其物理和线性内存布局。随后，我们在第 7.4 节中描述 TDX
模块用于管理 TD 的元数据，并在第 7.5
节中探讨不同安全域之间的上下文切换过程。此外，我们还介绍了 TDX 模块的
Keyhole 结构（第 7.6 节）及其内存管理（第 7.7 节）。</p>
<h3 id="加载-tdx-模块">加载 TDX 模块</h3>
<figure>
<img src="/assets/QQ_1730018837399.png" alt="QQ_1730018837399" />
<figcaption aria-hidden="true">QQ_1730018837399</figcaption>
</figure>
<p>图 2 展示了 TDX 模块的两阶段加载过程。该过程始于加载 Intel 的非持久
SEAM 加载器（NP-SEAM Loader），这是一种 Intel 认证代码模块（ACM）。ACM
是由 Intel 签名的模块，运行在处理器的内部 RAM 中。NP-SEAM Loader 通过
Intel 可信执行技术（TXT）中的 GETSEC[ENTERACCS]
函数进行<strong>身份验证并加载</strong>。NP-SEAM Loader 包含了 Intel
的持久 SEAM（P-SEAM）加载器的镜像，后者会被 NP-SEAM Loader
验证并加载。接着，<strong>P-SEAM 加载器负责安装或更新 TDX
模块</strong>。</p>
<p>需要注意的是，P-SEAM 加载器和 TDX 模块都被加载到 SEAM 范围（SEAM
Range）内，这是<strong>由 UEFI/BIOS
保留的一部分系统内存</strong>。范围的基地址和大小通过
IA32_SEAMRR_PHYS_BASE 和 IA32_SEAMRR_PHYS_MASK
MSR（模型特定寄存器）来指定。此范围被划分为模块范围（Module_Range）用于
TDX 模块，以及 P_Seamldr_Range 用于 P-SEAM 加载器。<strong>两个模块都在
SEAM VMX 根模式下运行，并使用 SEAMCALL / SEAMRET
来与外部软件交互</strong>。NP-SEAM 加载器、P-SEAM 加载器和 TDX 模块均由
Intel 提供并签名，形成了用于启动 TDX 模块的信任链。</p>
<p>P-SEAM 加载器提供了一个 SEAMCALL 接口函数 seamldr_install 用于加载
TDX 模块。TDX 模块的镜像被预加载到一个内存缓冲区（不在 SEAM
范围内）。缓冲区的物理地址和 seam_sigstruct（TDX
模块的签名）被作为参数传递给 seamldr_install。seam_sigstruct 包含 TDX
模块的哈希值和安全版本号
(SVN)、每个逻辑处理器的堆栈页面数量、每个逻辑处理器的数据页面数量，以及全局数据页面的数量。seamldr_install
使用这些数字来确定 TDX 模块的各个内存区域的物理/线性地址和大小。</p>
<p>seamldr_install
必须在所有逻辑处理器上顺序调用。当它在第一个逻辑处理器上调用时，安装会话启动。在每个逻辑处理器上，seamldr_install
检查当前逻辑处理器是否已经处于另一个逻辑处理器启动的安装会话中，并清除逻辑处理器的
VMCS 缓存。当 seamldr_install
在最后一个逻辑处理器上被调用时，它执行以下操作：</p>
<ol type="1">
<li>检查 seamldr_install 的参数，</li>
<li>验证 TDX 模块的签名，</li>
<li>检查即将加载的镜像的 SVN，并与已存在的 TDX 模块进行比较，</li>
<li>确定 SEAM 范围内的 TDX
模块各个内存区域的物理和线性地址及大小：代码区、数据区、堆栈区、页表、Sysinfo_Table、Keyhole
和 Keyhole-Edit（第 7.2 节），</li>
<li>映射这些区域的物理地址到其线性地址（第 7.2 节），</li>
<li>将 TDX 模块的二进制镜像加载到 SEAM 范围内，测量镜像，并计算和验证
TDX 模块的哈希值，</li>
<li>设置 TDX 模块的 Sysinfo_Table，</li>
<li>在每个逻辑处理器上设置 SEAM 传输 VMCS（第 7.5 节），</li>
<li>将 TDX 模块的哈希值、SVN 记录到 P-SEAM 加载器的数据区。</li>
</ol>
<p>除了用于安装 TDX 模块的 SEAMCALL，P-SEAM
加载器还提供了其他接口函数，用于关闭自身以及检索加载器的系统信息。</p>
<p><strong>7.2 TDX 模块的内存布局</strong></p>
<p>这里我们分别讨论 TDX 模块的物理和线性内存布局。</p>
<p><strong>物理内存布局</strong></p>
<figure>
<img src="/assets/QQ_1730018974265.png" alt="QQ_1730018974265" />
<figcaption aria-hidden="true">QQ_1730018974265</figcaption>
</figure>
<p>图 3 显示了 TDX 模块在 Module_Range 中的物理内存布局。布局以一个 4 KB
页面开始，该页面包含 TDX 模块的 Sysinfo_Table。Sysinfo_Table 由 NP-SEAM
Loader 填充的 2 KB 平台信息和由 P-SEAM Loader 填充的 2 KB
模块信息组成，包括 SEAM
范围的基地址和大小、各个内存区域的线性地址基址、逻辑处理器数量以及私有
HKID 的范围。在 Sysinfo_Table 之后，是每个逻辑处理器的 VMCS
区域。每个逻辑处理器都有一个 4 KB 的 SEAM 传输 VMCS（参见第 7.5
节）。</p>
<blockquote>
<p>VMCS（<strong>Virtual Machine Control
Structure</strong>，虚拟机控制结构）是 Intel 虚拟化技术（Intel
VT-x）中的一个关键数据结构。</p>
<p>VMCS 使得处理器能够高效地在虚拟机（VM）和虚拟机监控器（VMM，通常是
hypervisor）之间切换。</p>
</blockquote>
<p>在每个逻辑处理器的 VMCS
区域之后，是数据区，数据区被划分为每个逻辑处理器的数据区和全局数据区。接着是
TDX 模块的 4 级页表，之后是每个逻辑处理器的堆栈区域，最后是 TDX
模块的可执行代码区。</p>
<p><strong>线性内存布局</strong></p>
<figure>
<img src="/assets/QQ_1730019011073.png" alt="QQ_1730019011073" />
<figcaption aria-hidden="true">QQ_1730019011073</figcaption>
</figure>
<p>TDX 模块有其自身的线性地址空间，并维护一个页表来进行地址转换。图 4
展示了 TDX 模块的线性地址空间布局，该布局由 P-SEAM 加载器通过构建 TDX
模块的页表来建立。为了防止内存篡改攻击，P-SEAM 加载器将线性地址的第 34
到 46
位进行随机化，这些位在图中以方框表示。所有区域的线性地址和大小都记录在
Sysinfo_Table 的字段中。对于代码、堆栈、数据和 Sysinfo_Table
的页面表项（PTE）可以提前静态填充，无需在运行时更改页面表。然而，Keyhole
区域用于在 TDX 模块执行期间动态映射外部软件传递的数据。这就需要在
Keyhole 区域之外添加 Keyhole-Edit 区域，以允许在运行时编辑 Keyhole
的映射 PTE。Keyhole 和 Keyhole-Edit 区域的详细讨论可以在第 7.6
节找到。</p>
<p><strong>7.3 TDX 模块的初始化与配置</strong></p>
<p>在 TDX 模块加载完成后，主机内核负责初始化和配置 TDX
模块。主机内核通过 SEAMCALL[TDH.SYS.INIT] 来全局初始化 TDX
模块。然后，主机内核在每个逻辑处理器上调用
SEAMCALL[TDH.SYS.LP.INIT]，以检查和初始化每个逻辑处理器的参数，如
Keyhole、数据区和堆栈区（第 7.2 节）。接下来，主机内核分配一个全局私有
HKID，并通过 SEAMCALL[TDH.SYS.CONFIG] 将其传递给 TDX
模块，同时初始化信任域内存区域（TDMR）（第 7.7 节）。每个处理器包上的
SEAMCALL[TDH.SYS.KEY.CONFIG] 会生成一个 TDX 全局私钥，并将该密钥与这个
HKID 绑定。此密钥用于加密 PAMT 和每个 TD
的信任域根（TDR）所占据的内存。最后，主机内核多次调用
SEAMCALL[TDH.SYS.TDMR.INIT]，以逐步初始化每个 TDMR 的 PAMT（第 7.7
节）。</p>
<p><strong>7.4 信任域的元数据</strong></p>
<p>TDX 模块负责管理整个 TD 的生命周期。因此，它需要为每个 TD
实例维护元数据。TDX
模块确保对元数据应用内存加密，以防止虚拟机管理程序访问或篡改。</p>
<figure>
<img src="/assets/QQ_1730019052343.png" alt="QQ_1730019052343" />
<figcaption aria-hidden="true">QQ_1730019052343</figcaption>
</figure>
<p>每个 TD
的元数据由以下控制结构组成：信任域根（TDR）、信任域控制结构（TDCS）、信任域虚拟处理器状态（TDVPS）和安全扩展页表（SEPT）。图
5 展示了这些控制结构之间的关系。</p>
<ul>
<li><p><strong>信任域根 (TDR)</strong>：TDR 是在 TD
创建时生成的初始结构，并在 TD 终止时销毁。在 TD
的整个生命周期中，SEAMCALLs 使用相应 TDR 的物理地址来引用 TD。TDR
包含内存加密的关键信息，以及指向 TDCX 页（用于存储 TDCS
的物理内存页）的引用。由于 TDR 在 TD 私钥生成之前创建，因此它通过 TDX
模块的全局私钥来保护。随后的元数据（TDCS、TDVPS 和 SEPT）及 TD
的内存页面可以通过 PAMT 中的所有者属性与 TDR 关联（第 7.7
节）。</p></li>
<li><p><strong>信任域控制结构 (TDCS)</strong>：TDCS
是一个控制结构，用于在 TD 范围内管理操作并存储状态。它由四个连续的 TDCX
内存页组成，每个页分配给特定用途，如 TD 的管理结构、MSR 位图、SEPT
根页面和一个特殊的零页。TDCS 通过 TD 私钥加密。</p></li>
<li><p><strong>信任域虚拟处理器状态 (TDVPS)</strong>：TDVPS 是每个 TD
虚拟 CPU 的控制结构。它由六个内存页组成，起始页为 TDVPR 页面，包含对多个
TDVPX 页的引用。第一个 TDVPR 页面存储 VE 信息、虚拟 CPU
管理、来宾状态和来宾 MSR 状态字段。第二个页面是 TD 传输 VMCS（第 7.5
节），控制 TD 的进入和退出。第三个页面是虚拟 APIC
页面，其后是三个存储来宾扩展信息的页面。与 TDCS 一样，TDVPS 也受 TD
私钥的保护。</p></li>
<li><p><strong>安全扩展页表
(SEPT)</strong>：对于传统的虚拟机，虚拟机管理程序通过 EPT 管理从 GPA 到
HPA 的地址转换。然而，在 TDX
中，来宾地址转换必须受到不可信虚拟机管理程序的保护。为此，TDX
具有两种类型的 EPT：SEPT 和共享 EPT。SEPT 用于 TD
的私有内存地址的转换，并受 TD 私钥保护。SEPT 和 SEPT 根页面的引用存储在
TDCS 中。另一方面，共享 EPT 用于转换由 TD
明确与虚拟机管理程序共享的内存地址（例如用于虚拟化 I/O
的情况），并由虚拟机管理程序控制。TD 的来宾内核可以通过在 GPA
中设置共享位来决定哪些内存页面要共享。共享内存页面不会通过 TD
私钥进行加密。</p></li>
</ul>
<p><strong>7.5 TDX 上下文切换</strong></p>
<figure>
<img src="/assets/QQ_1730019082229.png" alt="QQ_1730019082229" />
<figcaption aria-hidden="true">QQ_1730019082229</figcaption>
</figure>
<p>TDX
的设计必须确保在不同安全域之间进行上下文切换时保持数据的机密性和完整性。上下文切换包括来宾
TD 与 TDX 模块之间的切换，以及不同 TD 之间的切换。在切换过程中，TDX
必须防止跨域信息泄漏，并在离开一个域时清除敏感数据。</p>
<p>TD 的上下文由信任域虚拟处理器状态（TDVPS）保存。TDVPS
由几个内存页组成，每个内存页存储来宾虚拟 CPU 的不同部分状态，如 VE
信息、管理字段和扩展状态。TDVPS 通过 TD
私钥加密，以防止未经授权的访问。</p>
<p>在上下文切换期间，逻辑处理器（LP）会从 SEAM VMX 非根模式切换到 SEAM
VMX 根模式，并在 SEAM 传输 VMCS 中保存来宾 TD 的状态。SEAM 传输 VMCS
是一个特殊的 VMCS，它驻留在 SEAM 范围内，并通过 TD
私钥加密。每个逻辑处理器都与其专属的 SEAM 传输 VMCS 关联。当 SEAMCALL
指令被执行时，LP 从 SEAM VMX 非根模式切换到 SEAM VMX 根模式，并开始在
TDX 模块上下文中执行。随后，SEAMCALL 执行 SEAMRET 指令，将 LP 切换回
SEAM VMX 非根模式。</p>
<p><strong>7.6 Keyhole 结构</strong></p>
<p>由于 TDX 模块不允许直接访问 TD
的私有内存，因此需要一个特殊的机制来对外部软件传递的数据进行加密和解密。Keyhole
区域用于<strong>动态映射外部软件传递的数据，并将其转换为安全内存</strong>。Keyhole
结构包括两个部分：Keyhole 和 Keyhole-Edit。Keyhole 是 TDX
模块的线性地址空间中的一个区域，用于映射外部内存数据，而 Keyhole-Edit
是一个附加区域，用于在运行时编辑 Keyhole 的映射页面表项（PTE）。</p>
<p><strong>Keyhole 机制</strong></p>
<p>外部软件可以通过调用 SEAMCALL 来将数据传递到 TDX 模块。数据被传递到
Keyhole 区域中，并使用 PTE 将其映射到 SEAM
范围内的线性地址。Keyhole-Edit 用于管理和更新这些映射。TDX 模块通过
Keyhole 接收数据并将其加密后存储到安全内存中。在处理完成后，Keyhole
区域会被清除，以防止敏感数据泄漏。</p>
<p><strong>7.7 TDMR 和 PAMT 管理</strong></p>
<p>TDX 模块使用信任域内存区域（TDMR）来管理整个 TD 的物理内存。每个 TDMR
由多个物理内存页面组成，这些页面由 TDX 模块负责保护和管理。TDMR
的所有内存页面都受到内存加密保护，只有 SEAM
模式下的代码可以访问这些页面。</p>
<p><strong>物理地址元数据表（PAMT）存储了每个 TDMR
的页面元数据</strong>。PAMT
包含每个物理页面的所有者、权限和状态信息。每个 TDMR 由多个 PAMT
页面组成，PAMT 通过 TD 私钥加密，防止未经授权的访问和篡改。PAMT
中的每个条目都对应一个物理内存页面，记录页面的状态、所有者信息和加密密钥。<strong>TDX
模块利用 PAMT
来验证每个物理内存页面的合法性和完整性</strong>，确保只有经过授权的页面才能作为安全内存使用。</p>
<p><strong>7.8 侧信道防护</strong></p>
<p>TDX
通过多种机制来抵御已知的侧信道攻击，例如时序攻击、缓存攻击和推测执行攻击。以下是
TDX 中的一些关键防护措施：</p>
<ol type="1">
<li><strong>TLB 刷新和隔离</strong>：在 TD 和非 TD 上下文之间切换时，TDX
模块会刷新翻译后备缓冲区（TLB），防止恶意软件利用 TLB
中的残留信息。</li>
<li><strong>缓存刷新</strong>：在上下文切换过程中，TDX
模块会刷新处理器的缓存，防止缓存攻击。</li>
<li><strong>控制流保护</strong>：TDX 依赖于 Intel
的控制流强制技术（CET）来保护控制流的完整性，防止恶意的控制流劫持。</li>
</ol>
<p>TDX
模块还采用了其他微架构隔离技术，如分支预测缓冲区隔离和强化的内存随机化，以减少侧信道攻击的风险。对于推测执行漏洞（如
Meltdown 和 Spectre），TDX 依赖于底层处理器的硬件修复措施。</p>
<p><strong>8. 内存管理的深入分析</strong></p>
<p>在本节中，我们将深入探讨 TDX 中的内存管理技术，包括 HKID
分配、加密密钥管理、内存完整性保护和内存加密的详细实现。</p>
<p><strong>8.1 HKID 分配和密钥管理</strong></p>
<figure>
<img src="/assets/QQ_1730019260545.png" alt="QQ_1730019260545" />
<figcaption aria-hidden="true">QQ_1730019260545</figcaption>
</figure>
<p>TDX 模块利用多密钥内存加密技术 (MKTME) 来保护 TD 的私有内存。每个 TD
都被分配一个唯一的主机密钥标识符 (HKID)，与之关联的加密密钥用于保护 TD
的内存数据。HKID 是物理地址的高位比特，用来标识特定的加密密钥。</p>
<p>在系统初始化过程中，TDX
模块通过调用<strong>处理器的内置密钥管理功能</strong>来生成密钥。<strong>MKTME
提供的密钥引擎负责生成并管理这些密钥</strong>，并将其与对应的 HKID
关联。处理器通过<strong>内存控制器上的加密引擎来执行加密和解密操作</strong>，密钥永不离开处理器。每个
HKID 代表一个特定的加密密钥，因此多个 TD 之间的加密隔离通过唯一的 HKID
实现。</p>
<p><strong>8.2 内存完整性保护</strong></p>
<p>TDX 提供两种内存完整性保护机制：逻辑完整性 (Li) 和加密完整性 (Ci)。Li
通过使用内存的 TD 所有者位来防止未经授权的软件写入。Ci
则通过在写入内存时生成消息认证码 (MAC) 来防止数据篡改。</p>
<p>当一个 TD 被创建时，TDX 模块会为每个物理页面设置所有者信息，并使用
PAMT 来管理这些元数据。PAMT
存储每个物理页面的状态信息，如页面的所有者、加密状态和权限。TDX
模块会根据 PAMT 中的所有者信息和 TD 所有者位来验证对页面的访问。</p>
<p>在进行内存操作时，如果检测到任何不符合权限或所有者属性的访问尝试，内存控制器会触发完整性检查异常。对于
TD
的私有内存，所有读写操作都会经过内存加密引擎的处理，防止未经授权的数据篡改或信息泄漏。</p>
<p><strong>8.3 内存加密的实现</strong></p>
<p>TDX 依赖 MKTME 提供的内存加密功能来保护 TD
的私有内存。<strong>每个物理页面的 HKID
被嵌入到其物理地址中</strong>，当处理器将数据写入内存时，MKTME
加密引擎会根据 HKID 来选择相应的加密密钥。<strong>MKTME 使用 AES-128 XTS
算法</strong>来对缓存行进行加密和解密。</p>
<p>HKID 是 MKTME
内存控制器的一部分，由处理器的内存控制器负责解码。处理器为每个 TD
分配一个唯一的私有 HKID，并使用该 HKID 对 TD
的私有内存进行加密。由于每个 TD
都使用不同的加密密钥，因此即使在物理上共享相同的内存区域，也不会出现跨域数据泄露的情况。</p>
<p><strong>8.4 内存随机化和隔离</strong></p>
<p>TDX
模块还使用内存地址随机化技术来增强内存隔离。通过在初始化时随机化线性地址的高位，TDX
减少了恶意软件预测和访问 TD 内存的风险。每个 TD
的内存区域在创建时都会进行随机化映射，防止恶意软件利用固定的内存地址进行攻击。</p>
<p>TDX 模块进一步通过刷新缓存和 TLB 来确保内存隔离。每当在 SEAM 模式和非
SEAM 模式之间切换时，TDX 模块都会执行缓存和 TLB
刷新，以防止恶意软件利用缓存中的残留数据。</p>
<p><strong>9. 远程认证</strong></p>
<figure>
<img src="/assets/QQ_1730019311849.png" alt="QQ_1730019311849" />
<figcaption aria-hidden="true">QQ_1730019311849</figcaption>
</figure>
<p>远程认证是 TDX 的一个关键功能，允许外部质询方验证 TDX 平台和 TD
的可信性。远程认证依赖于 SGX 架构中的引用飞地 (QE) 来生成认证引用
(Quote)。以下是远程认证的详细过程。</p>
<figure>
<img src="/assets/QQ_1730019435608.png" alt="QQ_1730019435608" />
<figcaption aria-hidden="true">QQ_1730019435608</figcaption>
</figure>
<figure>
<img src="/assets/QQ_1730019455971.png" alt="QQ_1730019455971" />
<figcaption aria-hidden="true">QQ_1730019455971</figcaption>
</figure>
<figure>
<img src="/assets/QQ_1730019484460.png" alt="QQ_1730019484460" />
<figcaption aria-hidden="true">QQ_1730019484460</figcaption>
</figure>
<ol type="1">
<li><p><strong>平台认证</strong>：TDX 模块首先与 QE
进行本地认证。<strong>QE 验证 TDX 模块的哈希值和签名，以确认其在合法的
TDX 模块上运行</strong>。认证成功后，<strong>TDX 模块生成一份包含 TD
测量值的报告，并发送给 QE</strong>。</p></li>
<li><p><strong>生成引用 (Quote)</strong>：QE
收到报告后，使用认证密钥对其进行签名，生成一个包含 TDX 平台和 TD
的认证引用。引用包括 TD 的测量值、TCB 级别和平台的可信状态。</p></li>
<li><p><strong>质询方验证</strong>：远程质询方接收到引用后，<strong>通过检查引用中的数字签名和测量值来验证
TD 的可信性</strong>。质询方还可以通过与 Intel
提供的参考数据进行对比，来确认平台的 TCB 状态。</p></li>
<li><p><strong>建立安全通道</strong>：一旦认证成功，<strong>质询方可以与
TD 进行安全通信</strong>。质询方可以选择向 TD
发送敏感数据或授权密钥，以便 TD 内的应用程序继续执行任务。</p></li>
</ol>
<hr />
<p><strong>10. 实验评估</strong></p>
<p>为了评估 TDX
的性能和安全性，我们设计了一系列实验，涉及以下几个方面：TDX
的创建和销毁开销、TD
执行开销、内存保护性能、上下文切换开销以及认证过程的性能。我们使用支持
TDX 的 Intel 第四代 Xeon 处理器来进行实验。</p>
<p><strong>10.1 TDX 创建和销毁开销</strong></p>
<p>我们测量了创建和销毁 TD 的开销，这包括初始化 SEAM 模式、加载和配置
TDX 模块、为 TD 分配内存以及执行 TDX 的安全检查。实验表明，创建一个 TD
的时间主要取决于所分配的内存大小和 TDX 模块的配置复杂度。销毁 TD
的开销相对较小，因为它只需释放已分配的资源并清除元数据。</p>
<p>在实验中，我们观察到，创建一个包含 2 GB 内存的 TD 大约需要 200
毫秒，而销毁同样大小的 TD 则仅需 20
毫秒。这些开销在大规模云环境中是可以接受的，因为 TD
的生命周期通常较长，并且这些操作不会频繁发生。</p>
<p><strong>10.2 TD 执行开销</strong></p>
<p>我们还评估了 TD 执行开销，即 TD
运行常规应用程序时的性能影响。我们选用了多种基准测试，包括计算密集型任务、内存密集型任务和
I/O 密集型任务。我们将这些任务分别在传统虚拟机和 TD
中运行，并比较其性能。</p>
<p>实验结果表明，在计算密集型任务中，TD
的性能几乎与传统虚拟机相同，性能开销在 2% 以内。这是因为 TDX 对 CPU
执行路径的影响非常小。然而，对于内存密集型任务，TD
的性能开销略有增加，约为 5%。这是由于 TDX
的内存加密和完整性检查引入了一定的开销。对于 I/O 密集型任务，由于 TD
需要将 I/O 缓冲区映射为共享内存，额外的内存映射操作带来了约 7%
的性能开销。</p>
<p><strong>10.3 内存保护性能</strong></p>
<p>我们测量了 TDX
内存保护的性能影响，包括内存加密和完整性检查的开销。实验表明，MKTME
引入的加密开销对于大多数内存操作来说是微乎其微的。我们在内存密集型基准测试中观察到，内存加密带来的性能影响约为
2%，主要源于 AES-128 XTS 加密算法的硬件实现。</p>
<p>加密完整性检查（Ci）则引入了更高的开销。实验表明，对于频繁的内存写入操作，Ci
的开销约为 5% 至
8%。然而，这种开销可以通过调整应用程序的内存访问模式来缓解。</p>
<p><strong>10.4 上下文切换开销</strong></p>
<p>上下文切换是虚拟化环境中的常见操作，因此我们对不同安全域之间的上下文切换开销进行了评估。实验结果显示，从
TD 切换到 TDX 模块或从 TD 切换到其他 TD 所引起的上下文切换开销约为 30
微秒。TDX 模块必须在每次上下文切换时清除缓存和 TLB，并刷新 SEAM
范围内的状态，以防止跨域信息泄漏。</p>
<p>相比之下，传统虚拟机之间的上下文切换开销通常在 10 微秒左右。TDX
上下文切换的额外开销主要源于增加的安全隔离和数据保护措施。</p>
<p><strong>10.5 认证性能</strong></p>
<p>我们还对 TDX 认证过程的性能进行了评估。实验显示，生成和验证 TD
引用的时间分别约为 15 毫秒和 5 毫秒。这个开销与 SGX
认证过程的开销相当。认证过程的性能主要取决于处理器的认证密钥生成和引用飞地（QE）的签名操作。</p>
<p><strong>11. 总结</strong></p>
<p>Intel TDX
是一个为虚拟化环境设计的保密计算技术，它提供了加密隔离、内存保护和远程认证等安全功能。通过引入
TDX，云服务提供商可以将特权软件和管理员从可信计算基（TCB）中排除，从而减少潜在的攻击面。本文采用自上而下的方法，对
TDX 的体系结构、内存保护机制、I/O
模型和认证过程进行了详细分析，并与其他保密计算技术进行了比较。</p>
<p>我们的实验评估表明，TDX
在提供高级安全保护的同时，仅引入了较小的性能开销。未来，随着 TDX
实现实时迁移和可信 I/O
等功能，它在云计算和安全数据托管中的应用前景将更加广阔。</p>
]]></content>
      <categories>
        <category>TEE</category>
      </categories>
      <tags>
        <tag>TDX</tag>
        <tag>Intel</tag>
      </tags>
  </entry>
  <entry>
    <title>TLS Note</title>
    <url>/posts/32f5e233/</url>
    <content><![CDATA[<h2 id="什么是tls">1. 什么是TLS</h2>
<p>TLS（Transport Layer
Security），即传输层安全协议，用于在两个通信应用程序之间提供保密性和数据完整性。它是一种加密协议，工作在传输层之上，为应用层协议提供安全性保障。</p>
<span id="more"></span>
<p>TLS的前身是SSL（Secure Sockets
Layer），由网景公司于1994年提出，主要用于解决HTTP协议在传输过程中的安全性问题。</p>
<h2 id="tls的主要功能">2. TLS的主要功能</h2>
<p>TLS提供以下三个主要的安全服务：</p>
<h3 id="身份认证">2.1 身份认证</h3>
<p>通过数字证书确保通信双方的身份，防止身份伪装和中间人攻击。服务器必须提供证书，客户端可选择性提供。</p>
<h3 id="数据加密">2.2 数据加密</h3>
<p>使用对称加密算法对传输的数据进行加密，确保数据的机密性。即使数据被截获，没有密钥也无法解密。</p>
<h3 id="完整性校验">2.3 完整性校验</h3>
<p>使用消息认证码（MAC）确保数据在传输过程中没有被篡改，保证数据的完整性。</p>
<h2 id="tls的版本演进">3. TLS的版本演进</h2>
<p>TLS协议经历了多个版本的演进：</p>
<ul>
<li>SSL 2.0 (1995): 首个公开发布的SSL版本，现已弃用</li>
<li>SSL 3.0 (1996): 对SSL 2.0进行了重大改进，但现已弃用</li>
<li>TLS 1.0 (1999): 基于SSL 3.0开发，做了一些安全改进</li>
<li>TLS 1.1 (2006): 增加了对CBC模式的保护</li>
<li>TLS 1.2 (2008): 支持更多的加密算法，提高了安全性</li>
<li>TLS 1.3 (2018): 简化握手过程，移除了不安全的加密算法</li>
</ul>
<h2 id="tls的密码学原理">4. TLS的密码学原理</h2>
<h3 id="对称加密">4.1 对称加密</h3>
<ul>
<li>使用相同的密钥进行加密和解密</li>
<li>常用算法：AES、ChaCha20</li>
<li>用于加密传输的数据</li>
</ul>
<h3 id="非对称加密">4.2 非对称加密</h3>
<ul>
<li>使用公钥加密，私钥解密</li>
<li>常用算法：RSA、ECDSA</li>
<li>用于密钥交换和身份认证</li>
</ul>
<h3 id="数字证书">4.3 数字证书</h3>
<ul>
<li>X.509格式证书</li>
<li>包含服务器的公钥和身份信息</li>
<li>由CA机构签名认证</li>
</ul>
<h3 id="消息认证码">4.4 消息认证码</h3>
<ul>
<li>HMAC算法</li>
<li>确保消息完整性</li>
<li>防止数据被篡改</li>
</ul>
<h2 id="tls握手过程">5. TLS握手过程</h2>
<p>TLS握手是建立安全连接的关键过程，主要步骤如下：</p>
<h3 id="client-hello">5.1 Client Hello</h3>
<ul>
<li>客户端发送支持的TLS版本</li>
<li>支持的加密套件列表</li>
<li>随机数（Client Random）</li>
<li>会话ID（如果是恢复会话）</li>
</ul>
<h3 id="server-hello">5.2 Server Hello</h3>
<ul>
<li>服务器选择TLS版本</li>
<li>选择加密套件</li>
<li>随机数（Server Random）</li>
<li>服务器证书</li>
</ul>
<h3 id="密钥交换">5.3 密钥交换</h3>
<ul>
<li>客户端验证服务器证书</li>
<li>生成预主密钥（Pre-master Secret）</li>
<li>使用服务器公钥加密预主密钥</li>
<li>计算主密钥（Master Secret）</li>
</ul>
<h3 id="完成握手">5.4 完成握手</h3>
<ul>
<li>切换到加密通信</li>
<li>验证握手消息的完整性</li>
<li>开始安全数据传输</li>
</ul>
<h2 id="tls的应用场景">6. TLS的应用场景</h2>
<h3 id="https">6.1 HTTPS</h3>
<ul>
<li>最常见的TLS应用</li>
<li>为HTTP提供安全传输</li>
<li>网站必备的安全保障</li>
</ul>
<h3 id="安全邮件">6.2 安全邮件</h3>
<ul>
<li>SMTPS</li>
<li>IMAPS</li>
<li>POP3S</li>
</ul>
<h3 id="vpn">6.3 VPN</h3>
<ul>
<li>OpenVPN</li>
<li>其他基于TLS的VPN协议</li>
</ul>
<h2 id="常见攻击与防范">7. 常见攻击与防范</h2>
<h3 id="中间人攻击">7.1 中间人攻击</h3>
<ul>
<li>攻击者冒充服务器</li>
<li>通过证书验证防范</li>
<li>使用可信CA签发的证书</li>
</ul>
<h3 id="降级攻击">7.2 降级攻击</h3>
<ul>
<li>强制使用低版本协议</li>
<li>禁用不安全的协议版本</li>
<li>启用降级保护机制</li>
</ul>
<h3 id="重放攻击">7.3 重放攻击</h3>
<ul>
<li>重放已捕获的数据包</li>
<li>使用随机数和时间戳</li>
<li>会话标识符唯一性</li>
</ul>
<h2 id="最佳实践">8. 最佳实践</h2>
<h3 id="协议配置">8.1 协议配置</h3>
<ul>
<li>仅启用TLS 1.2及以上版本</li>
<li>使用强加密套件</li>
<li>启用前向保密</li>
</ul>
<h3 id="证书管理">8.2 证书管理</h3>
<ul>
<li>及时更新证书</li>
<li>使用适当的密钥长度</li>
<li>正确配置证书链</li>
</ul>
<h3 id="安全监控">8.3 安全监控</h3>
<ul>
<li>记录TLS错误日志</li>
<li>监控证书有效期</li>
<li>定期安全评估</li>
</ul>
]]></content>
      <categories>
        <category>通信协议</category>
      </categories>
      <tags>
        <tag>网络安全</tag>
        <tag>加密</tag>
        <tag>HTTPS</tag>
      </tags>
  </entry>
  <entry>
    <title>Understanding the Linux Kernel Lecture 1 Note</title>
    <url>/posts/b1b647fa/</url>
    <content><![CDATA[<h2 id="引言">引言</h2>
<p>这本书是导师推荐给我很久了的，一直没啃完，希望这个寒假可以啃完这本八百页的书。(失败)</p>
<span id="more"></span>
<h2 id="内核基础概念">内核基础概念</h2>
<h3 id="内核的本质">内核的本质</h3>
<ul>
<li>内核是操作系统中最重要的基本程序集合</li>
<li>系统启动时被装入RAM</li>
<li>包含系统运行必需的核心过程(procedure)</li>
<li>决定了系统的基本特性和能力</li>
<li>"操作系统"常作为"内核"的同义词</li>
</ul>
<h3 id="操作系统的双重目标">操作系统的双重目标</h3>
<ol type="1">
<li><strong>硬件交互</strong>
<ul>
<li>管理硬件平台</li>
<li>为低层可编程部件提供服务</li>
</ul></li>
<li><strong>执行环境</strong>
<ul>
<li>为应用程序(用户程序)提供运行环境</li>
<li>管理程序执行</li>
</ul></li>
</ol>
<h3 id="硬件访问控制">硬件访问控制</h3>
<h4 id="不同系统的对比">不同系统的对比</h4>
<ul>
<li><strong>MS-DOS</strong>: 允许用户程序直接访问硬件</li>
<li><strong>Unix系统</strong>:
<ul>
<li>对用户程序隐藏底层硬件细节</li>
<li>通过系统调用请求访问硬件</li>
<li>由内核评估和代理硬件交互</li>
</ul></li>
</ul>
<h3 id="系统保护机制">系统保护机制</h3>
<h4 id="cpu执行模式">CPU执行模式</h4>
<ul>
<li><strong>用户态(User Mode)</strong>
<ul>
<li>非特权模式</li>
<li>用于运行普通用户程序</li>
</ul></li>
<li><strong>内核态(Kernel Mode)</strong>
<ul>
<li>特权模式</li>
<li>用于执行内核代码</li>
<li>可以直接访问硬件</li>
</ul></li>
</ul>
<h4 id="实现方式">实现方式</h4>
<ul>
<li>依赖硬件特性实现保护</li>
<li>禁止用户程序直接访问:
<ul>
<li>底层硬件</li>
<li>任意物理地址</li>
</ul></li>
</ul>
<h2 id="linux内核的基本特性">Linux内核的基本特性</h2>
<p>Linux包括了现代Unix操作系统的全部特点：</p>
<ul>
<li>虚拟存储：提供了虚拟内存管理,支持分页和交换</li>
<li>虚拟文件系统：统一的文件系统接口,支持多种文件系统</li>
<li>轻量级进程：高效的进程创建和调度机制</li>
<li>Unix信号量：进程间同步机制</li>
<li>SVR4进程间通信：支持多种IPC机制</li>
<li>支持对称多处理器(Symmetric Multiprocessor，SMP)系统等</li>
</ul>
<h2 id="linux内核的架构特点">Linux内核的架构特点</h2>
<h3 id="单块结构monolithic-kernel">单块结构(Monolithic Kernel)</h3>
<p>Linux采用单块内核结构,是一个庞大、复杂的自我完善(do-it-yourself)程序。它由几个逻辑上独立的成分构成,这一点上相当传统,大多数商用Unix变体也是单块结构。(值得注意的是Apple的Mac
OS X和GNU的Hurd操作系统采用了微内核方法)</p>
<h3 id="模块化支持">模块化支持</h3>
<p>Linux对模块的支持非常出色,内核可以动态地装载和卸载部分内核代码(典型的例子如设备驱动程序)。这些代码被称为"模块"(module),Linux支持在运行时动态加载或卸载模块。在主要的商用Unix变体中,只有SVR4.2和Solaris内核有类似特性。</p>
<h3 id="内核线程">内核线程</h3>
<p>内核线程是一个能被独立调度的执行环境(context),它与用户程序有关。线程之间的上下文切换比普通进程间的上下文切换花费要少得多,因为前者通常在同一个地址空间内执行。Linux以一种十分有限的方式使用内核线程来周期性地执行几个内核函数。</p>
<h3 id="多线程应用程序支持">多线程应用程序支持</h3>
<p>Linux通过轻量级进程(lightweight process,
LWP)实现多线程支持。Linux将轻量级进程当作基本的执行上下文,通过非标准的clone()系统调用来处理它们。这些进程可以共享同样的地址空间、共同的物理内存页面、共同的打开文件等。
&gt; <code>clone()</code> 是 Linux
内核中的一个系统调用，它用于创建一个新进程或线程，并允许更灵活的控制新创建的进程或线程与调用进程之间共享的资源。与传统的
<code>fork()</code> 系统调用不同，<code>clone()</code>
<strong>允许父进程和子进程在多个层面上共享不同的资源</strong>。具体来说，<code>clone()</code>
允许选择性地共享虚拟内存、文件描述符、信号处理器等。</p>
<h3 id="抢占式内核">抢占式内核</h3>
<p>从Linux
2.6开始,内核可以随意交错执行处于特权模式的执行流。这使得Linux成为完全的抢占式内核,提供了更好的实时性能。</p>
<h3 id="多处理器支持">多处理器支持</h3>
<p>Linux
2.6支持不同存储模式的对称多处理(SMP),包括NUMA架构。系统不仅可以使用多处理器,而且每个处理器可以毫无区别地处理任何一个任务。</p>
<h2 id="文件系统支持">文件系统支持</h2>
<p>Linux支持多种文件系统格式: - Ext2/Ext3: 标准的Linux文件系统 -
ReiserFS: 适合处理大量小文件的高性能文件系统 - 其他日志文件系统:
如IBM的JFS和SGI的XFS - 支持多种商业文件系统,便于与其他系统交互</p>
<h2 id="linux的优势">Linux的优势</h2>
<ol type="1">
<li>免费开源</li>
</ol>
<ul>
<li>除硬件之外,无需任何花费就能安装完整的Linux系统</li>
<li>基于GPL协议,可以自由地阅读、修改内核源代码</li>
</ul>
<ol start="2" type="1">
<li>低硬件要求</li>
</ol>
<ul>
<li>可以在低端、便宜的硬件平台上运行</li>
<li>仅需4MB内存的旧Intel 80386系统即可构建网络服务器</li>
</ul>
<ol start="3" type="1">
<li>高性能</li>
</ol>
<ul>
<li>充分挖掘了硬件部分的特点</li>
<li>注重效率,许多商用系统的设计选择因性能低下而被舍弃</li>
</ul>
<ol start="4" type="1">
<li>稳定可靠</li>
</ol>
<ul>
<li>系统非常稳定,有非常低的故障率</li>
<li>系统维护时间少</li>
</ul>
<ol start="5" type="1">
<li>体积小巧</li>
</ol>
<ul>
<li>内核映像和基本系统程序可以放在1.4MB软盘上</li>
<li>远小于任何商用Unix变体的体积</li>
</ul>
<ol start="6" type="1">
<li>兼容性强</li>
</ol>
<ul>
<li>可以让你直接安装多种文件系统</li>
<li>支持运行多种操作系统的程序</li>
<li>支持多种网络协议和接口</li>
</ul>
<ol start="7" type="1">
<li>技术支持</li>
</ol>
<ul>
<li>活跃的开发者社区</li>
<li>问题反馈迅速</li>
<li>新硬件支持及时</li>
</ul>
<h2 id="硬件平台支持">硬件平台支持</h2>
<p>Linux通过在arch和include目录下包含23个子目录来保持源代码与硬件相关的源代码之间的清晰界限，以支持不同的硬件平台。主要支持的处理器架构包括：</p>
<h3 id="个人计算机和工作站处理器">个人计算机和工作站处理器</h3>
<ul>
<li><strong>x86_64</strong>:
支持AMD的64位处理器(如Athlon和Opteron)和Intel的ia32e/EM64T64位处理器</li>
<li><strong>i386</strong>: 基于80x86微处理器的IBM兼容个人计算机</li>
<li><strong>ia64</strong>: 基于64位Itanium微处理器的工作站</li>
<li><strong>ppc/ppc64</strong>: 基于Motorola-IBM PowerPC
32位和64位微处理器的工作站</li>
</ul>
<h3 id="服务器和大型机处理器">服务器和大型机处理器</h3>
<ul>
<li><strong>alpha</strong>:
HP的Alpha工作站(最早属于Digital公司，后属于Compaq公司，现已停产)</li>
<li><strong>s390</strong>: IBM ESA/390及zSeries大型机</li>
<li><strong>sparc/sparc64</strong>: 基于Sun公司SPARC和64位Ultra
SPARC微处理器的工作站</li>
</ul>
<h3 id="嵌入式和移动设备处理器">嵌入式和移动设备处理器</h3>
<ul>
<li><strong>arm/arm26</strong>:
基于ARM处理器的计算机(如PDA)和嵌入式设备</li>
<li><strong>m32r</strong>: 基于Renesas M32R系列微处理器的计算机</li>
<li><strong>m68k/m68knommu</strong>: 基于Motorola
MC680x0微处理器的个人计算机</li>
<li><strong>cris</strong>:
Axis在其嵌入式服务器中使用的"代码精简指令集(CRIS)"CPU</li>
<li><strong>frv</strong>: 基于Fujitsu FR-V系列微处理器的嵌入式系统</li>
</ul>
<h3 id="risc处理器">RISC处理器</h3>
<ul>
<li><strong>h8300</strong>: Hitachi
h8/300和h8S的8位和16位RISC微处理器</li>
<li><strong>mips</strong>: 基于MIPS微处理器的工作站，如Silicon
Graphics公司销售的工作站</li>
<li><strong>parisc</strong>: 基于HP公司HP 9000
PA-RISC微处理器的工作站</li>
<li><strong>v850</strong>: 集成了基于Harvard体系结构的32位RISC核心的NEC
V850微控制器</li>
</ul>
<h3 id="特殊平台">特殊平台</h3>
<ul>
<li><strong>um</strong>: 用户态的Linux -
一个允许开发者在用户态下运行内核的虚拟平台</li>
<li><strong>sh/sh64</strong>:
基于Hitachi和STMicroelectronics联合开发的SuperH微处理器的嵌入式系统</li>
</ul>
<p>这种广泛的硬件支持使Linux能够运行在从嵌入式设备到超级计算机的各种平台上，展现了其卓越的可移植性和适应性。通过模块化的设计和清晰的代码组织，Linux能够在保持核心功能稳定的同时，有效地支持不同的硬件架构。</p>
<h2 id="linux版本管理">Linux版本管理</h2>
<h3 id="版本号体系">版本号体系</h3>
<p>一直到2.5版本的内核，Linux都通过简单的编号来区别内核的稳定版和开发版。每个版本号由三个数字描述，由圆点分隔：</p>
<ol type="1">
<li>第一位版本号：从1996年开始基本没有变化</li>
<li>第二位版本号：表示内核的类型
<ul>
<li>偶数表示稳定版本的内核</li>
<li>奇数表示正在开发中的内核</li>
</ul></li>
<li>第三位数字：表示发布号</li>
</ol>
<h3 id="版本发布机制">版本发布机制</h3>
<ul>
<li>稳定版本的内核由Linux的发布者和内核黑客彻底检查过</li>
<li>新的稳定版本主要用来修正用户报告的错误或增加新的驱动程序</li>
<li>开发版本之间可能存在非常明显的差异</li>
<li>内核开发者可以自由地采用不同方案进行实验，但这些实验可能导致内核有很大变化</li>
<li>用开发版运行应用程序的用户，当把内核升级到新版时，可能会遇到一些不太令人愉快的意外</li>
</ul>
<h3 id="版本的变革">2.6版本的变革</h3>
<p>在Linux内核2.6版的开发过程中，内核版本的编号方式发生了很大的变化：</p>
<ul>
<li>第二个数字不再用于表示内核是稳定版还是开发版</li>
<li>内核开发者都在当前的2.6版本中对内核进行大幅改进</li>
<li>只有在内核开发者必须对内核的重大修改进行测试时，才会采用一个新的内核分支2.7</li>
<li>这种2.7的分支要么产生一个新的内核版本，要么干脆舍弃所修改的部分而回退到2.6版</li>
</ul>
<h3 id="版本差异">版本差异</h3>
<p>Linux这种新的开发模式意味着两种内核具有相同的版本号，但却有不同的发布号，如2.6.10和2.6.11内核就可能在核心部件和基本算法上有很大的差别。这种方式使得Linux可以在保持版本号稳定的同时，持续进行重要的改进和更新。</p>
<h2 id="多用户系统">多用户系统</h2>
<h3 id="基本特性">基本特性</h3>
<p>多用户系统允许多个用户同时使用计算机系统，具有两个核心特性：</p>
<ol type="1">
<li>并发性(Concurrency)</li>
</ol>
<ul>
<li>多个应用程序同时活动</li>
<li>共享系统资源(CPU、内存、硬盘等)</li>
<li>通过调度机制优化响应时间</li>
</ul>
<ol start="2" type="1">
<li>独立性(Independence)</li>
</ol>
<ul>
<li>程序互不干扰</li>
<li>独立执行各自任务</li>
<li>资源隔离保护</li>
</ul>
<h3 id="安全保护机制">安全保护机制</h3>
<p>为保证多用户系统的安全运行，需要实现以下机制：</p>
<ol type="1">
<li>身份安全</li>
</ol>
<ul>
<li>用户认证系统</li>
<li>登录验证机制</li>
<li>密码保护</li>
</ul>
<ol start="2" type="1">
<li>程序保护</li>
</ol>
<ul>
<li>防止程序相互干扰</li>
<li>阻止恶意程序访问</li>
<li>基于CPU特权模式的硬件保护</li>
</ul>
<ol start="3" type="1">
<li>资源管理</li>
</ol>
<ul>
<li>限制单个用户资源使用</li>
<li>合理分配系统资源</li>
<li>防止资源滥用</li>
</ul>
<h3 id="用户权限体系">用户权限体系</h3>
<p>多用户系统采用分层的权限管理方式：</p>
<ol type="1">
<li>用户标识(UID)</li>
</ol>
<ul>
<li>唯一的用户标识符</li>
<li>用于身份识别和权限判断</li>
<li>控制用户私有空间访问</li>
</ul>
<ol start="2" type="1">
<li>用户组(GID)</li>
</ol>
<ul>
<li>用户组标识符</li>
<li>实现资源共享机制</li>
<li>灵活的权限分配</li>
</ul>
<ol start="3" type="1">
<li>文件权限</li>
</ol>
<ul>
<li>所有者权限</li>
<li>组成员权限</li>
<li>其他用户权限</li>
</ul>
<h3 id="超级用户root">超级用户(root)</h3>
<p>任何类Unix操作系统都有一个特殊的用户，叫做root，即超级用户(superuser)。root用户具有以下特点：</p>
<ol type="1">
<li>特殊权限</li>
</ol>
<ul>
<li>可以访问系统中的每一个文件</li>
<li>能够访问每个正在执行的用户程序</li>
<li>不受通常的保护机制限制</li>
</ul>
<ol start="2" type="1">
<li>管理职责</li>
</ol>
<ul>
<li>处理用户账号</li>
<li>系统备份</li>
<li>程序升级</li>
<li>系统维护任务</li>
</ul>
<h3 id="进程process">进程(Process)</h3>
<p>进程是操作系统使用的一种基本抽象，可以定义为： -
"程序执行时的一个实例" - "一个运行程序的执行上下文"</p>
<h4 id="进程特征">进程特征</h4>
<ol type="1">
<li>地址空间</li>
</ol>
<ul>
<li>每个进程在地址空间(address space)中执行</li>
<li>拥有一个独立的指令序列</li>
<li>地址空间是进程专用的内存地址集合</li>
</ul>
<ol start="2" type="1">
<li>多道程序设计</li>
</ol>
<ul>
<li>允许多个进程并发活动</li>
<li>竞争系统资源（主要是CPU）</li>
<li>支持多道程序系统(multiprogramming)或多处理系统(multiprocessing)</li>
</ul>
<h4 id="进程调度">进程调度</h4>
<ol type="1">
<li>调度类型</li>
</ol>
<ul>
<li>非抢占式(nonpreemptable)：进程自愿放弃CPU时才被调度</li>
<li>抢占式(preemptable)：操作系统记录和管理CPU时间，定期激活调度程序</li>
</ul>
<ol start="2" type="1">
<li>Unix进程特点</li>
</ol>
<ul>
<li>采用抢占式进程调度</li>
<li>即使没有用户登录，系统仍有进程在监视外围设备</li>
<li>用户登录时会创建shell进程</li>
<li>图形界面中每个窗口通常由独立进程执行</li>
</ul>
<ol start="3" type="1">
<li>进程/内核模式</li>
</ol>
<ul>
<li>每个进程都认为自己是系统中唯一的进程</li>
<li>可以独占操作系统提供的服务</li>
<li>通过系统调用请求内核服务</li>
</ul>
<h2 id="内核体系结构">内核体系结构</h2>
<h3 id="内核架构类型">内核架构类型</h3>
<h4 id="单块内核">单块内核</h4>
<ul>
<li>传统Unix采用的方式</li>
<li>所有内核功能集成到整个内核程序中</li>
<li>在内核态下运行所有系统功能</li>
<li>结构紧凑但复杂度高</li>
</ul>
<h4 id="微内核microkernel">微内核(Microkernel)</h4>
<ol type="1">
<li>基本特征</li>
</ol>
<ul>
<li>只需内核有一个很小的函数集</li>
<li>通常包括几个同步原语</li>
<li>简单的调度程序</li>
<li>进程间通信机制</li>
</ul>
<ol start="2" type="1">
<li>优缺点</li>
</ol>
<ul>
<li>优点：
<ul>
<li>结构清晰模块化</li>
<li>系统服务独立运行</li>
<li>易于移植和维护</li>
</ul></li>
<li>缺点：
<ul>
<li>性能较低</li>
<li>系统调用开销大</li>
<li>消息传递需要额外成本</li>
</ul></li>
</ul>
<h3 id="linux的模块化方案">Linux的模块化方案</h3>
<h4 id="模块module概念">模块(Module)概念</h4>
<ul>
<li>是一个目标文件</li>
<li>可在运行时链接到内核或从内核解除链接</li>
<li>由一组函数组成</li>
<li>实现特定功能（如文件系统、驱动程序等）</li>
<li>在内核态下执行</li>
</ul>
<h4 id="模块化优势">模块化优势</h4>
<ol type="1">
<li>灵活性</li>
</ol>
<ul>
<li>运行时动态加载/卸载</li>
<li>按需加载系统功能</li>
<li>便于开发新模块</li>
</ul>
<ol start="2" type="1">
<li>平台无关性</li>
</ol>
<ul>
<li>独立于具体硬件平台</li>
<li>适应不同系统架构</li>
<li>如SCSI驱动程序可在不同硬件上工作</li>
</ul>
<ol start="3" type="1">
<li>资源效率</li>
</ol>
<ul>
<li>未使用模块可被卸载</li>
<li>节省系统内存</li>
<li>优化系统性能</li>
</ul>
<p>这种模块化设计使Linux在保持高性能的同时，也获得了类似微内核的灵活性，是一种优秀的折中方案。</p>
<h2 id="unix-文件系统概述">Unix 文件系统概述</h2>
<h3 id="文件的定义">文件的定义</h3>
<ul>
<li>Unix文件是以字节序列组成的信息载体(container)。</li>
<li>内核不解释文件的内容。</li>
<li>程序通过系统调用访问文件。</li>
</ul>
<h3 id="目录结构">目录结构</h3>
<ul>
<li>文件被组织在一个树结构的命名空间中。</li>
<li>根目录("/")是树的起点。</li>
<li>目录节点表示目录名，包含其下文件及目录的所有节点。</li>
</ul>
<h4 id="示例目录结构">示例目录结构</h4>
<ul>
<li>根目录下有子目录如dev, home, bin, usr等。</li>
<li>叶节点表示具体文件，如ls, cp等。</li>
</ul>
<h3 id="文件命名与路径">文件命名与路径</h3>
<h4 id="文件命名规则">文件命名规则</h4>
<ul>
<li>文件名由ASCII字符序列组成(除"/"和"\0"外)</li>
<li>大多数文件系统限制文件名长度不超过255个字符</li>
<li>同一目录下文件名不能重复</li>
<li>不同目录下可以使用相同的文件名</li>
</ul>
<h4 id="路径名pathname">路径名(pathname)</h4>
<ol type="1">
<li>绝对路径</li>
</ol>
<ul>
<li>以根目录"/"开头</li>
<li>完整指定文件位置的路径</li>
<li>从根目录开始的完整路径名</li>
</ul>
<ol start="2" type="1">
<li>相对路径</li>
</ol>
<ul>
<li>从当前工作目录开始</li>
<li>使用目录名或文件名作为起点</li>
<li>相对于进程的当前目录</li>
</ul>
<h4 id="当前工作目录">当前工作目录</h4>
<ul>
<li>每个进程都有一个当前工作目录</li>
<li>属于进程执行上下文(execution context)的一部分</li>
<li>用于标识进程当前所在的目录位置</li>
</ul>
<h4 id="特殊目录符号">特殊目录符号</h4>
<ul>
<li>"." 表示当前工作目录</li>
<li>".." 表示父目录</li>
<li>当前工作目录为根目录时,"."和".."相同</li>
</ul>
<h3 id="链接类型">链接类型</h3>
<h4 id="硬链接hard-link">硬链接(Hard Link)</h4>
<ul>
<li>包含在目录中的文件名就是一个文件的硬链接</li>
<li>同一文件可以在不同目录中有多个硬链接</li>
<li>使用ln命令创建: <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">ln</span> P1 P2</span><br></pre></td></tr></table></figure></li>
<li>为路径名P1标识的文件创建一个路径名为P2的硬链接</li>
</ul>
<h4 id="硬链接限制">硬链接限制</h4>
<ol type="1">
<li>目录限制</li>
</ol>
<ul>
<li>不允许普通用户给目录创建硬链接</li>
<li>防止目录树变为环形图</li>
<li>避免无法通过名字定位文件</li>
</ul>
<ol start="2" type="1">
<li>文件系统限制</li>
</ol>
<ul>
<li>只能在同一文件系统内的文件之间创建链接</li>
<li>现代Unix系统可能包含多个文件系统</li>
<li>这些文件系统位于不同的磁盘和/或分区</li>
</ul>
<h4 id="软链接soft-link">软链接(Soft Link)</h4>
<ul>
<li>也称为符号链接(Symbolic Link)</li>
<li>是一个特殊的短文件</li>
<li>包含另一个文件的任意路径名</li>
<li>可以指向任意文件系统的文件或目录</li>
<li>甚至可以指向不存在的文件</li>
</ul>
<h4 id="软链接创建">软链接创建</h4>
<ul>
<li>使用ln命令的-s选项创建: <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">ln</span> -s P1 P2</span><br></pre></td></tr></table></figure></li>
<li>P2指向路径名P1</li>
<li>创建后文件系统会在P2的目录中建立一个名为P2的符号链接</li>
<li>任何对P2的引用都会被自动替换成对P1的引用</li>
</ul>
<h3 id="文件类型">文件类型</h3>
<p>Unix文件可以是以下类型之一：</p>
<ol type="1">
<li>普通文件(regular file)</li>
<li>目录</li>
<li>符号链接</li>
<li>面向块的设备文件(block-oriented device file)</li>
<li>面向字符的设备文件(character-oriented device file)</li>
<li>管道(pipe)和命名管道(named pipe)(也叫FIFO)</li>
<li>套接字(socket)</li>
</ol>
<p>前三种文件类型是所有Unix文件系统的基本类型，其实现将在第十八章详细讨论。</p>
<p>设备文件与I/O设备以及集成到内核中的设备驱动程序相关。例如，当程序访问设备文件时，它直接访问与那个文件相关的I/O设备(参见第十三章)。</p>
<p>管道和套接字是用于进程间通信的特殊文件(参见本章后面的"同步和临界区"一节以及第十九章)。</p>
<h3 id="文件描述符与索引节点">文件描述符与索引节点</h3>
<ul>
<li>Unix对文件的内容和描述文件的信息给出了清晰的区分</li>
<li>除了设备文件和特殊文件系统文件外，每个文件都由字符序列组成</li>
<li>文件内容不包含任何控制信息，如文件长度或文件结束(end-of-file,EOF)符</li>
</ul>
<h4 id="索引节点inode">索引节点(inode)</h4>
<ul>
<li>文件系统处理文件需要的所有信息都包含在一个名为索引节点的数据结构中</li>
<li>每个文件都有自己的索引节点</li>
<li>文件系统用索引节点来标识文件</li>
</ul>
<p>尽管文件系统及内核函数对索引节点的处理可能随Unix系统的不同有很大的差异，但它们必须至少提供在POSIX标准中指定的如下属性</p>
<h4 id="索引节点属性">索引节点属性</h4>
<ul>
<li>文件类型(参见前一节)</li>
<li>与文件相关的硬链接个数</li>
<li>以字节为单位的文件长度</li>
<li>设备标识符(即包含文件的设备的标识符)</li>
<li>在文件系统中标识文件的索引节点号</li>
<li>文件拥有者的UID</li>
<li>文件的用户组ID</li>
<li>几个时间戳，表示索引节点状态改变的时间、最后访问时间及最后修改时间</li>
<li>访问权限和文件模式(参见下一节)</li>
</ul>
<h3 id="访问权限和文件模式">访问权限和文件模式</h3>
<p>文件的潜在用户分为三种类型：</p>
<ul>
<li>作为文件所有者的用户</li>
<li>同组用户，不包括所有者</li>
<li>所有剩下的用户(其他)</li>
</ul>
<p>有三种类型的访问权限——读、写及执行每组用户都有这三种权限。因此，文件访问权限的组合就用九种不同的二进制标记位。还有三种附加的标记位，即suid(Set
User ID)、sgid(Set Group
ID)，及sticky用来定义文件的模式。当这些标记位应用到可执行文件时有如下含义：</p>
<h4 id="suid">suid</h4>
<ul>
<li>进程执行一个文件时通常保持进程拥有者的UID</li>
<li>如果设置了可执行文件suid的标志位，进程就获得了该文件拥有者的UID</li>
</ul>
<h4 id="sgid">sgid</h4>
<ul>
<li>进程执行一个文件时保持进程组的用户组ID</li>
<li>如果设置了可执行文件sgid的标志位，进程就获得了该文件用户组的ID</li>
</ul>
<h4 id="sticky">sticky</h4>
<ul>
<li>设置了sticky标志位的可执行文件相当于向内核发出一个请求</li>
<li>当程序执行结束后，依然将其保留在内存中(注8)</li>
</ul>
<blockquote>
<p>注8：这个标志已经过时，现在使用基于代码页共享的其他方法(参见第九章)。</p>
</blockquote>
<p>当文件由一个进程创建时，文件拥有者的ID就是该进程的UID，而其用户组ID可以是进程创建者的ID，也可以是父目录的ID，这取决于父目录sgid标志位的值。</p>
<h3 id="文件操作系统调用">文件操作系统调用</h3>
<h4 id="基本概念">基本概念</h4>
<ul>
<li>用户访问文件实际是访问存储在硬件块设备上的数据</li>
<li>文件系统是硬件块设备的抽象层</li>
<li>所有文件操作必须在内核态下进行</li>
<li>Unix系统通过系统调用实现文件操作</li>
</ul>
<h4 id="系统性能考虑">系统性能考虑</h4>
<ul>
<li>Unix内核高度重视硬件块设备的处理效率</li>
<li>目标是获得良好的系统整体性能</li>
<li>通过系统调用机制确保安全和效率</li>
</ul>
<h4 id="打开文件操作">打开文件操作</h4>
<p>进程必须先打开文件才能访问,使用<strong>open系统调用</strong>：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">fd=open(path, flag, mode)</span><br></pre></td></tr></table></figure>
<h5 id="参数说明">参数说明</h5>
<ol type="1">
<li>path参数</li>
</ol>
<ul>
<li>指定要打开文件的路径</li>
<li>可以是相对路径或绝对路径</li>
</ul>
<ol start="2" type="1">
<li>flag参数</li>
</ol>
<ul>
<li>指定文件的打开方式：
<ul>
<li>读取(read)</li>
<li>写入(write)</li>
<li>读写(read/write)</li>
<li>追加(append)</li>
</ul></li>
<li>可以指定是否创建不存在的文件</li>
</ul>
<ol start="3" type="1">
<li>mode参数</li>
</ol>
<ul>
<li>设置新创建文件的访问权限</li>
</ul>
<h5 id="返回值">返回值</h5>
<ul>
<li>返回文件描述符(file descriptor)</li>
<li>创建一个打开文件对象,包含：
<ul>
<li>文件操作的数据结构(打开方式标志、文件位置offset等)</li>
<li>可调用的内核函数集合(由flag参数决定)</li>
</ul></li>
</ul>
<h4 id="posix规范的一般特性">POSIX规范的一般特性</h4>
<ul>
<li>文件描述符表示进程与打开文件之间的交互</li>
<li>同一打开文件对象可以由一个进程的多个文件描述符引用</li>
<li>多个进程可以同时打开同一文件：
<ul>
<li>每个进程获得独立的打开文件对象和文件描述符</li>
<li>Unix文件系统默认不提供I/O操作的同步机制</li>
<li>可使用flock()系统调用实现对整个或部分文件的I/O操作同步(参见第十二章)</li>
</ul></li>
</ul>
<blockquote>
<p>注：创建新文件可以使用create()系统调用，功能与open()类似，都由内核处理。</p>
</blockquote>
<h3 id="访问打开的文件">访问打开的文件</h3>
<h4 id="访问方式">访问方式</h4>
<ul>
<li>普通Unix文件支持顺序访问和随机访问</li>
<li>设备文件和管道文件通常只能顺序访问</li>
<li>内核把文件指针存放在打开文件对象中</li>
<li>文件指针表示下一次读写操作的位置</li>
</ul>
<h4 id="文件操作">文件操作</h4>
<ul>
<li>顺序访问：使用read()和write()系统调用，基于文件指针的当前位置读写</li>
<li>随机访问：需要使用lseek()系统调用修改文件指针位置</li>
</ul>
<h4 id="lseek系统调用">lseek系统调用</h4>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">newoffset=lseek(fd, offset, whence);</span><br></pre></td></tr></table></figure>
<p><strong>参数说明：</strong> - <strong>fd</strong>:
打开文件的文件描述符 - <strong>offset</strong>:
有符号整数值，用于计算文件指针的新位置 - <strong>whence</strong>:
指定文件指针新位置的计算方式 - offset加0：从文件头移动 -
offset加当前位置：从当前位置移动 - offset加文件末尾：从文件末尾移动</p>
<h4 id="read系统调用">read系统调用</h4>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">count=read(fd, buf, count);</span><br></pre></td></tr></table></figure>
<p><strong>参数说明：</strong> - <strong>fd</strong>:
打开文件的文件描述符 - <strong>buf</strong>:
指向缓冲区的指针，用于存储读取的数据 - <strong>count</strong>:
要读取的字节数</p>
<p><strong>操作说明：</strong> -
内核会尝试从文件描述符fd指向的文件中读取count个字节 -
读取起始位置为文件的offset字段当前值 -
可能因文件结束、空管道等原因无法读取全部字节 -
返回值nread表示实际读取的字节数 - 读取完成后会更新文件指针 -
write()系统调用的参数与read()类似</p>
<h3 id="关闭文件">关闭文件</h3>
<p>当进程不再需要访问文件内容时，使用系统调用：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">result=close(fd);</span><br></pre></td></tr></table></figure>
<ul>
<li>释放文件描述符fd相关的打开文件对象</li>
<li>当进程终止时，内核会自动关闭其所有打开的文件</li>
</ul>
<h3 id="重命名和删除文件">重命名和删除文件</h3>
<p>这些操作不需要打开文件，它们作用于目录项而非文件内容。</p>
<h4 id="重命名文件">重命名文件</h4>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">result=rename(oldpath, newpath);</span><br></pre></td></tr></table></figure>
<h4 id="删除文件">删除文件</h4>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">result=unlink(path);</span><br></pre></td></tr></table></figure>
<ul>
<li>减少文件链接数</li>
<li>当链接数为0时，文件才被真正删除</li>
</ul>
<blockquote>
<p>25页</p>
</blockquote>
]]></content>
      <categories>
        <category>linux</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>操作系统</tag>
        <tag>内核</tag>
      </tags>
  </entry>
  <entry>
    <title>influxdb Connection Problem</title>
    <url>/posts/dbeb2432/</url>
    <content><![CDATA[<h2 id="influxdb连接问题">influxdb连接问题</h2>
<p>最近之前实习的嵌入式公司修改了他们产品原有的数据库结构，从mysql转移到了influxdb，在接触influxdb的过程中也遇到了一些问题，现在汇总如下：
<span id="more"></span></p>
<h3 id="token401问题">token401问题</h3>
<h4 id="登录401">登录401</h4>
<p>一开始拿到公司的token测试连接</p>
<figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="section">[InfluxDB]</span></span><br><span class="line"><span class="attr">Token</span> = ***</span><br><span class="line"><span class="attr">ServerUrl</span> = http://localhost:<span class="number">8086</span></span><br><span class="line"><span class="attr">Bucket</span> = ***</span><br></pre></td></tr></table></figure>
<figure>
<img src="/assets/78c665f7d532f73e4cf89e901284eb7f.png"
alt="78c665f7d532f73e4cf89e901284eb7f" />
<figcaption
aria-hidden="true">78c665f7d532f73e4cf89e901284eb7f</figcaption>
</figure>
<p>发现可以正常连接，这意味着token本身没有问题，但是想要执行查询时，会报错401</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">2024-09-13 18:14:47,261 - ERROR - Error querying InfluxDB: (401)</span><br><span class="line">Reason: Unauthorized</span><br><span class="line">HTTP response headers: HTTPHeaderDict(&#123;&#x27;Content-Type&#x27;: &#x27;application/json; charset=utf-8&#x27;, &#x27;X-Influxdb-Build&#x27;: &#x27;OSS&#x27;, &#x27;X-Influxdb-Version&#x27;: &#x27;v2.7.8&#x27;, &#x27;X-Platform-Error-Code&#x27;: &#x27;unauthorized&#x27;, &#x27;Date&#x27;: &#x27;Fri, 13 Sep 2024 09:44:47 GMT&#x27;, &#x27;Content-Length&#x27;: &#x27;55&#x27;&#125;)</span><br><span class="line">HTTP response body: b&#x27;&#123;&quot;code&quot;:&quot;unauthorized&quot;,&quot;message&quot;:&quot;unauthorized access&quot;&#125;&#x27;</span><br></pre></td></tr></table></figure>
<p>上网搜索发现许多人遇到过这个问题，最终在一个评论区找到思路</p>
<figure>
<img src="/assets/image-20240919184721228.png"
alt="image-20240919184721228" />
<figcaption aria-hidden="true">image-20240919184721228</figcaption>
</figure>
<p>于是把ini文件改为</p>
<figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="section">[InfluxDB]</span></span><br><span class="line"><span class="attr">Username</span> = ***</span><br><span class="line"><span class="attr">Password</span> = ***</span><br><span class="line"><span class="attr">Org</span> = ***</span><br><span class="line"><span class="attr">ServerUrl</span> = http://localhost:<span class="number">8086</span></span><br><span class="line"><span class="attr">Bucket</span> = ***</span><br></pre></td></tr></table></figure>
<p>发现可以正常读取了</p>
<h4 id="过期401">过期401</h4>
<p>后面在公司测试时又发现了401问题，还是同样的报错，重启程序发现又可以查询了，猜测可能是连接有时间限制</p>
<p>于是修改建立连接的方式，改为每次要查询前建立连接，问题得到解决</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 动态建立 InfluxDB 连接的函数</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_influxdb_client</span>():</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        client = InfluxDBClient(url=influxdb_url, username=influxdb_username, password=influxdb_password, org=influxdb_org)</span><br><span class="line">        logger.info(<span class="string">&quot;Successfully connected to InfluxDB.&quot;</span>)</span><br><span class="line">        <span class="keyword">return</span> client</span><br><span class="line">    <span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">        logger.error(<span class="string">f&quot;Failed to connect to InfluxDB: <span class="subst">&#123;<span class="built_in">str</span>(e)&#125;</span>&quot;</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">None</span></span><br></pre></td></tr></table></figure>
<h3 id="数据读取问题">数据读取问题</h3>
<p>原本的数据库为mysql，查询语句如下</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">fetch_device_events</span>(<span class="params">cursor, f_device_id, start_date, end_date</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    获取设备事件数据</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    query = <span class="string">f&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    SELECT</span></span><br><span class="line"><span class="string">        f_dtc_index, f_notify, f_create_time, f_report_time, f_channel, f_desc</span></span><br><span class="line"><span class="string">    FROM</span></span><br><span class="line"><span class="string">        t_mqtt_event </span></span><br><span class="line"><span class="string">    WHERE</span></span><br><span class="line"><span class="string">        f_device_id = &#x27;<span class="subst">&#123;f_device_id&#125;</span>&#x27; AND f_report_time BETWEEN &#x27;<span class="subst">&#123;start_date&#125;</span>&#x27; AND &#x27;<span class="subst">&#123;end_date&#125;</span>&#x27;</span></span><br><span class="line"><span class="string">    ORDER BY f_id DESC</span></span><br><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br><span class="line"></span><br><span class="line">    cursor.execute(query)</span><br><span class="line">    <span class="keyword">return</span> cursor.fetchall()</span><br></pre></td></tr></table></figure>
<p>现在要修改为influxdb的查询语句，influxdb有自己的查询语法，这里的难点在于理解influxdb的数据是如何存取的。</p>
<p>我个人的理解为，这里的field是一列，每个field是一个表，这里的查询返回的是所有field的行，但是每行只有一个field以及对应的值。</p>
<p>这样会产生一个问题，就是我们要合并数据。原有的mysql查询语句是每行有多列的内容，这里我们要根据时间戳去合并所有field来达到同样的效果</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">fetch_device_events</span>(<span class="params">influxdb_client, f_device_id, start_date, end_date</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    从 InfluxDB 获取设备事件数据并以元组形式输出</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    <span class="comment"># 定义中国时区</span></span><br><span class="line">    cst_tz = pytz.timezone(<span class="string">&#x27;Asia/Shanghai&#x27;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 确保传入的 start_date 和 end_date 是 datetime 对象</span></span><br><span class="line">    <span class="keyword">if</span> <span class="built_in">isinstance</span>(start_date, <span class="built_in">str</span>):</span><br><span class="line">        start_date = datetime.fromisoformat(start_date)</span><br><span class="line">    <span class="keyword">if</span> <span class="built_in">isinstance</span>(end_date, <span class="built_in">str</span>):</span><br><span class="line">        end_date = datetime.fromisoformat(end_date)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 将时间从中国时区转换为 UTC</span></span><br><span class="line">    start_date = cst_tz.localize(start_date).astimezone(pytz.utc)</span><br><span class="line">    end_date = cst_tz.localize(end_date).astimezone(pytz.utc)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 将 datetime 转换为符合 RFC 3339 格式的字符串 (UTC 时间)</span></span><br><span class="line">    start_date_rfc3339 = start_date.strftime(<span class="string">&quot;%Y-%m-%dT%H:%M:%SZ&quot;</span>)</span><br><span class="line">    end_date_rfc3339 = end_date.strftime(<span class="string">&quot;%Y-%m-%dT%H:%M:%SZ&quot;</span>)</span><br><span class="line"></span><br><span class="line">    query = <span class="string">f&#x27;&#x27;&#x27;</span></span><br><span class="line"><span class="string">    from(bucket: &quot;***&quot;)</span></span><br><span class="line"><span class="string">      |&gt; range(start: <span class="subst">&#123;start_date_rfc3339&#125;</span>, stop: <span class="subst">&#123;end_date_rfc3339&#125;</span>)</span></span><br><span class="line"><span class="string">      |&gt; filter(fn: (r) =&gt; r[&quot;guid&quot;] == &quot;<span class="subst">&#123;f_device_id&#125;</span>&quot;)</span></span><br><span class="line"><span class="string">      |&gt; filter(fn: (r) =&gt; r._measurement == &quot;device_history&quot;)</span></span><br><span class="line"><span class="string">      |&gt; filter(fn: (r) =&gt; r[&quot;_field&quot;] == &quot;fqIndex&quot; or </span></span><br><span class="line"><span class="string">                           r[&quot;_field&quot;] == &quot;code&quot; or </span></span><br><span class="line"><span class="string">                           r[&quot;_field&quot;] == &quot;reportTime&quot; or </span></span><br><span class="line"><span class="string">                           r[&quot;_field&quot;] == &quot;channel&quot; or </span></span><br><span class="line"><span class="string">                           r[&quot;_field&quot;] == &quot;desc&quot;)</span></span><br><span class="line"><span class="string">      |&gt; sort(columns: [&quot;_time&quot;], desc: true)</span></span><br><span class="line"><span class="string">    &#x27;&#x27;&#x27;</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        result = influxdb_client.query_api().query(query, org=<span class="string">&quot;shike&quot;</span>)</span><br><span class="line">        events = &#123;&#125;</span><br><span class="line">        <span class="keyword">for</span> table <span class="keyword">in</span> result:</span><br><span class="line">            <span class="keyword">for</span> record <span class="keyword">in</span> table.records:</span><br><span class="line">                <span class="comment"># 获取当前记录的时间戳</span></span><br><span class="line">                timestamp = record.get_time()</span><br><span class="line"></span><br><span class="line">                <span class="comment"># 如果当前时间戳还没有对应的事件，创建一个新的事件</span></span><br><span class="line">                <span class="keyword">if</span> timestamp <span class="keyword">not</span> <span class="keyword">in</span> events:</span><br><span class="line">                    events[timestamp] = &#123;</span><br><span class="line">                        <span class="string">&quot;fqIndex&quot;</span>: <span class="literal">None</span>,</span><br><span class="line">                        <span class="string">&quot;code&quot;</span>: <span class="literal">None</span>,</span><br><span class="line">                        <span class="string">&quot;reportTime&quot;</span>: <span class="literal">None</span>,</span><br><span class="line">                        <span class="string">&quot;channel&quot;</span>: <span class="literal">None</span>,</span><br><span class="line">                        <span class="string">&quot;desc&quot;</span>: <span class="literal">None</span>,</span><br><span class="line">                        <span class="string">&quot;f_create_time&quot;</span>: timestamp  <span class="comment"># 入库时间</span></span><br><span class="line">                    &#125;</span><br><span class="line"></span><br><span class="line">                <span class="comment"># 根据 _field 来提取 _value 的值</span></span><br><span class="line">                field_name = record.get_field()</span><br><span class="line">                field_value = record.get_value()</span><br><span class="line"></span><br><span class="line">                <span class="comment"># 更新事件的相应字段</span></span><br><span class="line">                <span class="keyword">if</span> field_name == <span class="string">&quot;fqIndex&quot;</span>:</span><br><span class="line">                    events[timestamp][<span class="string">&quot;fqIndex&quot;</span>] = field_value</span><br><span class="line">                <span class="keyword">elif</span> field_name == <span class="string">&quot;code&quot;</span>:</span><br><span class="line">                    events[timestamp][<span class="string">&quot;code&quot;</span>] = field_value</span><br><span class="line">                <span class="keyword">elif</span> field_name == <span class="string">&quot;reportTime&quot;</span>:</span><br><span class="line">                    events[timestamp][<span class="string">&quot;reportTime&quot;</span>] = field_value</span><br><span class="line">                <span class="keyword">elif</span> field_name == <span class="string">&quot;channel&quot;</span>:</span><br><span class="line">                    events[timestamp][<span class="string">&quot;channel&quot;</span>] = field_value</span><br><span class="line">                <span class="keyword">elif</span> field_name == <span class="string">&quot;desc&quot;</span>:</span><br><span class="line">                    events[timestamp][<span class="string">&quot;desc&quot;</span>] = field_value</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 将事件转换为所需的元组格式</span></span><br><span class="line">        event_list = []</span><br><span class="line">        <span class="keyword">for</span> timestamp, event <span class="keyword">in</span> events.items():</span><br><span class="line">            event_list.append((</span><br><span class="line">                event[<span class="string">&quot;fqIndex&quot;</span>],  <span class="comment"># f_dtc_index</span></span><br><span class="line">                event[<span class="string">&quot;code&quot;</span>],  <span class="comment"># f_notify</span></span><br><span class="line">                event[<span class="string">&quot;f_create_time&quot;</span>],  <span class="comment"># f_create_time (入库时间)</span></span><br><span class="line">                datetime.fromtimestamp(event[<span class="string">&quot;reportTime&quot;</span>] / <span class="number">1000</span>) <span class="keyword">if</span> event[<span class="string">&quot;reportTime&quot;</span>] <span class="keyword">else</span> <span class="literal">None</span>,  <span class="comment"># f_report_time (上报时间)</span></span><br><span class="line">                event[<span class="string">&quot;channel&quot;</span>],  <span class="comment"># f_channel</span></span><br><span class="line">                event[<span class="string">&quot;desc&quot;</span>]  <span class="comment"># f_desc</span></span><br><span class="line">            ))</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> event_list</span><br><span class="line">    <span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">        logger.error(<span class="string">f&quot;Error querying InfluxDB: <span class="subst">&#123;e&#125;</span>&quot;</span>)</span><br><span class="line">        <span class="keyword">return</span> []</span><br></pre></td></tr></table></figure>
<h3 id="其他问题">其他问题</h3>
<p>上面的代码衍生出了一个问题就是influxdb的时间戳时区设置，听公司的前辈说时间戳要设为UTC，否则会出现一些很多问题，于是在查询语句前新增了时区转换。</p>
<p>此外influxdb不支持int索引。</p>
]]></content>
      <categories>
        <category>Database</category>
      </categories>
      <tags>
        <tag>influxdb</tag>
      </tags>
  </entry>
  <entry>
    <title>前端浏览器缓存机制</title>
    <url>/posts/50ac740b/</url>
    <content><![CDATA[<h2 id="前端浏览器缓存机制">前端浏览器缓存机制</h2>
<p>浏览器缓存机制通过强缓存和协商缓存两种方式，有效提升网页加载性能和用户体验。</p>
<span id="more"></span>
<p>浏览器的缓存机制是前端优化的重要手段之一，它通过减少对服务器的请求次数和数据传输量，提升网页加载速度和用户体验。浏览器的缓存机制主要包括两种类型：<strong>强缓存</strong>和<strong>协商缓存</strong>。此外，哈希（hash）也是一种与缓存相关的优化手段。</p>
<h4 id="强缓存strong-caching">1. 强缓存（Strong Caching）</h4>
<p><strong>强缓存</strong>是指在不向服务器发送请求的情况下，<strong>直接从浏览器缓存中读取资源</strong>。这种方式的优点是可以极大地减少请求数，提高页面加载速度。强缓存主要通过以下两种HTTP头来实现：</p>
<ul>
<li><p><strong>Expires</strong>:
一个绝对时间的GMT格式字符串，表示资源的到期时间。浏览器在这个时间之前都会直接使用缓存，不会发送请求。但由于客户端的时间可能不准，这种方式现在较少使用。</p></li>
<li><p><strong>Cache-Control</strong>:
这是一个相对时间的设置方式，使用更广泛。<code>Cache-Control</code>通过指定一个时间长度（例如
<code>max-age=3600</code>，表示3600秒后过期）来控制资源的缓存时间。它可以使用多个指令组合，如：</p>
<ul>
<li><code>public</code>:
资源可以被任何缓存（包括浏览器、CDN等）缓存。</li>
<li><code>private</code>:
资源仅能被浏览器缓存，不能被共享缓存（如CDN）缓存。</li>
<li><code>no-cache</code>:
资源会被缓存，但每次使用前必须先向服务器验证缓存是否有效（实际上会走协商缓存）。</li>
<li><code>no-store</code>: 完全不缓存，资源每次都需要重新请求。</li>
</ul></li>
</ul>
<p><strong>判断是否命中强缓存</strong>：如果命中了强缓存，浏览器不会与服务器通信，状态码为
<code>200 (from cache)</code>。</p>
<h4 id="协商缓存negotiation-caching">2. 协商缓存（Negotiation
Caching）</h4>
<p><strong>协商缓存</strong>是指当强缓存失效或未命中时，浏览器会向服务器发送请求，询问资源是否有更新。如果资源未更新，则可以继续使用缓存的副本，若资源已更新，则下载新的资源并替换缓存。</p>
<p>协商缓存的实现主要依赖以下两种HTTP头：</p>
<ul>
<li><strong>Last-Modified &amp; If-Modified-Since</strong>:
<ul>
<li><strong>Last-Modified</strong>:
服务器在响应头中返回资源的最后修改时间。</li>
<li><strong>If-Modified-Since</strong>: 浏览器在请求头中带上上次缓存的
<code>Last-Modified</code>
时间。服务器比较这个时间和资源的最后修改时间，如果没有变化则返回
<code>304 Not Modified</code>，浏览器继续使用缓存；如果资源已更新，则返回
<code>200</code> 和新资源。</li>
</ul></li>
<li><strong>ETag &amp; If-None-Match</strong>:
<ul>
<li><strong>ETag</strong>:
服务器为资源生成的唯一标识符（哈希值），资源改变时，ETag 也会改变。</li>
<li><strong>If-None-Match</strong>: 浏览器在请求头中带上上次缓存的 ETag
值。服务器比较该值与当前资源的 ETag，如果匹配则返回
<code>304 Not Modified</code>，浏览器继续使用缓存；如果不匹配，则返回
<code>200</code> 和新资源。</li>
</ul></li>
</ul>
<p><strong>判断是否命中协商缓存</strong>：如果命中协商缓存，浏览器会收到
<code>304 Not Modified</code> 状态码，表示可以继续使用缓存。</p>
<h4 id="哈希与缓存hash">3. 哈希与缓存（Hash）</h4>
<p><strong>哈希（Hash）</strong>与缓存机制紧密相关，尤其在前端资源文件（如
JavaScript、CSS、图片等）的缓存管理中非常有用。通过在文件名或路径中添加哈希值（通常是文件内容的哈希值），可以确保当文件内容发生变化时，浏览器会请求新资源而不是使用旧缓存。</p>
<p><strong>典型用法</strong>：</p>
<ul>
<li>文件名中添加哈希值：<code>main.abc123.js</code>。每次文件内容变化时，哈希值都会改变，这样浏览器就会识别为一个新文件，避免使用旧缓存。</li>
<li>结合 <code>Cache-Control</code> 的 <code>immutable</code>
标识符，确保不会重新验证文件（因为文件名唯一标识了内容，不会改变）。</li>
</ul>
<p><strong>总结</strong>：</p>
<ul>
<li><strong>强缓存</strong>可以直接避免请求，提升性能；</li>
<li><strong>协商缓存</strong>在资源更新时能保证用户获取最新内容，同时避免不必要的下载；</li>
<li><strong>哈希</strong>通过文件名标识内容变化，结合缓存机制有效管理前端资源的更新与缓存。</li>
</ul>
<p>通过合理配置这三者，前端开发可以显著优化网页加载速度和用户体验。</p>
<h3 id="什么是cdn内容分发网络">什么是CDN（内容分发网络）</h3>
<p><strong>CDN（Content Delivery Network，内容分发网络）</strong>
是一种通过地理位置分散的服务器集群来加速用户访问网站内容的技术。它的核心目的是将网站的静态资源（如图片、CSS、JavaScript
文件、视频等）分布到靠近用户的节点上，以提高访问速度和减少服务器的负载。</p>
<h4 id="cdn的工作原理">CDN的工作原理</h4>
<ol type="1">
<li><p><strong>内容缓存</strong>：网站的静态资源会被缓存到CDN的多个节点服务器上。这些节点分布在全球各地，每个节点都称为一个“边缘服务器”。</p></li>
<li><p><strong>用户请求</strong>：当用户访问一个使用CDN的网站时，用户的请求会被自动路由到离他们最近的CDN节点，而不是直接请求网站的源服务器。</p></li>
<li><p><strong>缓存命中</strong>：如果该节点缓存中已有用户请求的资源，则直接返回给用户，从而减少延迟和负载。</p></li>
<li><p><strong>缓存未命中</strong>：如果缓存中没有该资源，CDN节点会向源服务器请求该资源，之后将资源传递给用户，并缓存到本地以备后续用户请求。</p></li>
</ol>
<h4 id="cdn的主要优势">CDN的主要优势</h4>
<ol type="1">
<li><p><strong>加速内容传递</strong>：通过将内容放在离用户更近的服务器上，CDN能显著减少数据传输的时间，提升网页加载速度。</p></li>
<li><p><strong>减轻服务器负载</strong>：由于大量的静态内容被分发到CDN节点，源服务器的负载减轻，能够更高效地处理动态请求。</p></li>
<li><p><strong>高可用性和冗余</strong>：CDN通过多个节点提供内容，即使某个节点宕机，用户请求也能被路由到其他节点，提高了内容的可用性和网站的容错性。</p></li>
<li><p><strong>带宽优化</strong>：通过分散用户请求，CDN可以有效减少单个服务器的带宽压力，降低带宽成本。</p></li>
<li><p><strong>安全性增强</strong>：许多CDN服务提供安全功能，如DDoS防护、SSL加速和Web应用防火墙（WAF），从而增强网站的整体安全性。</p></li>
</ol>
<h4 id="cdn的使用场景">CDN的使用场景</h4>
<ul>
<li><strong>大型网站</strong>：如电商网站、视频流媒体服务、社交媒体平台等，需要为全球用户提供快速、稳定的访问体验。</li>
<li><strong>高流量事件</strong>：如体育赛事直播、大型促销活动等，CDN能有效分散流量，避免服务器崩溃。</li>
<li><strong>静态资源分发</strong>：如网站的图片、CSS、JavaScript
文件等，通过CDN可以提升资源加载速度。</li>
</ul>
<p>总之，CDN通过将内容分布到全球各地的边缘服务器上，加快了用户访问的速度，提升了网站的性能和可用性，是现代网络架构中不可或缺的一部分。</p>
]]></content>
      <categories>
        <category>前端</category>
      </categories>
      <tags>
        <tag>前端</tag>
      </tags>
  </entry>
  <entry>
    <title>机器学习入门01</title>
    <url>/posts/debf589c/</url>
    <content><![CDATA[<p>本科毕设的研究方向与隐私计算有关，联邦学习算是其中的一个分支技术，机器学习算是其中的一个基础理论。因此打算记录一下机器学习的一些基础知识。</p>
<span id="more"></span>
<h2 id="参考教程">参考教程</h2>
<p>以下是我计划学习的机器学习课程资源：</p>
<ul>
<li><p><a
href="https://developers.google.cn/machine-learning/crash-course/linear-regression?hl=zh-cn">Google机器学习速成课程</a>
- Google官方入门教程，内容简洁实用</p></li>
<li><p><a
href="https://search.bilibili.com/all?keyword=%E5%90%B4%E6%81%A9%E8%BE%BE%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0">吴恩达机器学习课程</a>
- Coursera上最受欢迎的机器学习课程之一，适合入门</p></li>
<li><p><a
href="https://www.bilibili.com/video/BV1YsqSY8EiW">李宏毅机器学习课程</a>
- 台湾大学教授的机器学习课程，讲解深入浅出</p></li>
<li><p><a
href="https://www.bilibili.com/video/BV1if4y147hS">李沐动手学深度学习</a>
- 亚马逊资深科学家的实战课程，注重代码实践</p></li>
</ul>
<p>今天先从google的机器学习速成课程开始。</p>
<h2 id="什么是机器学习machine-learning">什么是机器学习(Machine
Learning)？</h2>
<p>从基本层面来说，机器学习是指训练一款名为模型(Model)的软件，以便做出有用的预测或根据数据生成内容。</p>
<h3 id="机器学习系统类型">机器学习系统类型</h3>
<p>根据机器学习系统学习预测或生成内容的方式，它们可分为以下一个或多个类别：</p>
<ul>
<li>监督式学习(Supervised Learning)</li>
<li>非监督式学习(Unsupervised Learning)</li>
<li>强化学习(Reinforcement Learning)</li>
<li>生成式 AI(Generative AI)</li>
</ul>
<h3 id="监督式学习supervised-learning">监督式学习(Supervised
Learning)</h3>
<p>监督式学习模型在看到包含正确答案的大量数据后，可以发现产生正确答案的数据元素之间的关联，然后进行预测。这就像学生通过研究包含问题和答案的旧考试来学习新材料。学生在使用足够多的旧版考试进行训练后，就可以做好准备参加新版考试了。这些机器学习系统是"监督式"的，这意味着人类会向机器学习系统提供包含已知正确结果的数据。</p>
<p>监督式学习的两种最常见用例是回归(Regression)和分类(Classification)。</p>
<h4 id="回归regression">回归(Regression)</h4>
<p>回归模型可以预测连续的数值(Continuous
Value)。回归模型的输出是一个具体的数字,比如预测房价是500万元,或预测明天的降雨量是50毫米。</p>
<p>以下是一些典型的回归应用场景:</p>
<table>
<thead>
<tr>
<th>场景</th>
<th>输入数据</th>
<th>预测结果</th>
</tr>
</thead>
<tbody>
<tr>
<td>房价预测</td>
<td>建筑面积、位置、装修等级、周边配套等</td>
<td>房屋价格(元)</td>
</tr>
<tr>
<td>行程时间预测</td>
<td>路况、天气、历史数据等</td>
<td>到达时间(分钟)</td>
</tr>
</tbody>
</table>
<h4 id="分类classification">分类(Classification)</h4>
<p>分类模型用于预测某个对象属于特定类别的可能性。与输出连续数值的回归模型不同,分类模型输出的是离散的类别(Discrete
Category)标签(Category Label)。</p>
<p>分类模型可以分为:</p>
<ul>
<li>二元分类(Binary
Classification):输出仅包含两个类别,如判断邮件是否为垃圾邮件(是/否)</li>
<li>多类分类(Multi-class
Classification):可以输出多个类别,如识别图片中的天气类型(晴天/雨天/雪天等)</li>
</ul>
<p>分类的典型应用包括:</p>
<ul>
<li>垃圾邮件检测</li>
<li>图像识别</li>
<li>疾病诊断</li>
<li>信用评估</li>
</ul>
<h3 id="非监督式学习unsupervised-learning">非监督式学习(Unsupervised
Learning)</h3>
<p>非监督式学习模型通过获得不含任何正确答案的数据来进行预测。非监督式学习模型的目标是找出数据中具有意义的模式。换句话说，模型没有关于如何对每项数据进行分类的提示，而是必须推断自己的规则。</p>
<p>一种常用的非监督式学习模型采用了一种称为聚类(Clustering)的技术。该模型会找到用于划分自然分组的数据点。</p>
<p><img src="/assets/机器学习入门01.png" /></p>
<h3 id="强化学习reinforcement-learning">强化学习(Reinforcement
Learning)</h3>
<p>强化学习(Reinforcement
Learning)模型根据在环境中执行的操作获得奖励或惩罚，从而进行预测。强化学习系统会生成政策，定义用于获得最多奖励的最佳策略。</p>
<p>强化学习用于训练机器人执行任务（例如在房间内四处走动），以及训练
AlphaGo 等软件程序玩围棋。</p>
<h3 id="生成式-aigenerative-ai">生成式 AI(Generative AI)</h3>
<p>生成式 AI 是一类根据用户输入生成内容的模型。例如，生成式 AI
可以创作独特的图片、音乐作品和笑话；它可以总结文章、说明如何执行任务或编辑照片。</p>
<p>生成式 AI
可以接受各种输入，并创建各种输出，例如文本、图片、音频和视频。它还可以接受和创建这些值的组合。例如，模型可以接受图片作为输入，并创建图片和文本作为输出，也可以接受图片和文本作为输入，并创建视频作为输出。</p>
<p>我们可以通过生成模型的输入和输出来讨论它们，通常以"输入类型"到"输出类型"的形式写出。例如，以下是生成式模型的部分输入和输出列表：</p>
<ul>
<li>文本到文本</li>
<li>文本到图像</li>
<li>文本到视频</li>
<li>文本转代码</li>
<li>文本转语音</li>
<li>图片和文本到图片</li>
</ul>
<p>生成式 AI
如何运作？概括来讲，生成式模型会学习数据中的模式，目标是生成新的但类似的数据。
为了生成独特且富有创意的输出，生成式模型最初使用非监督式方法进行训练，在这种方法中，模型会学习模仿其训练数据。有时，系统会使用监督学习或强化学习对与模型可能被要求执行的任务（例如总结文章或编辑照片）相关的特定数据进行进一步训练。</p>
<h3 id="监督式学习基础概念">监督式学习基础概念</h3>
<p>监督式学习的任务定义明确，可用于多种场景，例如识别垃圾内容或预测降水。</p>
<p>监督式机器学习基于以下核心概念：</p>
<ul>
<li>数据(Data)</li>
<li>模型(Model)</li>
<li>训练(Training)</li>
<li>评估(Evaluation)</li>
<li>推理(Inference)</li>
</ul>
<h4 id="数据data">数据(Data)</h4>
<p>数据是机器学习的驱动力。数据的形式为存储在表中的单词和数字，或以图片和音频文件中捕获的像素和波形值的形式。我们将相关数据存储在数据集(Dataset)中。</p>
<p>例如，我们可能拥有以下类型的数据集：</p>
<ul>
<li>猫的图片集</li>
<li>房价数据</li>
<li>天气信息</li>
</ul>
<p>数据集由包含特征(Feature)和标签(Label)的各个样本(Sample)组成。您可以将样本视为电子表格中的一行。其中：</p>
<ul>
<li>特征是监督式模型用于预测标签的值</li>
<li>标签就是"答案"，也就是我们想要模型预测的值</li>
</ul>
<p>以预测降雨的天气模型为例：</p>
<table>
<thead>
<tr>
<th>类型</th>
<th>内容</th>
</tr>
</thead>
<tbody>
<tr>
<td>特征</td>
<td>纬度、经度、温度、湿度、云覆盖范围、风向、气压</td>
</tr>
<tr>
<td>标签</td>
<td>降雨量</td>
</tr>
</tbody>
</table>
<p>同时包含特征和标签的样本称为有标签样本(Labeled Sample)。</p>
<h4 id="数据集特征dataset-characteristics">数据集特征(Dataset
Characteristics)</h4>
<p>数据集的特征是规模(Scale)和多样性(Diversity)。大小表示样本数量。多样性表示这些样本所涵盖的范围。好的数据集要既大又多样化。</p>
<p>有些数据集规模庞大且丰富多样。然而，有些数据集很大但多样性较低，还有一些虽然较小但多样性较高。换言之，大型数据集不能保证足够的多样性，高度多样性的数据集也无法保证有足够的样本。</p>
<p>例如：</p>
<ul>
<li>数据集可能包含 100 年的数据，但只包含 7
月份的数据。使用此数据集来预测 1 月的降雨量会产生较差的预测。</li>
<li>相反，数据集可能仅涵盖几年，但包含每月。此数据集可能产生较差的预测，因为它未涵盖足够的年份解释可变性。</li>
</ul>
<p>数据集的特征数量也可以作为特征。例如：</p>
<ul>
<li>某些天气数据集可能包含数百个特征，从卫星图像到云覆盖率值，不一而足</li>
<li>其他数据集可能只包含三四个特征，例如湿度、大气压力和温度</li>
</ul>
<p>具有更多特征的数据集有助于模型发现其他模式并做出更好的预测。但是，特征较多的数据集并不一定生成的模型能够提供更好的预测，因为某些特征可能与标签没有因果关系。</p>
<h4 id="模型model">模型(Model)</h4>
<p>在监督式学习中，模型是复杂的数字集合，用于定义从特定输入特征模式与特定输出标签值的数学关系。模型通过训练发现这些模式。</p>
<h4 id="训练training">训练(Training)</h4>
<p>监督式模型必须先接受训练，然后才能进行预测。为了训练模型，我们为模型提供一个带有标签样本的数据集。</p>
<p>训练过程包括以下步骤：</p>
<ol type="1">
<li>模型的目标是找到根据特征预测标签的最佳解决方案</li>
<li>模型通过将预测值与标签的实际值进行比较来找到最佳解决方案</li>
<li>根据预测值与实际值（定义为损失(Loss)）之间的差异，模型会逐步更新其解决方案</li>
<li>模型会学习特征与标签之间的数学关系，以便对未见过的数据做出最佳预测</li>
</ol>
<p><img src="/assets/机器学习入门02.png" /></p>
<p>例如，如果模型预测降雨量为 1.15 inches，但实际值为 0.75
inches，则模型会修改其解决方案，使其预测更接近 0.75
inches。在模型查看数据集中的每个样本（在某些情况下，多次查看）后，会得到一个能够为每个样本做出平均最佳预测的解决方案。</p>
<p>通过这种方式，模型可以逐步学习特征与标签之间的正确关系。这种逐渐理解的机制也是为什么庞大、多样化的数据集能够产生更好的模型的原因。该模型看到了更多值范围更广的数据，并且改进了对特征与标签之间关系的理解。</p>
<p>在训练期间，机器学习从业者可以对模型用于进行预测的配置和功能进行细微调整。例如，某些特征的预测能力高于其他特征。因此，机器学习从业者可以选择模型在训练期间使用哪些特征。例如，假设天气数据集包含
time_of_day
作为特征。在这种情况下，机器学习从业者可以在训练期间添加或移除
time_of_day，以查看在使用或不使用该模型的情况下，模型是否能够更好地进行预测。</p>
<h4 id="评估evaluation">评估(Evaluation)</h4>
<p>我们会评估经过训练的模型，以确定它的学习效果。在评估模型时，我们会使用加标签的数据集，但仅向模型提供数据集的特征。然后，我们会将模型的预测结果与标签的真实值进行比较。</p>
<p>评估模型时，我们通常会使用以下指标：</p>
<ul>
<li>准确率(Accuracy)</li>
<li>精确率(Precision)</li>
<li>召回率(Recall)</li>
</ul>
<p>根据模型的预测，我们可能会在实际应用中部署模型之前进行更多的训练和评估。</p>
<h4 id="推理inference">推理(Inference)</h4>
<p>对评估模型的结果感到满意后，我们就可以使用模型对无标签样本进行预测（称为推断）。在天气应用示例中，我们将为模型提供当前的天气条件（如温度、大气压和相对湿度），并预测降雨量。</p>
<p>以上是机器学习的基础概念，后续的内容请看下一篇博客。</p>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>机器学习</tag>
      </tags>
  </entry>
  <entry>
    <title>机器学习入门02</title>
    <url>/posts/ee0cb7e3/</url>
    <content><![CDATA[<p>今天继续进行机器学习的入门，主题是线性回归。 <span id="more"></span></p>
<h2 id="线性回归linear-regression">线性回归(Linear Regression)</h2>
<p>线性回归是一种
用于找出各个变量之间的关系的统计技术。在机器学习中线性回归模型会找出
功能和 label 之间的关联。</p>
<p>例如，假设我们要预测汽车的燃油效率（以英里/英里为单位）
根据汽车的重量确定加仑，我们有以下数据集：</p>
<table>
<thead>
<tr>
<th>干磷(功能)</th>
<th>英里/加仑(标签)</th>
</tr>
</thead>
<tbody>
<tr>
<td>3.5</td>
<td>18</td>
</tr>
<tr>
<td>3.69</td>
<td>15</td>
</tr>
<tr>
<td>3.44</td>
<td>18</td>
</tr>
<tr>
<td>3.43</td>
<td>16</td>
</tr>
<tr>
<td>4.34</td>
<td>15</td>
</tr>
<tr>
<td>4.42</td>
<td>14</td>
</tr>
<tr>
<td>2.37</td>
<td>24</td>
</tr>
</tbody>
</table>
<p>如果我们绘制这些点，通过这些点绘制一条最适合的直线来创建自己的模型,就会得到以下图表：</p>
<p><img src="/assets/机器学习入门03.png" /></p>
<h3 id="线性回归方程linear-regression-equation">线性回归方程(Linear
Regression Equation)</h3>
<p>用代数术语来说，模型的定义为：</p>
<p><span class="math display">\[y = mx + b\]</span></p>
<p>其中：</p>
<ul>
<li>y 表示每加仑英里数，即我们想要预测的值</li>
<li>m 是直线的斜率</li>
<li>x 是磷，即我们的输入值</li>
<li>b 为 y 轴截距</li>
</ul>
<p>在机器学习中，我们编写线性回归模型的方程式如下所示：</p>
<p><span class="math display">\[y&#39; = b + w_1x_1\]</span></p>
<p>其中：</p>
<ul>
<li>y' 是预测标签，即输出</li>
<li>b
是偏差(Bias)模型。偏差与代数中y截距的概念相同直线方程。在机器学习中，偏差有时称为
w₀。偏差是一个模型的参数，而且是在训练期间计算的</li>
<li>w₁ 是权重(Weight)功能。权重与代数中斜率 m
的概念相同直线方程。权重为参数，是在训练期间计算的</li>
<li>x₁ 是一项特征(Feature)，即输入</li>
</ul>
<p>在训练期间，模型会计算可产生最佳结果的权重和偏差模型。</p>
<p><img src="/assets/机器学习入门04.png" /></p>
<p>在我们的示例中，通过终止的线性计算得到：</p>
<ul>
<li>偏差为 30 (对应 y 轴截距)</li>
<li>权重为 -3.6 (直线的斜率)</li>
</ul>
<p>因此，我们的模型可以表示为：</p>
<p><span class="math display">\[y&#39; = 30 + (-3.6)(x_1)\]</span></p>
<p>现在我们就可以用这个模型进行预测了。例如，对于一辆重量为 4000
磷的汽车，我们可以预测其燃油效率：</p>
<p><span class="math display">\[y&#39; = 30 + (-3.6)(4.0) = 30 - 14.4 =
15.6\]</span></p>
<p>预测结果表明这辆汽车的燃油效率约为每加仑15.6英里。</p>
<h3
id="具有多个特征的模型models-with-multiple-features">具有多个特征的模型(Models
with Multiple Features)</h3>
<p>虽然本文中的示例仅使用了一项功能（即重量级汽车的特征），但更复杂的模型可能依赖于多种特征，每个特征都有单独的权重(w₁、w₂
等)。例如，一个模型可以写为：</p>
<p><span class="math display">\[y&#39; = b + w_1x_1 + w_2x_2 + w_3x_3 +
w_4x_4 + w_5x_5\]</span></p>
<p>例如，预测汽油里程的模型可以额外使用特征如：</p>
<ul>
<li>发动机排量 (Displacement)</li>
<li>加速性能 (Acceleration)</li>
<li>汽缸数 (Number of cylinders)</li>
<li>马力 (Horsepower)</li>
</ul>
<p>此模型的编写方式如下图所示：</p>
<p><img src="/assets/机器学习入门05.png" /></p>
<p>在这个模型中：</p>
<ul>
<li>x₁ 代表重量 (Pounds)</li>
<li>x₂ 代表排量 (Displacement)</li>
<li>x₃ 代表加速性能 (Acceleration)</li>
<li>x₄ 代表汽缸数 (Number of cylinders)</li>
<li>x₅ 代表马力 (Horsepower)</li>
</ul>
<p>每个特征(x)都有其对应的权重(w)，这些权重在训练过程中被优化，以获得最准确的预测结果。</p>
<h3 id="线性回归损失loss">线性回归：损失(Loss)</h3>
<p>损失是一个数值指标，用于描述模型的预测有多大偏差。损失函数用于衡量模型预测与实际标签之间的距离。训练模型的目标是尽可能降低损失，将其降至最低值。</p>
<p>在下图中，可以将损失可视化为从数据点指向模型的箭头。箭头表示模型的预测结果与实际值之间的差距。</p>
<p><img src="/assets/机器学习入门06.png" /></p>
<h4 id="损失距离loss-distance">损失距离(Loss Distance)</h4>
<p>在统计学和机器学习中，损失函数用于衡量预测值与实际值之间的差异。损失函数侧重于值之间的距离，而不是方向。例如，如果模型预测值为
2，但实际值为 5，我们并不关心损失为负值（5 - 2 =
-3）。我们关心的是这两个值之间的距离为
3。因此，所有用于计算损失的方法都会移除符号。</p>
<p>移除此标记的两种最常用方法如下：</p>
<ol type="1">
<li><p>计算实际值与预测值之间的差值的绝对值： <span
class="math display">\[L1 = |y - y&#39;|\]</span></p></li>
<li><p>将实际值与预测值之间的差值平方： <span class="math display">\[L2
= (y - y&#39;)^2\]</span></p></li>
</ol>
<h4 id="损失类型">损失类型</h4>
<p>在线性回归中，有四种主要的损失函数，如下表所示。</p>
<p><img src="/assets/机器学习入门07.png" /></p>
<p>L₁ 损失函数和 L₂ 损失函数（或 MAE 和
MSE）之间的功能差异在于平方。当预测值与标签之间的差异较大时，平方会使损失变得更大。当差异较小（小于
1）时，平方会使损失更小。</p>
<p>当处理多个示例时，我们通常会对所有示例的损失进行平均，无论是使用 MAE
还是 MSE。</p>
<blockquote>
<ul>
<li>MSE (Mean Square Error)：均方误差</li>
<li>计算公式：MSE = (Σ(预测值 - 实际值)²) / n</li>
<li>RMSE (Root Mean Square Error)：均方根误差</li>
<li>计算公式：RMSE = √MSE</li>
<li>即MSE开平方根</li>
</ul>
</blockquote>
<h4 id="损失计算示例">损失计算示例</h4>
<p>使用之前的最佳拟合直线，我们将计算单个示例的 L₂
损失。从最优拟合直线中，我们得到了权重和偏差的以下值：</p>
<ul>
<li>Weight: -3.6</li>
<li>Bias: 30</li>
</ul>
<p>如果模型预测重 2,370 磅的汽车每加仑可行驶 21.5
英里，但实际每加仑可行驶 24 英里，我们将按如下方式计算 L₂ 损失：</p>
<p><span class="math display">\[L_2 = (y - y&#39;)^2 = (24 - 21.5)^2 =
(2.5)^2 = 6.25\]</span></p>
<p>在此示例中，该单个数据点的 L₂ 损失为 6.25。</p>
<h4 id="选择损失函数choosing-loss-functions">选择损失函数(Choosing Loss
Functions)</h4>
<p>确定使用 MAE 还是 MSE
可能取决于数据集以及希望处理特定预测的方式。数据集中的大多数特征值通常属于一个特定范围，例如：</p>
<ul>
<li>汽车通常在 2,000 到 5,000 磅之间</li>
<li>每加仑汽油能行驶的英里数介于 8 到 50 英里之间</li>
<li>重 8,000 磅的汽车或每加仑汽油能行驶 100
英里的汽车都超出了典型范围，会被视为离群值(Outliers)</li>
</ul>
<h4 id="离群值处理">离群值处理</h4>
<p>离群值还可以指模型的预测与真实值之间的差距。例如：</p>
<ul>
<li>3,000 磅的车重属于典型的车重范围</li>
<li>每加仑 40 英里的油耗属于典型的油耗范围</li>
<li>但是，对于每加仑 40 英里的 3,000 磅汽车，如果模型预测每加仑能行驶 18
到 20 英里，这个预测结果会认为是离群值</li>
</ul>
<h4 id="损失函数的选择">损失函数的选择</h4>
<p>选择最佳损失函数时，考虑希望模型如何处理离群值：</p>
<ul>
<li>MSE 会使模型更接近离群值</li>
<li>MAE 则不会</li>
<li>与 L₁ 损失函数相比，L₂ 损失函数对离群值的惩罚更高</li>
</ul>
<p>例如，下图片显示了使用 MAE 训练的模型和使用 MSE
训练的模型的比较。红线表示将用于进行预测的完全训练好的模型：</p>
<ul>
<li>离群值更接近使用 MSE 训练的模型</li>
<li>离群值对使用 MAE 训练的模型影响较小</li>
</ul>
<p><img src="/assets/机器学习入门08.png" /></p>
<h3 id="线性回归梯度下降gradient-descent">线性回归：梯度下降(Gradient
Descent)</h3>
<p>梯度下降法是一种数学技术，能够以迭代方式找出权重和偏差，从而生成损失最低的模型。梯度下降法会针对用户指定的多次迭代重复以下过程，以找到最佳权重和偏差。</p>
<p>模型开始训练时使用接近于零的随机权重和偏差，然后重复以下步骤：</p>
<ol type="1">
<li>使用当前权重和偏差计算损失。</li>
<li>确定用于减少损失的权重和偏差的移动方向。</li>
<li>将权重和偏差值在可减少损失的方向上稍微移动。</li>
<li>返回第 1 步并重复此过程，直到模型无法进一步降低损失。</li>
</ol>
<p>下图概述了梯度下降法为找出损失最低的模型权重和偏差所执行的迭代步骤。</p>
<p><img src="/assets/机器学习入门09.png" /></p>
<h4 id="梯度下降实例">梯度下降实例</h4>
<h4 id="示例数据集">示例数据集</h4>
<p>我们使用一个包含 7
个示例的小型数据集来演示梯度下降，这些示例分别对应汽车的重量(以磅为单位)
和每加仑行驶里程数：</p>
<table>
<thead>
<tr>
<th>以千计的英磅 (特征)</th>
<th>每加仑英里 (标签)</th>
</tr>
</thead>
<tbody>
<tr>
<td>3.5</td>
<td>18</td>
</tr>
<tr>
<td>3.69</td>
<td>15</td>
</tr>
<tr>
<td>3.44</td>
<td>18</td>
</tr>
<tr>
<td>3.43</td>
<td>16</td>
</tr>
<tr>
<td>4.34</td>
<td>15</td>
</tr>
<tr>
<td>4.42</td>
<td>14</td>
</tr>
<tr>
<td>2.37</td>
<td>24</td>
</tr>
</tbody>
</table>
<h4 id="梯度下降步骤">梯度下降步骤</h4>
<h5 id="初始化模型参数">1. 初始化模型参数</h5>
<p>模型会先将权重和偏差设为零，然后开始训练： <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Weight: 0</span><br><span class="line">Bias: 0</span><br><span class="line">y = 0 + 0(x₁)</span><br></pre></td></tr></table></figure></p>
<h5 id="计算初始损失">2. 计算初始损失</h5>
<p>使用当前的模型参数计算 MSE 损失： <span class="math display">\[Loss =
\frac{(18-0)^2 + (15-0)^2 + (18-0)^2 + (16-0)^2 + (15-0)^2 + (14-0)^2 +
(24-0)^2}{7}\]</span> <span class="math display">\[Loss =
303.71\]</span></p>
<h5 id="计算梯度">3. 计算梯度</h5>
<p>我们需要计算损失函数相对于权重和偏差的导数。预测函数定义为： <span
class="math display">\[f_{w,b}(x) = (w * x) + b\]</span></p>
<p>MSE 损失函数为： <span class="math display">\[\frac{1}{M}
\sum_{i=1}^M (f_{w,b}(x_{(i)}) - y_{(i)})^2\]</span> 其中 <span
class="math inline">\(i\)</span> 表示第 <span
class="math inline">\(i\)</span> 个训练示例，<span
class="math inline">\(M\)</span> 表示示例数量。</p>
<h6 id="权重的梯度">权重的梯度</h6>
<p>权重的导数计算： <span
class="math display">\[\frac{\partial}{\partial w} \frac{1}{M}
\sum_{i=1}^M (f_{w,b}(x_{(i)}) - y_{(i)})^2\]</span></p>
<p>求值结果： <span class="math display">\[\frac{1}{M} \sum_{i=1}^M
(f_{w,b}(x_{(i)}) - y_{(i)}) * 2x_{(i)}\]</span></p>
<p>当权重和偏差为零时，计算得到斜率为 -119.7。</p>
<h6 id="偏差的梯度">偏差的梯度</h6>
<p>偏差的导数计算： <span
class="math display">\[\frac{\partial}{\partial b} \frac{1}{M}
\sum_{i=1}^M (f_{w,b}(x_{(i)}) - y_{(i)})^2\]</span></p>
<p>求值结果： <span class="math display">\[\frac{1}{M} \sum_{i=1}^M
(f_{w,b}(x_{(i)}) - y_{(i)}) * 2\]</span></p>
<p>当权重和偏差为零时，计算得到斜率为 -34.3。</p>
<h5 id="更新参数">4. 更新参数</h5>
<p>向负斜率方向移动一小步（学习率设为 0.01）：</p>
<p><span class="math display">\[New\ weight = old\ weight - (0.01 *
weight\ slope)\]</span> <span class="math display">\[New\ bias = old\
bias - (0.01 * bias\ slope)\]</span></p>
<p>第一次迭代的计算： <span class="math display">\[New\ weight = 0 -
(0.01) * (-119.7) = 1.2\]</span> <span class="math display">\[New\ bias
= 0 - (0.01) * (-34.3) = 0.34\]</span></p>
<h4 id="迭代结果">迭代结果</h4>
<p>重复上述步骤六次后，得到以下结果：</p>
<table>
<thead>
<tr>
<th>迭代</th>
<th>权重</th>
<th>偏差</th>
<th>损失 (MSE)</th>
</tr>
</thead>
<tbody>
<tr>
<td>1</td>
<td>0</td>
<td>0</td>
<td>303.71</td>
</tr>
<tr>
<td>2</td>
<td>1.2</td>
<td>0.34</td>
<td>170.67</td>
</tr>
<tr>
<td>3</td>
<td>2.75</td>
<td>0.59</td>
<td>67.3</td>
</tr>
<tr>
<td>4</td>
<td>3.17</td>
<td>0.72</td>
<td>50.63</td>
</tr>
<tr>
<td>5</td>
<td>3.47</td>
<td>0.82</td>
<td>42.1</td>
</tr>
<tr>
<td>6</td>
<td>3.68</td>
<td>0.9</td>
<td>37.74</td>
</tr>
</tbody>
</table>
<h4 id="收敛过程">收敛过程</h4>
<p>在训练过程中,损失值会随着迭代次数的增加而不断降低。当损失值趋于稳定,不再显著下降时,我们就说模型达到了收敛状态。此时继续训练,损失值可能会出现小幅波动,但整体保持稳定。</p>
<h4 id="损失曲线">损失曲线</h4>
<p>损失曲线是观察模型收敛情况的重要工具。它展示了损失值随迭代次数的变化趋势:</p>
<p><img src="/assets/机器学习入门10.png" /></p>
<p>典型的损失曲线有以下特点:</p>
<ul>
<li>初期阶段: 损失值快速下降</li>
<li>中期阶段: 下降速度逐渐放缓</li>
<li>后期阶段: 趋于平稳,表明模型接近收敛</li>
</ul>
<h4 id="凸函数与最小值">凸函数与最小值</h4>
<p>线性回归模型的损失函数是一个凸函数,这意味着它只有一个全局最小值。下图展示了一个单特征模型的损失函数表面:</p>
<p><img src="/assets/机器学习入门11.png" /></p>
<ul>
<li>x轴: 权重 (Weight)</li>
<li>y轴: 偏差 (Bias)</li>
<li>z轴: 损失值 (Loss)</li>
</ul>
<p>这种凸形状保证了:</p>
<ol type="1">
<li>梯度下降一定能找到最小损失点</li>
<li>不会陷入局部最小值</li>
</ol>
<p>需要注意的是:</p>
<ul>
<li>模型通常找到的是接近最小值的点,而不是精确的最小值</li>
<li>最小损失点并不意味着零损失,而是在当前数据下能达到的最优解</li>
</ul>
<h3
id="线性回归超参数hyperparameters">线性回归：超参数(Hyperparameters)</h3>
<p>超参数是用于控制训练过程的变量。常见的超参数包括：</p>
<ul>
<li>学习速率 (Learning Rate)</li>
<li>批量大小 (Batch Size)</li>
<li>纪元 (Epoch)</li>
</ul>
<p>与模型参数(如权重和偏差)不同，超参数是在训练前设置的控制值，而不是在训练过程中学习得到的值。</p>
<h4 id="学习速率learning-rate">学习速率(Learning Rate)</h4>
<p>学习速率决定了每次梯度下降时模型参数的调整步长。它直接影响模型的训练效果：</p>
<ul>
<li>学习速率过低：
<ul>
<li>模型收敛速度慢</li>
<li>需要更多迭代才能达到最优解</li>
<li>训练时间较长</li>
</ul></li>
<li>学习速率过高：
<ul>
<li>可能导致模型无法收敛</li>
<li>参数更新幅度过大，在最优解附近来回震荡</li>
<li>难以找到最优解</li>
</ul></li>
</ul>
<p>选择合适的学习速率很重要： - 需要在训练速度和稳定性之间取得平衡 -
通常从一个较小的值开始尝试（如0.01） - 根据训练效果进行调整</p>
<p>例如：如果梯度为2.5，学习速率为0.01，则参数的更新步长为0.025。理想的学习速率应该使模型在合理的迭代次数内收敛。</p>
<blockquote>
<p>将学习速率翻倍可能会减慢训练速度。
此声明是正确的。将学习率翻倍可能会导致学习率过大，从而导致权重"跳来跳去"，增加收敛所需的时间。</p>
</blockquote>
<h4 id="批量大小-batch-size">批量大小 (Batch Size)</h4>
<p>批量大小是一个超参数，表示每次更新模型参数时使用的训练样本数量。虽然理论上可以在每次迭代时使用所有训练样本，但当数据集很大时（如包含数十万至数百万个样本），这种方法在计算上是不实际的。</p>
<p>为了解决这个问题，有两种常用的梯度下降方法：</p>
<h5 id="随机梯度下降法-sgd-stochastic-gradient-descent">随机梯度下降法
(SGD, Stochastic Gradient Descent)</h5>
<ul>
<li>每次迭代只使用一个随机样本（批量大小为1）</li>
<li>优点：
<ul>
<li>计算速度快</li>
<li>能够跳出局部最优解</li>
</ul></li>
<li>缺点：
<ul>
<li>参数更新波动较大</li>
<li>收敛过程不稳定</li>
</ul></li>
</ul>
<p>下图展示了使用SGD训练时的损失曲线：</p>
<p><img src="/assets/机器学习入门12.png" /></p>
<p>可以看到，虽然整体趋势是下降的，但曲线呈现出明显的波动。这种波动不仅出现在训练初期，在接近收敛时也会存在。</p>
<h5 id="小批量随机梯度下降法-mini-batch-sgd">小批量随机梯度下降法
(mini-batch SGD)</h5>
<ul>
<li>是完整批量梯度下降和SGD的折中方案</li>
<li>每次使用一小批样本（批量大小通常在1到N之间，N为总样本数）</li>
<li>特点：
<ul>
<li>对每个批次计算平均梯度</li>
<li>参数更新更稳定</li>
<li>计算效率较高</li>
</ul></li>
</ul>
<p>批量大小的选择：</p>
<ul>
<li>批量较小时：行为类似于SGD，更新波动大但计算快</li>
<li>批量较大时：行为类似于完整批量梯度下降，更新稳定但计算慢</li>
<li>需要在计算效率和训练稳定性之间找到平衡</li>
</ul>
<p>在训练模型时，噪声并不总是不良特征。适当的噪声可以帮助模型更好地泛化，这一点在神经网络的权重和偏差优化中尤为重要。</p>
<h4 id="纪元-epoch">纪元 (Epoch)</h4>
<p>纪元表示模型完整处理训练集一次的过程。例如，对于包含1,000个样本的训练集，如果批量大小为100，则需要10次迭代(iterations)才能完成一个纪元。</p>
<p>训练通常需要多个纪元，因为模型需要多次处理训练集中的所有样本才能达到较好的效果。纪元数是在训练开始前设置的超参数。一般来说，纪元数越多，模型拟合得越好，但训练时间也越长。</p>
<p>下表比较了不同批处理方式对参数更新次数的影响：</p>
<table>
<colgroup>
<col style="width: 47%" />
<col style="width: 52%" />
</colgroup>
<thead>
<tr>
<th>批处理类型</th>
<th>参数更新频率</th>
</tr>
</thead>
<tbody>
<tr>
<td>完整批次</td>
<td>每个纪元更新一次。例如：1000个样本，训练20个纪元，总共更新20次</td>
</tr>
<tr>
<td>随机梯度下降</td>
<td>每个样本更新一次。例如：1000个样本，训练20个纪元，总共更新20,000次</td>
</tr>
<tr>
<td>小批量梯度下降</td>
<td>每个批次更新一次。例如：1000个样本，批量大小100，训练20个纪元，总共更新200次</td>
</tr>
</tbody>
</table>
<p>从上表可以看出：</p>
<ul>
<li>完整批次：参数更新最少，但每次计算量大</li>
<li>随机梯度下降：参数更新最频繁，但可能不稳定</li>
<li>小批量梯度下降：在更新频率和计算效率之间取得平衡</li>
</ul>
<h3 id="线性回归编程练习">线性回归：编程练习</h3>
<h4 id="part-1---setup-exercise">Part 1 - Setup Exercise</h4>
<p>在机器学习项目中，通常需要以下几类Python库来帮助我们完成不同的任务：</p>
<h5 id="数据处理库">数据处理库</h5>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np  <span class="comment"># 数值计算</span></span><br><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd <span class="comment"># 数据分析</span></span><br></pre></td></tr></table></figure>
<ol type="1">
<li><strong>NumPy</strong> (np)</li>
</ol>
<ul>
<li>用于处理数值数据的基础库</li>
<li>提供高效的数组操作</li>
<li>常用功能示例： <figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 创建数组</span></span><br><span class="line">arr = np.array([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>])</span><br><span class="line"><span class="comment"># 基本运算</span></span><br><span class="line">arr + <span class="number">1</span>  <span class="comment"># [2, 3, 4, 5, 6]</span></span><br><span class="line">arr * <span class="number">2</span>  <span class="comment"># [2, 4, 6, 8, 10]</span></span><br><span class="line"><span class="comment"># 统计运算</span></span><br><span class="line">arr.mean()  <span class="comment"># 平均值</span></span><br><span class="line">arr.<span class="built_in">max</span>()   <span class="comment"># 最大值</span></span><br></pre></td></tr></table></figure></li>
</ul>
<ol start="2" type="1">
<li><strong>Pandas</strong> (pd)</li>
</ol>
<ul>
<li>用于处理表格数据的高级库</li>
<li>提供DataFrame数据结构（类似Excel表格）</li>
<li>常用功能示例： <figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 创建数据表</span></span><br><span class="line">df = pd.DataFrame(&#123;</span><br><span class="line">    <span class="string">&#x27;重量&#x27;</span>: [<span class="number">2.0</span>, <span class="number">2.4</span>, <span class="number">1.5</span>],</span><br><span class="line">    <span class="string">&#x27;里程&#x27;</span>: [<span class="number">18</span>, <span class="number">15</span>, <span class="number">18</span>]</span><br><span class="line">&#125;)</span><br><span class="line"><span class="comment"># 读取CSV文件</span></span><br><span class="line">df = pd.read_csv(<span class="string">&#x27;data.csv&#x27;</span>)</span><br><span class="line"><span class="comment"># 基本统计</span></span><br><span class="line">df.describe()  <span class="comment"># 显示统计信息</span></span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="机器学习库">机器学习库</h5>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> keras  <span class="comment"># 深度学习框架</span></span><br></pre></td></tr></table></figure>
<p><strong>Keras</strong> - 用于构建和训练深度学习模型的高级框架 -
提供简单易用的API - 常用功能示例： <figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 构建简单的神经网络</span></span><br><span class="line">model = keras.Sequential([</span><br><span class="line">    keras.layers.Dense(<span class="number">1</span>, input_shape=[<span class="number">1</span>])</span><br><span class="line">])</span><br><span class="line"><span class="comment"># 编译模型</span></span><br><span class="line">model.<span class="built_in">compile</span>(optimizer=<span class="string">&#x27;sgd&#x27;</span>, loss=<span class="string">&#x27;mse&#x27;</span>)</span><br></pre></td></tr></table></figure></p>
<h5 id="数据可视化库">数据可视化库</h5>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> plotly.express <span class="keyword">as</span> px</span><br><span class="line"><span class="keyword">import</span> plotly.graph_objects <span class="keyword">as</span> go</span><br><span class="line"><span class="keyword">import</span> seaborn <span class="keyword">as</span> sns</span><br></pre></td></tr></table></figure>
<ol type="1">
<li><strong>Plotly</strong> (px, go)</li>
</ol>
<ul>
<li>用于创建交互式图表</li>
<li>支持多种图表类型</li>
<li>常用功能示例： <figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 创建散点图</span></span><br><span class="line">fig = px.scatter(df, x=<span class="string">&#x27;重量&#x27;</span>, y=<span class="string">&#x27;里程&#x27;</span>)</span><br><span class="line"><span class="comment"># 显示图表</span></span><br><span class="line">fig.show()</span><br></pre></td></tr></table></figure></li>
</ul>
<ol start="2" type="1">
<li><strong>Seaborn</strong> (sns)</li>
</ol>
<ul>
<li>基于Matplotlib的统计可视化库</li>
<li>提供美观的默认样式</li>
<li>常用功能示例： <figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 创建散点图</span></span><br><span class="line">sns.scatterplot(data=df, x=<span class="string">&#x27;重量&#x27;</span>, y=<span class="string">&#x27;里程&#x27;</span>)</span><br></pre></td></tr></table></figure></li>
</ul>
<p>在实际项目中，这些库通常会一起使用： 1. 使用Pandas读取和处理数据 2.
用NumPy进行数值计算 3. 使用Keras构建和训练模型 4.
用Plotly或Seaborn可视化结果</p>
<h5 id="加载数据">加载数据</h5>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 从网络加载芝加哥出租车数据集</span></span><br><span class="line">chicago_taxi_dataset = pd.read_csv(<span class="string">&quot;https://download.mlcc.google.com/mledu-datasets/chicago_taxi_train.csv&quot;</span>)</span><br></pre></td></tr></table></figure>
<p>这行代码使用pandas的<code>read_csv()</code>函数从网络下载并读取CSV文件。这个数据集包含了芝加哥出租车的行程信息。</p>
<h5 id="选择特定列">选择特定列</h5>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 选择需要的列创建新的DataFrame</span></span><br><span class="line">training_df = chicago_taxi_dataset[[<span class="string">&#x27;TRIP_MILES&#x27;</span>,    <span class="comment"># 行程里程</span></span><br><span class="line">                                  <span class="string">&#x27;TRIP_SECONDS&#x27;</span>,    <span class="comment"># 行程时间（秒）</span></span><br><span class="line">                                  <span class="string">&#x27;FARE&#x27;</span>,            <span class="comment"># 车费</span></span><br><span class="line">                                  <span class="string">&#x27;COMPANY&#x27;</span>,         <span class="comment"># 出租车公司</span></span><br><span class="line">                                  <span class="string">&#x27;PAYMENT_TYPE&#x27;</span>,    <span class="comment"># 支付方式</span></span><br><span class="line">                                  <span class="string">&#x27;TIP_RATE&#x27;</span>]]       <span class="comment"># 小费率</span></span><br></pre></td></tr></table></figure>
<p>这里我们从原始数据集中选择了6个重要特征：</p>
<ul>
<li>TRIP_MILES：行程距离</li>
<li>TRIP_SECONDS：行程时长</li>
<li>FARE：车费金额</li>
<li>COMPANY：出租车公司</li>
<li>PAYMENT_TYPE：支付方式</li>
<li>TIP_RATE：小费比例</li>
</ul>
<h5 id="查看数据信息">查看数据信息</h5>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 打印数据集基本信息</span></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&#x27;Read dataset completed successfully.&#x27;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&#x27;Total number of rows: &#123;0&#125;\n\n&#x27;</span>.<span class="built_in">format</span>(<span class="built_in">len</span>(training_df.index)))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 显示前200行数据</span></span><br><span class="line">training_df.head(<span class="number">200</span>)</span><br></pre></td></tr></table></figure>
<p>这段代码做了两件事：</p>
<ol type="1">
<li>打印数据集的总行数</li>
<li>显示数据集的前200行，帮助我们了解数据的基本结构</li>
</ol>
<h4 id="part-2---dataset-exploration">Part 2 - Dataset Exploration</h4>
<h5 id="基本统计信息">基本统计信息：</h5>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查看数值列的统计信息</span></span><br><span class="line"><span class="built_in">print</span>(taxi_data.describe())</span><br></pre></td></tr></table></figure>
<p>输出结果类似： <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">       TRIP_MILES        FARE</span><br><span class="line">count         4.0         5.0    # 非空值数量</span><br><span class="line">mean          2.75       13.0    # 平均值</span><br><span class="line">std           1.04        4.7    # 标准差</span><br><span class="line">min           1.50        8.0    # 最小值</span><br><span class="line">25%           2.25       10.0    # 25%分位数</span><br><span class="line">50%           2.75       12.0    # 中位数</span><br><span class="line">75%           3.25       15.0    # 75%分位数</span><br><span class="line">max           4.00       20.0    # 最大值</span><br></pre></td></tr></table></figure></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查看数据类型</span></span><br><span class="line"><span class="built_in">print</span>(taxi_data.info())</span><br></pre></td></tr></table></figure>
<p>输出结果： <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Data columns (total 4 columns):</span><br><span class="line"> #   Column        Non-Null Count  Dtype  </span><br><span class="line">---  ------        --------------  -----  </span><br><span class="line"> 0   TRIP_MILES    4 non-null     float64   # 4条非空数据，浮点型</span><br><span class="line"> 1   FARE          5 non-null     int64     # 5条非空数据，整数型</span><br><span class="line"> 2   PAYMENT_TYPE  5 non-null     object    # 5条非空数据，字符串型</span><br><span class="line"> 3   COMPANY       5 non-null     object    # 5条非空数据，字符串型</span><br></pre></td></tr></table></figure></p>
<h5 id="缺失值检查">缺失值检查：</h5>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 检查缺失值</span></span><br><span class="line"><span class="built_in">print</span>(taxi_data.isnull().<span class="built_in">sum</span>())</span><br></pre></td></tr></table></figure>
<p>输出结果： <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">TRIP_MILES     1    # TRIP_MILES列有1个缺失值</span><br><span class="line">FARE           0    # FARE列没有缺失值</span><br><span class="line">PAYMENT_TYPE   0    # PAYMENT_TYPE列没有缺失值</span><br><span class="line">COMPANY        0    # COMPANY列没有缺失值</span><br></pre></td></tr></table></figure></p>
<h5 id="分类变量分析">分类变量分析：</h5>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 查看支付方式的种类</span></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;支付方式类型:&quot;</span>, taxi_data[<span class="string">&#x27;PAYMENT_TYPE&#x27;</span>].unique())</span><br><span class="line"><span class="comment"># 输出: [&#x27;信用卡&#x27;, &#x27;现金&#x27;, &#x27;支付宝&#x27;]</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 统计每个公司的订单数量</span></span><br><span class="line"><span class="built_in">print</span>(taxi_data[<span class="string">&#x27;COMPANY&#x27;</span>].value_counts())</span><br><span class="line"><span class="comment"># 输出:</span></span><br><span class="line"><span class="comment"># A公司    2</span></span><br><span class="line"><span class="comment"># B公司    2</span></span><br><span class="line"><span class="comment"># C公司    1</span></span><br></pre></td></tr></table></figure>
<h5 id="相关性分析">相关性分析：</h5>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 计算行程里程和车费的相关性</span></span><br><span class="line">correlation = taxi_data[[<span class="string">&#x27;TRIP_MILES&#x27;</span>, <span class="string">&#x27;FARE&#x27;</span>]].corr()</span><br><span class="line"><span class="built_in">print</span>(correlation)</span><br></pre></td></tr></table></figure>
<p>输出结果： <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">            TRIP_MILES      FARE</span><br><span class="line">TRIP_MILES    1.000000   0.850123  # 正相关，说明里程越长，费用越高</span><br><span class="line">FARE          0.850123   1.000000</span><br></pre></td></tr></table></figure></p>
<p>在这一步中，我们将使用相关矩阵（Correlation
Matrix）来识别与标签值相关性良好的特征。相关值具有以下含义： 1.
完全正相关（Perfect Positive Correlation）：1.0 -
定义：当一个属性（attribute）上升时，另一个属性也上升 -
例如：学习时间与考试成绩的关系 2. 完全负相关（Perfect Negative
Correlation）：-1.0 - 定义：当一个属性上升时，另一个属性下降 -
例如：商品价格与销售量的关系 3. 无相关性（No Correlation）：0.0 -
定义：两列之间不存在线性关系（Linear Relationship） -
例如：学生的身高与考试成绩的关系</p>
<p>一般来说，相关值（Correlation
Value）的绝对值越大，该特征（Feature）的预测能力就越强</p>
<p>使用pairplot进行多变量关系可视化，这段代码将创建一个3x3的图表矩阵：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">sns.pairplot(training_df, </span><br><span class="line">             x_vars=[<span class="string">&quot;FARE&quot;</span>, <span class="string">&quot;TRIP_MILES&quot;</span>, <span class="string">&quot;TRIP_SECONDS&quot;</span>],</span><br><span class="line">             y_vars=[<span class="string">&quot;FARE&quot;</span>, <span class="string">&quot;TRIP_MILES&quot;</span>, <span class="string">&quot;TRIP_SECONDS&quot;</span>])</span><br></pre></td></tr></table></figure>
<p><img src="/assets/机器学习入门13.png" /></p>
<h4 id="part-3---train-model">Part 3 - Train Model</h4>
<p>在这部分，我们将实现一组用于模型训练和可视化的函数。这些函数可以帮助我们可视化训练过程、展示模型预测结果和分析模型性能。</p>
<h3 id="可视化函数实现">1. 可视化函数实现</h3>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">make_plots</span>(<span class="params">df, feature_names, label_name, model_output, sample_size=<span class="number">200</span></span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;主函数：创建模型训练的可视化图表</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    功能：</span></span><br><span class="line"><span class="string">    - 创建包含损失曲线和模型预测的双图表</span></span><br><span class="line"><span class="string">    - 支持1D和2D特征的可视化</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    参数：</span></span><br><span class="line"><span class="string">        df: DataFrame，训练数据</span></span><br><span class="line"><span class="string">        feature_names: list，特征名列表</span></span><br><span class="line"><span class="string">        label_name: str，标签名</span></span><br><span class="line"><span class="string">        model_output: tuple，模型输出(weights, bias, epochs, rmse)</span></span><br><span class="line"><span class="string">        sample_size: int，采样大小，默认200</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    <span class="comment"># 获取随机样本</span></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 确定是2D还是3D图</span></span><br><span class="line">    is_2d_plot = <span class="built_in">len</span>(feature_names) == <span class="number">1</span></span><br><span class="line">    model_plot_type = <span class="string">&quot;scatter&quot;</span> <span class="keyword">if</span> is_2d_plot <span class="keyword">else</span> <span class="string">&quot;surface&quot;</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 创建子图</span></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 绘制数据和模型</span></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">plot_loss_curve</span>(<span class="params">epochs, rmse, fig</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;绘制损失曲线</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    功能：</span></span><br><span class="line"><span class="string">    - 展示训练过程中的损失变化</span></span><br><span class="line"><span class="string">    - 使用红色线条表示</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    参数：</span></span><br><span class="line"><span class="string">        epochs: array，训练轮数</span></span><br><span class="line"><span class="string">        rmse: array，均方根误差</span></span><br><span class="line"><span class="string">        fig: Figure，plotly图表对象</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">plot_data</span>(<span class="params">df, features, label, fig</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;绘制原始数据散点图</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    功能：</span></span><br><span class="line"><span class="string">    - 支持1D特征的2D散点图</span></span><br><span class="line"><span class="string">    - 支持2D特征的3D散点图</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    参数：</span></span><br><span class="line"><span class="string">        df: DataFrame，数据</span></span><br><span class="line"><span class="string">        features: list，特征名列表</span></span><br><span class="line"><span class="string">        label: str，标签名</span></span><br><span class="line"><span class="string">        fig: Figure，plotly图表对象</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">plot_model</span>(<span class="params">df, features, weights, bias, fig</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;绘制模型预测结果</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    功能：</span></span><br><span class="line"><span class="string">    - 1D特征：显示预测线</span></span><br><span class="line"><span class="string">    - 2D特征：显示预测平面</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    参数：</span></span><br><span class="line"><span class="string">        df: DataFrame，数据</span></span><br><span class="line"><span class="string">        features: list，特征名列表</span></span><br><span class="line"><span class="string">        weights: array，特征权重</span></span><br><span class="line"><span class="string">        bias: float，偏置值</span></span><br><span class="line"><span class="string">        fig: Figure，plotly图表对象</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    <span class="comment"># 计算预测值</span></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 绘制预测线或预测面</span></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">model_info</span>(<span class="params">feature_names, label_name, model_output</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;生成模型信息摘要</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    功能：</span></span><br><span class="line"><span class="string">    - 显示特征权重</span></span><br><span class="line"><span class="string">    - 显示偏置值</span></span><br><span class="line"><span class="string">    - 生成预测方程</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    参数：</span></span><br><span class="line"><span class="string">        feature_names: list，特征名列表</span></span><br><span class="line"><span class="string">        label_name: str，标签名</span></span><br><span class="line"><span class="string">        model_output: tuple，模型输出(weights, bias, epochs, rmse)</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    返回：</span></span><br><span class="line"><span class="string">        str，格式化的模型信息字符串</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    <span class="comment"># 生成模型方程</span></span><br><span class="line">    ...</span><br></pre></td></tr></table></figure>
<h5 id="模型训练函数实现">模型训练函数实现</h5>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">build_model</span>(<span class="params">my_learning_rate, num_features</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;创建并编译简单的线性回归模型</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    功能：</span></span><br><span class="line"><span class="string">    - 创建具有单个Dense层的Keras模型</span></span><br><span class="line"><span class="string">    - 配置优化器和损失函数</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    参数：</span></span><br><span class="line"><span class="string">        my_learning_rate: float，学习率</span></span><br><span class="line"><span class="string">        num_features: int，特征数量</span></span><br><span class="line"><span class="string">        </span></span><br><span class="line"><span class="string">    返回：</span></span><br><span class="line"><span class="string">        model: keras.Model，编译好的模型</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    <span class="comment"># 描述模型结构</span></span><br><span class="line">    <span class="comment"># 简单线性回归模型由单层单节点组成</span></span><br><span class="line">    inputs = keras.Input(shape=(num_features,))</span><br><span class="line">    outputs = keras.layers.Dense(units=<span class="number">1</span>)(inputs)</span><br><span class="line">    model = keras.Model(inputs=inputs, outputs=outputs)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 编译模型，配置优化器和损失函数</span></span><br><span class="line">    model.<span class="built_in">compile</span>(optimizer=keras.optimizers.RMSprop(learning_rate=my_learning_rate),</span><br><span class="line">                 loss=<span class="string">&quot;mean_squared_error&quot;</span>,</span><br><span class="line">                 metrics=[keras.metrics.RootMeanSquaredError()])</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> model</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">train_model</span>(<span class="params">model, df, features, label, epochs, batch_size</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;训练模型</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    功能：</span></span><br><span class="line"><span class="string">    - 使用数据训练模型</span></span><br><span class="line"><span class="string">    - 获取训练后的权重和偏置</span></span><br><span class="line"><span class="string">    - 记录训练过程中的误差</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    参数：</span></span><br><span class="line"><span class="string">        model: keras.Model，待训练的模型</span></span><br><span class="line"><span class="string">        df: DataFrame，训练数据</span></span><br><span class="line"><span class="string">        features: array，特征数据</span></span><br><span class="line"><span class="string">        label: array，标签数据</span></span><br><span class="line"><span class="string">        epochs: int，训练轮数</span></span><br><span class="line"><span class="string">        batch_size: int，批次大小</span></span><br><span class="line"><span class="string">        </span></span><br><span class="line"><span class="string">    返回：</span></span><br><span class="line"><span class="string">        tuple，包含(trained_weight, trained_bias, epochs, rmse)</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    <span class="comment"># 训练模型</span></span><br><span class="line">    history = model.fit(x=features,</span><br><span class="line">                       y=label,</span><br><span class="line">                       batch_size=batch_size,</span><br><span class="line">                       epochs=epochs)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 获取训练后的权重和偏置</span></span><br><span class="line">    trained_weight = model.get_weights()[<span class="number">0</span>]</span><br><span class="line">    trained_bias = model.get_weights()[<span class="number">1</span>]</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 获取训练历史</span></span><br><span class="line">    epochs = history.epoch</span><br><span class="line">    hist = pd.DataFrame(history.history)</span><br><span class="line">    rmse = hist[<span class="string">&quot;root_mean_squared_error&quot;</span>]</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> trained_weight, trained_bias, epochs, rmse</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">run_experiment</span>(<span class="params">df, feature_names, label_name, learning_rate, epochs, batch_size</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;运行完整的训练实验</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    功能：</span></span><br><span class="line"><span class="string">    - 准备训练数据</span></span><br><span class="line"><span class="string">    - 构建并训练模型</span></span><br><span class="line"><span class="string">    - 可视化训练结果</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    参数：</span></span><br><span class="line"><span class="string">        df: DataFrame，训练数据</span></span><br><span class="line"><span class="string">        feature_names: list，特征名列表</span></span><br><span class="line"><span class="string">        label_name: str，标签名</span></span><br><span class="line"><span class="string">        learning_rate: float，学习率</span></span><br><span class="line"><span class="string">        epochs: int，训练轮数</span></span><br><span class="line"><span class="string">        batch_size: int，批次大小</span></span><br><span class="line"><span class="string">        </span></span><br><span class="line"><span class="string">    返回：</span></span><br><span class="line"><span class="string">        model: keras.Model，训练好的模型</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;INFO: starting training experiment with features=&#123;&#125; and label=&#123;&#125;\n&#x27;</span>.<span class="built_in">format</span>(</span><br><span class="line">        feature_names, label_name))</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 准备数据</span></span><br><span class="line">    num_features = <span class="built_in">len</span>(feature_names)</span><br><span class="line">    features = df.loc[:, feature_names].values</span><br><span class="line">    label = df[label_name].values</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 构建并训练模型</span></span><br><span class="line">    model = build_model(learning_rate, num_features)</span><br><span class="line">    model_output = train_model(model, df, features, label, epochs, batch_size)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 输出结果</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;\nSUCCESS: training experiment complete\n&#x27;</span>)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;&#123;&#125;&#x27;</span>.<span class="built_in">format</span>(model_info(feature_names, label_name, model_output)))</span><br><span class="line">    make_plots(df, feature_names, label_name, model_output)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> model</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;SUCCESS: defining linear regression functions complete.&quot;</span>)</span><br></pre></td></tr></table></figure>
<h5 id="运行训练实验">运行训练实验</h5>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 设置超参数</span></span><br><span class="line">learning_rate = <span class="number">0.001</span>  <span class="comment"># 学习率</span></span><br><span class="line">epochs = <span class="number">20</span>           <span class="comment"># 训练轮数</span></span><br><span class="line">batch_size = <span class="number">50</span>      <span class="comment"># 批次大小</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 数据预处理 - 将秒转换为分钟</span></span><br><span class="line">training_df.loc[:, <span class="string">&#x27;TRIP_MINUTES&#x27;</span>] = training_df[<span class="string">&#x27;TRIP_SECONDS&#x27;</span>]/<span class="number">60</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 定义特征和标签</span></span><br><span class="line">features = [<span class="string">&#x27;TRIP_MILES&#x27;</span>, <span class="string">&#x27;TRIP_MINUTES&#x27;</span>]  <span class="comment"># 使用里程和时间作为特征</span></span><br><span class="line">label = <span class="string">&#x27;FARE&#x27;</span>                             <span class="comment"># 预测出租车费用</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 运行训练实验</span></span><br><span class="line">model_2 = run_experiment(training_df, features, label, learning_rate, epochs, batch_size)</span><br></pre></td></tr></table></figure>
<h4 id="part-4---validate-model">Part 4 - Validate Model</h4>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">format_currency</span>(<span class="params">x</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;格式化金额为美元形式</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    参数：</span></span><br><span class="line"><span class="string">        x: float，待格式化的金额</span></span><br><span class="line"><span class="string">        </span></span><br><span class="line"><span class="string">    返回：</span></span><br><span class="line"><span class="string">        str，格式化后的金额字符串，如&quot;$12.34&quot;</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;$&#123;:.2f&#125;&quot;</span>.<span class="built_in">format</span>(x)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">build_batch</span>(<span class="params">df, batch_size</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;构建预测用的数据批次</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    功能：</span></span><br><span class="line"><span class="string">    - 从数据集中随机抽样</span></span><br><span class="line"><span class="string">    - 重置索引</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    参数：</span></span><br><span class="line"><span class="string">        df: DataFrame，源数据集</span></span><br><span class="line"><span class="string">        batch_size: int，批次大小</span></span><br><span class="line"><span class="string">        </span></span><br><span class="line"><span class="string">    返回：</span></span><br><span class="line"><span class="string">        DataFrame，构建好的批次数据</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    batch = df.sample(n=batch_size).copy()</span><br><span class="line">    batch.set_index(np.arange(batch_size), inplace=<span class="literal">True</span>)</span><br><span class="line">    <span class="keyword">return</span> batch</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">predict_fare</span>(<span class="params">model, df, features, label, batch_size=<span class="number">50</span></span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;使用模型预测车费</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    功能：</span></span><br><span class="line"><span class="string">    - 构建预测批次</span></span><br><span class="line"><span class="string">    - 进行批量预测</span></span><br><span class="line"><span class="string">    - 计算预测误差</span></span><br><span class="line"><span class="string">    - 整理预测结果</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    参数：</span></span><br><span class="line"><span class="string">        model: keras.Model，训练好的模型</span></span><br><span class="line"><span class="string">        df: DataFrame，预测数据</span></span><br><span class="line"><span class="string">        features: list，特征名列表</span></span><br><span class="line"><span class="string">        label: str，标签名</span></span><br><span class="line"><span class="string">        batch_size: int，批次大小，默认50</span></span><br><span class="line"><span class="string">        </span></span><br><span class="line"><span class="string">    返回：</span></span><br><span class="line"><span class="string">        DataFrame，包含预测结果的数据框</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    batch = build_batch(df, batch_size)</span><br><span class="line">    predicted_values = model.predict_on_batch(x=batch.loc[:, features].values)</span><br><span class="line"></span><br><span class="line">    data = &#123;<span class="string">&quot;PREDICTED_FARE&quot;</span>: [], <span class="string">&quot;OBSERVED_FARE&quot;</span>: [], <span class="string">&quot;L1_LOSS&quot;</span>: [],</span><br><span class="line">            features[<span class="number">0</span>]: [], features[<span class="number">1</span>]: []&#125;</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(batch_size):</span><br><span class="line">        predicted = predicted_values[i][<span class="number">0</span>]</span><br><span class="line">        observed = batch.at[i, label]</span><br><span class="line">        data[<span class="string">&quot;PREDICTED_FARE&quot;</span>].append(format_currency(predicted))</span><br><span class="line">        data[<span class="string">&quot;OBSERVED_FARE&quot;</span>].append(format_currency(observed))</span><br><span class="line">        data[<span class="string">&quot;L1_LOSS&quot;</span>].append(format_currency(<span class="built_in">abs</span>(observed - predicted)))</span><br><span class="line">        data[features[<span class="number">0</span>]].append(batch.at[i, features[<span class="number">0</span>]])</span><br><span class="line">        data[features[<span class="number">1</span>]].append(<span class="string">&quot;&#123;:.2f&#125;&quot;</span>.<span class="built_in">format</span>(batch.at[i, features[<span class="number">1</span>]]))</span><br><span class="line"></span><br><span class="line">    output_df = pd.DataFrame(data)</span><br><span class="line">    <span class="keyword">return</span> output_df</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">show_predictions</span>(<span class="params">output</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;展示预测结果</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    功能：</span></span><br><span class="line"><span class="string">    - 添加表头装饰</span></span><br><span class="line"><span class="string">    - 打印预测结果表格</span></span><br><span class="line"><span class="string">    </span></span><br><span class="line"><span class="string">    参数：</span></span><br><span class="line"><span class="string">        output: DataFrame，预测结果数据</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    header = <span class="string">&quot;-&quot;</span> * <span class="number">80</span></span><br><span class="line">    banner = header + <span class="string">&quot;\n&quot;</span> + <span class="string">&quot;|&quot;</span> + <span class="string">&quot;PREDICTIONS&quot;</span>.center(<span class="number">78</span>) + <span class="string">&quot;|&quot;</span> + <span class="string">&quot;\n&quot;</span> + header</span><br><span class="line">    <span class="built_in">print</span>(banner)</span><br><span class="line">    <span class="built_in">print</span>(output)</span><br><span class="line">    <span class="keyword">return</span></span><br></pre></td></tr></table></figure>
<h5 id="执行模型预测">执行模型预测</h5>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 使用训练好的模型进行预测</span></span><br><span class="line">output = predict_fare(model_2, training_df, features, label)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 展示预测结果</span></span><br><span class="line">show_predictions(output)</span><br></pre></td></tr></table></figure>
<p>以上是线性回归的基础内容，后续的内容请看下一篇博客。</p>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>机器学习</tag>
      </tags>
  </entry>
  <entry>
    <title>机器学习入门03</title>
    <url>/posts/990b8775/</url>
    <content><![CDATA[<p>继续进行机器学习的入门，主题是了解现有的机器学习库。 <span id="more"></span></p>
<p>在开始机器学习项目之前，了解现有的工具库非常重要。不同的库有其特点和适用场景，选择合适的工具可以大大提高开发效率。本文将介绍当前主流的机器学习库，分析它们的优缺点，帮助大家选择合适的工具。</p>
<h2 id="基础机器学习库">基础机器学习库</h2>
<h3 id="scikit-learn">scikit-learn</h3>
<p>最受欢迎的机器学习库之一，特别适合入门学习：</p>
<ul>
<li>优点：
<ul>
<li>API设计简洁统一</li>
<li>文档详尽,示例丰富</li>
<li>包含大量经典机器学习算法</li>
<li>适合入门学习</li>
</ul></li>
<li>缺点：
<ul>
<li>不支持GPU加速</li>
<li>不适合深度学习</li>
<li>处理大规模数据性能较差</li>
</ul></li>
</ul>
<h3 id="xgboost">XGBoost</h3>
<p>著名的梯度提升库，在结构化数据竞赛中表现出色：</p>
<ul>
<li>优点：
<ul>
<li>梯度提升算法效果出色</li>
<li>支持分布式计算</li>
<li>运行速度快</li>
</ul></li>
<li>缺点：
<ul>
<li>功能相对单一</li>
<li>参数调优复杂</li>
</ul></li>
</ul>
<h3 id="lightgbm">LightGBM</h3>
<p>微软开发的高效梯度提升库：</p>
<ul>
<li>优点：
<ul>
<li>训练速度快</li>
<li>内存占用小</li>
<li>支持大规模数据</li>
</ul></li>
<li>缺点：
<ul>
<li>小数据集效果不如XGBoost</li>
<li>对参数较敏感</li>
</ul></li>
</ul>
<h3 id="keras">Keras</h3>
<p>高层深度学习API，特别适合入门：</p>
<ul>
<li>优点：
<ul>
<li>API设计简洁优雅</li>
<li>快速搭建模型</li>
<li>文档友好，学习曲线平缓</li>
<li>与TensorFlow深度集成</li>
</ul></li>
<li>缺点：
<ul>
<li>灵活性不如底层框架</li>
<li>高度定制化时受限</li>
<li>某些高级功能需要直接使用TensorFlow</li>
</ul></li>
</ul>
<h2 id="深度学习框架">深度学习框架</h2>
<h3 id="tensorflow">TensorFlow</h3>
<p>Google开发的深度学习框架，在工业界应用广泛：</p>
<ul>
<li>优点：
<ul>
<li>完整的深度学习生态</li>
<li>部署便捷(TF Serving)</li>
<li>可视化工具强大(TensorBoard)</li>
</ul></li>
<li>缺点：
<ul>
<li>学习曲线陡峭</li>
<li>API变动频繁</li>
<li>调试相对困难</li>
</ul></li>
</ul>
<h3 id="pytorch">PyTorch</h3>
<p>Facebook开发的深度学习框架，在学术界备受欢迎：</p>
<ul>
<li>优点：
<ul>
<li>语法直观pythonic</li>
<li>动态计算图灵活</li>
<li>调试方便</li>
<li>学术界广泛使用</li>
</ul></li>
<li>缺点：
<ul>
<li>部署相对复杂</li>
<li>生态不如TensorFlow完善</li>
</ul></li>
</ul>
<h2 id="数据处理库">数据处理库</h2>
<h3 id="numpy">NumPy</h3>
<p>科学计算的基础库：</p>
<ul>
<li>优点：
<ul>
<li>数组运算高效</li>
<li>是其他库的基础</li>
</ul></li>
<li>缺点：
<ul>
<li>功能相对基础</li>
</ul></li>
</ul>
<h3 id="pandas">Pandas</h3>
<p>数据分析和处理的利器：</p>
<ul>
<li>优点：
<ul>
<li>数据处理功能强大</li>
<li>适合表格数据</li>
</ul></li>
<li>缺点：
<ul>
<li>内存占用较大</li>
<li>处理超大数据集效率低</li>
</ul></li>
</ul>
<h2 id="如何选择">如何选择</h2>
<p>根据不同场景，以下是一些选择建议：</p>
<ol type="1">
<li>入门学习推荐使用 scikit-learn
<ul>
<li>API简单易用</li>
<li>文档完善</li>
<li>包含主流算法</li>
</ul></li>
<li>深度学习入门推荐 Keras
<ul>
<li>容易上手</li>
<li>快速实现想法</li>
<li>TensorFlow的良好支持</li>
</ul></li>
<li>结构化数据任务推荐 XGBoost/LightGBM
<ul>
<li>竞赛首选</li>
<li>处理表格数据效果好</li>
</ul></li>
<li>深度学习进阶：
<ul>
<li>工业界部署选择 TensorFlow</li>
<li>研究/原型开发选择 PyTorch</li>
<li>快速验证想法使用 Keras</li>
</ul></li>
<li>数据预处理必备 Pandas + NumPy
<ul>
<li>几乎所有项目都需要</li>
<li>数据处理的基础工具</li>
</ul></li>
</ol>
<p>最后建议：</p>
<ol type="1">
<li>初学者路线：scikit-learn -&gt; Keras -&gt; TensorFlow/PyTorch</li>
<li>根据具体项目需求和个人偏好选择合适的工具</li>
<li>掌握一个基础机器学习库(scikit-learn)和一个深度学习框架是很好的组合</li>
</ol>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>机器学习</tag>
      </tags>
  </entry>
  <entry>
    <title>机器学习入门04</title>
    <url>/posts/76f12d6/</url>
    <content><![CDATA[<p>今天继续进行机器学习的入门，主题是逻辑回归。 <span id="more"></span></p>
<h2 id="逻辑回归-logistic-regression">逻辑回归 (Logistic
Regression)</h2>
<p>逻辑回归模型旨在预测给定结果的概率。许多问题需要将概率估算值(Probability
Estimation)作为输出，逻辑回归是一种极其高效的概率计算机制。</p>
<h3 id="sigmoid函数-sigmoid-function">Sigmoid函数 (Sigmoid
Function)</h3>
<p>Sigmoid函数(也称为S型函数, S-shaped
Function)是逻辑回归中的核心函数，其数学表达式为：</p>
<p><span class="math display">\[ f(x) = \frac{1}{1 + e^{-x}}
\]</span></p>
<p><img src="/assets/机器学习入门13.png" /></p>
<p>Sigmoid函数具有以下重要特征(Features)：</p>
<ol type="1">
<li>输出范围(Output
Range)：函数值始终在0到1之间，这使其非常适合表示概率</li>
<li>S形曲线(S-shaped Curve)：函数呈现出特征性的S形曲线</li>
<li>渐近线(Asymptote)：
<ul>
<li>当x趋向正无穷时，f(x)趋近于1</li>
<li>当x趋向负无穷时，f(x)趋近于0</li>
</ul></li>
<li>对称性(Symmetry)：函数关于点(0, 0.5)对称</li>
</ol>
<h3
id="使用sigmoid函数转换线性输出-transform-linear-output">使用Sigmoid函数转换线性输出
(Transform Linear Output)</h3>
<p>逻辑回归模型首先计算线性组合(Linear Combination)：</p>
<p><span class="math display">\[ z = b + w_1x_1 + w_2x_2 + ... + w_Nx_N
\]</span></p>
<p>其中：</p>
<ul>
<li>z 是线性方程的输出 (也称为对数几率, Log-odds)</li>
<li>b 是偏差项 (Bias)</li>
<li>w 的值是模型学习的权重 (Weights)</li>
<li>x 的值是特征样本的特征值 (Feature Values)</li>
</ul>
<p><img src="/assets/机器学习入门14.png" /></p>
<p>要获得逻辑回归的预测结果，需要将 z
值传递给Sigmoid函数，将得到一个介于0到1之间的值（概率,
Probability）：</p>
<p><span class="math display">\[ y&#39; = \frac{1}{1 + e^{-z}}
\]</span></p>
<p>其中：</p>
<ul>
<li>y' 是逻辑回归模型的输出 (Model Output)</li>
<li>z 是线性输出（按上述等式计算得出）</li>
</ul>
<p>逻辑回归 模型的训练过程与 线性回归 两个关键区别：</p>
<ol type="1">
<li>使用 对数损失函数 作为损失函数 而不是平方损失函数。</li>
<li>应用正则化 是防止 过拟合。</li>
</ol>
<h3 id="对数损失函数-log-loss-function">对数损失函数 (Log Loss
Function)</h3>
<p>逻辑回归模型的训练过程与线性回归有两个关键区别：</p>
<ol type="1">
<li>使用对数损失函数作为损失函数，而不是平方损失函数。</li>
<li>应用正则化防止过拟合。</li>
</ol>
<h4 id="为什么不使用平方损失函数">为什么不使用平方损失函数？</h4>
<p>在线性回归中，我们使用平方损失函数(L2 Loss
Function)作为损失函数。但对于逻辑回归，平方损失函数并不适合，原因如下：</p>
<ol type="1">
<li>非线性输出：由于Sigmoid函数的非线性特性，当对数几率(z)接近0时，输出(y')的变化幅度比z的变化更大</li>
<li>精度问题：下表展示了当输入值从5到10时，输出值和所需精度的变化：</li>
</ol>
<table>
<thead>
<tr>
<th>输入(Input)</th>
<th>逻辑输出(Output)</th>
<th>所需精度位数(Precision)</th>
</tr>
</thead>
<tbody>
<tr>
<td>5</td>
<td>0.993</td>
<td>3</td>
</tr>
<tr>
<td>6</td>
<td>0.997</td>
<td>3</td>
</tr>
<tr>
<td>7</td>
<td>0.999</td>
<td>3</td>
</tr>
<tr>
<td>8</td>
<td>0.9997</td>
<td>4</td>
</tr>
<tr>
<td>9</td>
<td>0.9999</td>
<td>4</td>
</tr>
<tr>
<td>10</td>
<td>0.99998</td>
<td>5</td>
</tr>
</tbody>
</table>
<p>如果使用平方损失来计算Sigmoid函数的误差，当输出接近0和1时，需要更多的精度位数来保留这些差异。</p>
<h4 id="对数损失函数定义">对数损失函数定义</h4>
<p>对数损失函数(Log Loss
Function)通过对数形式来返回变化梯度的对数，而不仅是从数值到预测的距离。其计算公式为：</p>
<p><span class="math display">\[ \text{Log Loss} = \sum_{(x,y) \in D}
-y\log(y&#39;) - (1-y)\log(1-y&#39;) \]</span></p>
<p>其中：</p>
<ul>
<li><span class="math inline">\((x,y) \in D\)</span>
是包含多个有标签样本的数据集，这些样本为<span
class="math inline">\((x,y)\)</span>对</li>
<li><span class="math inline">\(y\)</span>
是有标签样本中的标签，由于这是逻辑回归，<span
class="math inline">\(y\)</span>的每个值都必须为0或1</li>
<li><span class="math inline">\(y&#39;\)</span>
是模型的预测结果（介于0和1之间），给定集合<span
class="math inline">\(x\)</span>中的功能</li>
</ul>
<h3
id="逻辑回归中的正则化-regularization-in-logistic-regression">逻辑回归中的正则化
(Regularization in Logistic Regression)</h3>
<h4 id="什么是正则化">什么是正则化？</h4>
<p>正则化(Regularization)是用来降低模型复杂度的技术。在逻辑回归研究中，如果没有正则化，逻辑回归的渐近性质会导致全局损失接近0，容易造成过拟合(Overfitting)。</p>
<h4 id="常用的正则化方法">常用的正则化方法</h4>
<ol type="1">
<li>L2正则化 (L2 Regularization)
<ul>
<li>也称为权重衰减(Weight Decay)</li>
<li>通过在损失函数中添加所有特征权重的平方和来实现</li>
<li>倾向于让权重变得更小，但不会变成0</li>
</ul></li>
<li>早停法 (Early Stopping)
<ul>
<li>通过限制训练步数来控制模型复杂度</li>
<li>在损失曲线趋于平缓时停止训练</li>
<li>可以防止模型过度拟合训练数据</li>
<li>需要使用验证集来确定最佳停止时机</li>
</ul></li>
</ol>
<p>以上是逻辑回归的基础内容，后续的内容请看下一篇博客。</p>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>机器学习</tag>
      </tags>
  </entry>
  <entry>
    <title>机器学习入门05</title>
    <url>/posts/70682240/</url>
    <content><![CDATA[<p>今天继续进行机器学习的入门，主题是分类。 <span id="more"></span></p>
<h2 id="分类-classification">分类 (Classification)</h2>
<p>分类是机器学习中的一种重要任务，其目标是根据已知样本的某些特征，判断一个新的样本属于哪种已知的样本类。</p>
<h3 id="阈值threshold和分类决策">阈值(Threshold)和分类决策</h3>
<p>在二分类问题中，分类模型通常会输出一个0到1之间的预测值，表示样本属于某个类别的概率。例如，在垃圾邮件分类中，预测值0.75表示该邮件有75%的可能性是垃圾邮件。为了将这个概率值转换为具体的分类结果（垃圾邮件或非垃圾邮件），我们需要设定一个阈值（threshold）。</p>
<p>当我们设定阈值后：</p>
<ul>
<li>如果预测概率大于阈值，样本会被分类为正类（如垃圾邮件）</li>
<li>如果预测概率小于阈值，样本会被分类为负类（如正常邮件）</li>
</ul>
<h4 id="阈值threshold的选择">阈值(Threshold)的选择</h4>
<p>阈值的选择非常重要，会直接影响分类结果。以垃圾邮件分类为例：</p>
<ul>
<li>如果模型对某封邮件的预测值为0.99，表示它有99%的可能性是垃圾邮件</li>
<li>如果另一封邮件的预测值为0.51，表示它有51%的可能性是垃圾邮件</li>
<li>如果我们将阈值设为0.5，这两封邮件都会被分类为垃圾邮件</li>
<li>但如果将阈值设为0.95，则只有第一封邮件会被分类为垃圾邮件</li>
</ul>
<p>虽然0.5看起来是一个直观的选择，但在实际应用中，我们需要考虑：</p>
<ol type="1">
<li>不同类别样本的分布是否均衡</li>
<li>错误分类的代价是否相同</li>
<li>业务需求的具体要求</li>
</ol>
<p>例如，如果将正常邮件错误地归类为垃圾邮件的代价远高于将垃圾邮件错误地归类为正常邮件，我们可能需要设置一个较高的阈值，以减少误判正常邮件的情况。</p>
<h3 id="混淆矩阵confusion-matrix">混淆矩阵(Confusion Matrix)</h3>
<p>在评估分类模型的性能时,仅仅知道预测概率是不够的,我们还需要了解模型预测的具体情况。混淆矩阵(Confusion
Matrix)是一个非常有用的工具,它可以帮助我们详细了解分类器的预测结果。</p>
<p>混淆矩阵可以用下表表示:</p>
<table>
<colgroup>
<col style="width: 35%" />
<col style="width: 32%" />
<col style="width: 32%" />
</colgroup>
<thead>
<tr>
<th></th>
<th>实际正例</th>
<th>实际负例</th>
</tr>
</thead>
<tbody>
<tr>
<td>预测为正</td>
<td>真正例(TP):
被正确分类为垃圾邮件的垃圾邮件。这些是系统自动发送到"垃圾邮件"文件夹的垃圾邮件。</td>
<td>假正例(FP):
非垃圾邮件被误分类为垃圾邮件。这些是最终进入"垃圾邮件"文件夹的正常电子邮件。</td>
</tr>
<tr>
<td>预测为负</td>
<td>假负例(FN):
垃圾邮件被错误分类为非垃圾邮件。这些是垃圾邮件过滤器未捕获的垃圾邮件，并已进入收件箱。</td>
<td>真负例(TN):
非垃圾邮件被正确分类为非垃圾邮件。这些是直接发送到收件箱的正常电子邮件。</td>
</tr>
</tbody>
</table>
<p>在二分类问题中,混淆矩阵包含四种可能的预测结果:</p>
<ol type="1">
<li>真正例(True Positive, TP): 正确地将正类样本预测为正类
<ul>
<li>例如:正确地将垃圾邮件识别为垃圾邮件</li>
</ul></li>
<li>假正例(False Positive, FP): 错误地将负类样本预测为正类
<ul>
<li>例如:错误地将正常邮件判定为垃圾邮件</li>
</ul></li>
<li>假负例(False Negative, FN): 错误地将正类样本预测为负类
<ul>
<li>例如:错误地将垃圾邮件判定为正常邮件</li>
</ul></li>
<li>真负例(True Negative, TN): 正确地将负类样本预测为负类
<ul>
<li>例如:正确地将正常邮件识别为正常邮件</li>
</ul></li>
</ol>
<p>混淆矩阵的一个重要特点是:</p>
<ul>
<li>每一行的总和(TP + FP 和 FN + TN)代表模型预测的结果分布</li>
<li>每一列的总和(TP + FN 和 FP + TN)代表实际的类别分布</li>
</ul>
<h4 id="数据不平衡问题">数据不平衡问题</h4>
<p>在实际应用中,我们经常会遇到数据不平衡的情况,即不同类别的样本数量差异很大。例如,在垃圾邮件分类中,正常邮件可能远多于垃圾邮件。这种不平衡会影响模型的性能评估和训练效果。</p>
<p>如果实际正例的总数与实际负例的总数相差较大,我们称之为数据不平衡。这种情况下,仅仅依靠准确率可能会产生误导。
例如,在一个数据集中,如果只有1%的邮件是垃圾邮件,那么一个简单地将所有邮件都判定为正常邮件的模型也能获得99%的准确率,但这显然不是一个好的模型。</p>
<h3 id="分类评估指标">分类评估指标</h3>
<p>在分类问题中,我们需要一些指标来评估模型的性能。这些指标基于混淆矩阵中的不同组成部分计算得出,并且会随着阈值的设置而变化。通常,用户会根据具体任务调整阈值来优化某个指标。</p>
<h4 id="准确率accuracy">准确率(Accuracy)</h4>
<p>准确率是最基本的分类评估指标,它表示所有分类(无论是正类还是负类)中正确分类的比例。其数学定义为:</p>
<p><span class="math display">\[
Accuracy = \frac{correct\ classifications}{total\ classifications} =
\frac{TP + TN}{TP + TN + FP + FN}
\]</span></p>
<p>在垃圾邮件分类示例中,准确率衡量的是所有电子邮件正确分类所占的比例。</p>
<p>一个完美的模型没有假正例和假负例,因此准确率为1.0,即100%。</p>
<p>由于准确率包含混淆矩阵中的所有四种结果(TP、FP、TN、FN),因此在<strong>数据集平衡且各个类别中的示例数量相近</strong>的情况下,准确率可以用作衡量模型质量的相对指标。因此,它通常是许多通用或未指定任务的通用或未指定模型使用的默认评估指标。</p>
<p>然而,如果数据集不平衡,或者一种错误(假负例或假正例)的代价高于另一种错误(大多数实际应用中都是如此),则最好改为对其他指标进行优化。</p>
<p>例如,对于严重不均衡的数据集(其中一个类别出现的频率非常低,例如1%),如果模型100%都预测为负类,则准确率得分为99%,尽管该模型毫无用处。</p>
<blockquote>
<p>注意：在机器学习(ML)中,recall、precision和accuracy等字词的数学定义可能与这些字词的常用含义不同,或更具体。</p>
</blockquote>
<h4 id="召回率或真正例率recall-or-tpr">召回率或真正例率(Recall or
TPR)</h4>
<p>召回率(Recall)，也称为真正例率(True Positive Rate,
TPR)，表示在所有实际正例中被正确分类为正例的比例。其数学定义为：</p>
<p><span class="math display">\[
Recall\ (TPR) = \frac{correctly\ classified\ actual\ positives}{all\
actual\ positives} = \frac{TP}{TP + FN}
\]</span></p>
<p>在垃圾邮件分类示例中，召回率衡量的是被正确分类为垃圾邮件的垃圾邮件的比例。因此，召回率的另一个名称是检测概率：它回答了"此模型检测到垃圾邮件的比例是多少？"这一问题。</p>
<p>一个完美的模型，其假负例率为0，因此召回率(TPR)为1.0，也就是说，检测率为100%。</p>
<p>在实际正例数量非常少（例如总共1-2个样本）的不平衡数据集中，召回率作为指标的意义不大，作用不大。</p>
<h4 id="假正例率fpr">假正例率(FPR)</h4>
<p>假正例率(False Positive Rate,
FPR)是指被错误分类为正例的所有实际负例所占的比例，也称为误报概率。其数学定义为：</p>
<p><span class="math display">\[
FPR = \frac{incorrectly\ classified\ actual\ negatives}{all\ actual\
negatives} = \frac{FP}{FP + TN}
\]</span></p>
<p>假正例是被错误分类的实际负例，因此会出现在分母中。在垃圾邮件分类示例中，FPR用于衡量被错误分类为垃圾邮件的合法电子邮件的比例，或模型的误报率。</p>
<p>完美的模型没有假正例，因此FPR为0.0，也就是说，假警报率为0%。</p>
<p>在实际负例数量非常少（例如总共1-2个示例）的不平衡数据集中，FPR作为一个指标就没有那么有意义和实用。</p>
<h4 id="精确率precision">精确率(Precision)</h4>
<p>精确率是指模型所有正分类分类中实际正分类的比例。在数学上，其定义为：</p>
<p><span class="math display">\[
Precision = \frac{correctly\ classified\ actual\ positives}{everything\
classified\ as\ positive} = \frac{TP}{TP + FP}
\]</span></p>
<p>在垃圾邮件分类示例中，精确率衡量的是被归类为垃圾邮件的电子邮件中实际是垃圾邮件的比例。</p>
<p>假设有一个完美的模型，则该模型没有假正例，因此精确率为1.0。</p>
<p>在实际正例数量非常少（例如总共1-2个示例）的不平衡数据集中，精确率作为指标的意义和实用性较低。</p>
<p>随着假正例的减少，精确率会提高；随着假负例的减少，召回率会提高。但正如前面所述，提高分类阈值往往会减少正例的数量并增加假负例的数量，而降低阈值则会产生相反的效果。因此，精确率和召回率通常呈反向关系，提高一个会反过来降低另一个。</p>
<h3 id="指标的选择和权衡">指标的选择和权衡</h3>
<p>在评估模型和选择阈值时，选择优先处理的指标取决于特定问题的需求、收益和风险。在垃圾邮件分类示例中，通常最好优先考虑召回率（抓取所有垃圾邮件）或准确率（尽量确保被标记为垃圾邮件的电子邮件实际上是垃圾邮件），或者在达到某个最低准确性水平的情况下，兼顾这两者。</p>
<table>
<colgroup>
<col style="width: 50%" />
<col style="width: 50%" />
</colgroup>
<thead>
<tr>
<th>指标</th>
<th>指南</th>
</tr>
</thead>
<tbody>
<tr>
<td>准确率</td>
<td>作为平衡数据集的模型训练进度/收敛情况的粗略指标。<br>对于模型效果，请仅与其他指标结合使用。<br>避免使用不平衡的数据集，考虑使用其他指标。</td>
</tr>
<tr>
<td>召回率<br>（真正例率）</td>
<td>在假负例比假正例开销更高时使用。</td>
</tr>
<tr>
<td>假正例率</td>
<td>在假正例比假负例开销更高时使用。</td>
</tr>
<tr>
<td>精确率</td>
<td>当正例预测的准确性非常重要时，请使用此方法。</td>
</tr>
</tbody>
</table>
<h4 id="f1-得分">F1 得分</h4>
<p>F1 得分是精确率和召回率的调和平均数（一种平均值）。</p>
<p>在数学上，它可以按下式计算：</p>
<p><span class="math display">\[
F1 = 2 * \frac{precision * recall}{precision + recall} = \frac{2TP}{2TP
+ FP + FN}
\]</span></p>
<p>该指标在精确率和召回率的重要性之间进行了平衡，对于类别不平衡的数据集，该指标优于准确率。如果精确率和召回率的完美分数均为
1.0，则F1 的完美分数也是 1.0。更广泛地说，当精确率和召回率的值接近时，F1
也会接近它们的值。当精确率和召回率相差很大时，F1
将与较差的指标相似。</p>
<h3 id="roc-和曲线下面积">ROC 和曲线下面积</h3>
<h4 id="接收者操作特征曲线roc">接收者操作特征曲线(ROC)</h4>
<p>ROC
曲线是模型在所有阈值上的表现的可视化表示。这个长版名称（即接收器操作特征）来自二战雷达侦测工具。</p>
<p>ROC 曲线的绘制方法是计算真正例率(TPR) 和假正例率(FPR)
值（在实践中，通常绘制 FPR 对 TPR 图表。完美的模特在某些阈值下，TPR 为
1.0，FPR 为 0.0，都表示为(0,
1)（如果所有其他阈值均被忽略），或者出现如下情况：</p>
<p><img src="/assets/机器学习入门15.png" /></p>
<h4 id="曲线下面积-auc">曲线下面积 (AUC)</h4>
<p>ROC 曲线下面积 (AUC)
表示模型的概率，如果给定一个随机选择的正类别和负类别样本，正值将大于负值。</p>
<p>上述完美模型包含一个边长为 1 的正方形，曲线下面积(AUC) 为
1.0。也就是说，有 100%
的可能性模型会正确地将随机选择的正例排列在随机选择的负例之前。换句话说，如果观察
ROC 给出了模型将随机选择的阈值右侧的平方，与阈值。</p>
<p>具体而言，使用 AUC
的垃圾邮件分类器则随机分配垃圾邮件的概率始终较高，随机生成合法电子邮件每个单元的实际分类取决于您选择的阈值。</p>
<p>对于二元分类器来说，一个模型与随机猜测相比较的 ROC，它是一条从 (0,0)
到 (1,1) 的对角线。曲线下面积为 0.5，表示对某个随机正例进行排名的概率为
50%，反例。</p>
<p>在垃圾邮件分类器示例中，AUC 为 0.5
的垃圾邮件分类器将随机发送垃圾邮件的概率要高于随机发送的垃圾邮件
合法电子邮件只占一半的时间。</p>
<p><img src="/assets/机器学习入门16.png" /></p>
<h4 id="精确率与召回率曲线">精确率与召回率曲线</h4>
<p>AUC 和 ROC
非常适合在数据集大致如下的情况下比较模型不同类别之间的平衡。当数据集不平衡时，精确率与召回率以及这些曲线下方区域的面积可能会提供直观呈现模型性能的精确率/召回率曲线由
y 轴的精确率和 x 轴的召回率构成。</p>
<p><img src="/assets/机器学习入门17.png" /></p>
<h4 id="用于选择模型和阈值的-auc-和-roc">用于选择模型和阈值的 AUC 和
ROC</h4>
<p>曲线下面积是一个非常有用的指标，用于比较两个不同模型，只要数据集大致均衡即可。（请参阅精确率与召回率曲线，针对不平衡的数据集。）下方是较大区域，那么曲线通常效果最佳：</p>
<p><img src="/assets/机器学习入门18.png" /></p>
<p>ROC 曲线上最接近 (0,1)
的点表示指定模型表现最佳的阈值，正如阈值、温度矩阵和指标选择和权衡中所述。选择阈值取决于对应用而言最重要的指标。具体用例，请考虑以下要点
A、B 和 C 示意图，每个选项代表一个阈值：</p>
<p><img src="/assets/机器学习入门19.png" /></p>
<p>如果误报（误报）的代价很高，即使
TPR，相反，如果假正例费用低廉，（错失的真正例）代价很高，C
点的阈值可以最大限度提高 TPR，可能更合适。如果费用大致相等，请确保 B
点可在 TPR 和 FPR 之间实现最佳平衡。</p>
]]></content>
      <categories>
        <category>机器学习</category>
      </categories>
      <tags>
        <tag>机器学习</tag>
      </tags>
  </entry>
  <entry>
    <title>编码</title>
    <url>/posts/6f8a188e/</url>
    <content><![CDATA[<h2 id="编码">编码</h2>
<p>今天在复习编译原理的时候，发现对底层的编码都是还停留在听过或者见过的状态，所以决定学习一下有关编码的知识。</p>
<span id="more"></span>
<h3 id="编码概述">编码概述</h3>
<ul>
<li><strong>定义</strong>：编码是将信息从一种形式或格式转换为另一种形式的过程，以便信息能够被存储、传输或处理。在计算机科学中，编码通常指将人类可读的信息（如文本、图像、声音等）转换为计算机可识别的二进制形式。</li>
<li><strong>目的</strong>：编码的主要目的是实现信息的有效存储、传输和处理。通过编码，信息可以在不同的系统、设备和网络之间进行交换和共享，同时还能对信息进行加密、压缩等操作以提高安全性、减少存储空间和传输带宽。</li>
</ul>
<h3 id="字符编码">字符编码</h3>
<h4 id="ascii编码">ASCII编码</h4>
<ul>
<li><strong>背景与起源</strong>
<ul>
<li>1960年代，计算机开始普及，但不同厂商使用不同的编码标准，导致信息交换困难</li>
<li>1967年，美国标准化组织制定ASCII，实现了计算机间的标准化通信</li>
</ul></li>
<li><strong>工作原理</strong>
<ul>
<li>使用7位二进制数，可以表示128个字符（2^7 = 128）</li>
<li>0-31：控制字符（如换行符= 10，回车符 13）</li>
<li>32-127：可打印字符（如'A' = 65，'a' = 97，'0' = 48）</li>
</ul></li>
<li><strong>优点</strong>
<ul>
<li>简单易用，是最基础的编码标准</li>
<li>至今仍被广泛使用，现代编码方案都兼容ASCII</li>
</ul></li>
<li><strong>缺点</strong>
<ul>
<li>只能表示英文字母和符号，无法表示其他语言字符</li>
<li>例如：无法表示"中文"、"日本語"等字符</li>
</ul></li>
</ul>
<h4 id="gb2312与gbk">GB2312与GBK</h4>
<ul>
<li><strong>GB2312</strong>
<ul>
<li><strong>起源</strong>：1980年，中国为解决计算机无法显示汉字问题而制定</li>
<li><strong>原理</strong>：采用双字节编码
<ul>
<li>首字节范围0xA1-0xF7，次字节范围0xA1-0xFE</li>
<li>加0xA1是为了避开ASCII码的0x00-0x7F区域，确保与ASCII兼容</li>
<li>分区设计：
<ul>
<li>01-09区：特殊符号</li>
<li>16-55区：一级汉字（3755个常用字）</li>
<li>56-87区：二级汉字（3008个次常用字）</li>
<li>10-15区：保留区</li>
</ul></li>
<li>每个区94个字符，共94个区</li>
</ul></li>
<li><strong>示例</strong>：
<ul>
<li>"中"的GB2312编码为0xD6D0（第45区第60个汉字）</li>
<li>"国"的GB2312编码为0xB9FA（第16区第15个汉字）</li>
</ul></li>
</ul></li>
<li><strong>优点</strong>：
<ul>
<li>完全兼容ASCII</li>
<li>包含6763个常用汉字，满足基本需求</li>
<li><strong>缺点</strong>：
<ul>
<li>汉字数量有限，部分生僻字无法表示</li>
<li>不支持繁体中文、日韩文字</li>
</ul></li>
</ul></li>
<li><strong>GBK</strong>
<ul>
<li><strong>起源</strong>：1995年推出，是GB2312的扩展</li>
<li><strong>特点</strong>：
<ul>
<li>向下完全兼容GB2312</li>
<li>收录21003个汉字，包括繁体字</li>
</ul></li>
<li><strong>示例</strong>：
<ul>
<li>"㠭"（生僻字）在GB2312中无法表示，但GBK可以表示</li>
</ul></li>
<li><strong>优点</strong>：
<ul>
<li>字符集更大，能满足绝大多数中文处理需求</li>
<li>兼容性好，在中国大陆使用广泛</li>
</ul></li>
<li><strong>缺点</strong>：
<ul>
<li>不是国际标准，在跨国际平台可能出现乱码</li>
<li>无法表示其他国家的文字系统</li>
</ul></li>
</ul></li>
</ul>
<h4 id="unicode与utf编码">Unicode与UTF编码</h4>
<ul>
<li><strong>Unicode</strong>
<ul>
<li><strong>起源</strong>：
<ul>
<li>1991年推出，目标是统一全球所有字符编码</li>
<li>由于各国编码标准不一，同一个二进制值在不同编码下可能表示不同字符，造成乱码</li>
</ul></li>
<li><strong>特点</strong>：
<ul>
<li>为每个字符分配唯一编码点（Code Point）</li>
<li>编码空间从U+0000到U+10FFFF，共17个平面（Plane）</li>
<li>第0平面（BMP，基本多语言平面）：U+0000至U+FFFF，收录最常用字符</li>
<li>目前已收录超过14万个字符，包括各国文字、符号、emoji等</li>
</ul></li>
<li><strong>重要概念</strong>：
<ul>
<li>码点（Code Point）：字符在Unicode字符集中的唯一编号</li>
<li>平面（Plane）：Unicode编码空间的逻辑分区，每个平面收录65536个字符</li>
<li>代理对（Surrogate Pair）：使用两个16位值表示BMP之外的字符</li>
</ul></li>
<li><strong>示例</strong>：
<ul>
<li>"A" → U+0041（基本拉丁字母）</li>
<li>"中" → U+4E2D（CJK统一表意文字）</li>
<li>"😀" → U+1F600（补充表意平面，表情符号）</li>
</ul></li>
</ul></li>
<li><strong>UTF-8</strong>
<ul>
<li><strong>原理</strong>：变长编码方案，使用1-4个字节表示一个字符
<ul>
<li>对于单字节，最高位为0</li>
<li>对于多字节，第一个字节以几个1开头，就使用几个字节编码</li>
</ul></li>
<li><strong>编码规则</strong>： <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Unicode范围            UTF-8编码方式</span><br><span class="line">0000-007F: 0xxxxxxx</span><br><span class="line">0080-07FF: 110xxxxx 10xxxxxx</span><br><span class="line">0800-FFFF: 1110xxxx 10xxxxxx 10xxxxxx</span><br><span class="line">10000-10FFFF: 11110xxx 10xxxxxx 10xxxxxx 10xxxxxx</span><br></pre></td></tr></table></figure></li>
<li><strong>示例</strong>： <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">字符    Unicode编码点     UTF-8编码          二进制表示</span><br><span class="line">A       U+0041           41                01000001</span><br><span class="line">中      U+4E2D           E4 B8 AD          11100100 10111000 10101101</span><br><span class="line">😀      U+1F600          F0 9F 98 80      11110000 10011111 10011000 10000000</span><br></pre></td></tr></table></figure></li>
<li><strong>优点</strong>：
<ul>
<li>完全兼容ASCII</li>
<li>节省存储空间（常用字符占用字节少）</li>
<li>自同步（可以从任意位置开始解码）</li>
<li>无字节序问题</li>
</ul></li>
<li><strong>缺点</strong>：
<ul>
<li>变长编码使字符定位较慢</li>
<li>在内存中操作不如定长编码方便</li>
</ul></li>
</ul></li>
<li><strong>UTF-16</strong>
<ul>
<li><strong>原理</strong>：
<ul>
<li>BMP平面内字符：使用2个字节直接表示</li>
<li>BMP平面外字符：使用4个字节的代理对（Surrogate Pair）表示</li>
</ul></li>
<li><strong>编码规则</strong>：
<ul>
<li>BMP字符：直接用16位表示</li>
<li>非BMP字符：将码点转换为代理对
<ol type="1">
<li>码点减去0x10000得到20位数</li>
<li>高10位加上0xD800得到高位代理</li>
<li>低10位加上0xDC00得到低位代理</li>
</ol></li>
</ul></li>
<li><strong>示例</strong>： <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">字符    Unicode编码点     UTF-16编码        说明</span><br><span class="line">A       U+0041           00 41            BMP内字符</span><br><span class="line">中      U+4E2D           4E 2D            BMP内字符</span><br><span class="line">😀      U+1F600          D83D DE00        代理对表示</span><br></pre></td></tr></table></figure></li>
<li><strong>优点</strong>：
<ul>
<li>处理中日韩文字效率高（大多在BMP平面内）</li>
<li>Java、JavaScript等语言内部采用此编码</li>
<li>定长编码便于字符定位</li>
</ul></li>
<li><strong>缺点</strong>：
<ul>
<li>不兼容ASCII</li>
<li>存在大小端序问题（需要BOM标记）</li>
<li>占用空间较大</li>
<li>代理对处理复杂</li>
</ul></li>
</ul></li>
</ul>
<h3 id="数据编码">数据编码</h3>
<ul>
<li><strong>数字编码</strong>
<ul>
<li><strong>二进制编码</strong>：将数字转换为二进制形式，是计算机处理数字信息的基础。例如，十进制数10的二进制编码是1010。</li>
<li><strong>BCD编码</strong>：二进制编码的十进制数，将十进制数的每一位分别转换为4位二进制数。例如，十进制数10的BCD编码是0001
0000。</li>
</ul></li>
<li><strong>图像编码</strong>
<ul>
<li><strong>位图编码</strong>：将图像的每个像素点的颜色信息直接存储起来，常见的位图格式有BMP、PNG等。</li>
<li><strong>压缩编码</strong>：通过减少图像数据的冗余信息来实现数据压缩，常见的压缩编码格式有JPEG、GIF等。JPEG采用离散余弦变换（DCT）和量化等技术，GIF采用LZW压缩算法。</li>
</ul></li>
<li><strong>音频编码</strong>
<ul>
<li><strong>PCM编码</strong>：脉冲编码调制，将模拟音频信号转换为数字信号，是音频数字化的基础。PCM编码的音频数据量较大。</li>
<li><strong>压缩编码</strong>：通过减少音频数据的冗余信息来实现数据压缩，常见的压缩编码格式有MP3、AAC等。MP3采用心理声学模型和变换编码等技术，AAC是MP3的改进版本，具有更高的压缩效率和音质。</li>
</ul></li>
</ul>
]]></content>
      <categories>
        <category>计算机基础</category>
      </categories>
      <tags>
        <tag>编码</tag>
      </tags>
  </entry>
  <entry>
    <title>计算机网络通信漫谈 - 从TCP/IP到gRPC</title>
    <url>/posts/f520f9c/</url>
    <content><![CDATA[<h2 id="计算机通信的层次">计算机通信的层次</h2>
<p>计算机通信是计算机网络形成的基础，这篇博客我们来简单了解一下现有的计算机网络通信协议。</p>
<span id="more"></span>
<h3 id="网络分层模型">网络分层模型</h3>
<p>理论上有OSI七层模型,但在实际应用中我们更常用TCP/IP四层模型:</p>
<ul>
<li>应用层:HTTP、FTP、SMTP、gRPC等应用协议</li>
<li>传输层:TCP、UDP等传输控制协议</li>
<li>网络层:IP、ICMP等网络协议</li>
<li>网络接口层:以太网、WiFi等物理传输协议</li>
</ul>
<p>这种分层设计让我们可以在不改变其他层的情况下优化或替换某一层的实现。就像我们可以在同样的TCP协议上开发HTTP、FTP等不同的应用协议,也可以用WiFi或网线作为物理传输介质。</p>
<h2 id="协议栈的演进">协议栈的演进</h2>
<h3 id="基础传输层---tcp与udp">基础传输层 - TCP与UDP</h3>
<p>传输层是整个协议栈的核心,提供了两种主要的传输服务:</p>
<p>TCP(传输控制协议): - 面向连接:通信前需要建立连接(三次握手) -
可靠传输:使用确认机制和重传机制保证数据可靠到达 -
流量控制:根据接收方处理能力调整发送速率 -
拥塞控制:根据网络状况调整发送速率 -
适用场景:文件传输、网页访问等对可靠性要求高的应用</p>
<p>UDP(用户数据报协议): - 无连接:不需要建立连接即可传输数据 -
不可靠:不保证数据一定送达 - 无流量控制和拥塞控制 - 开销小、延迟低 -
适用场景:视频直播、游戏等对实时性要求高的应用</p>
<h3 id="应用层的发展">应用层的发展</h3>
<h4 id="http---互联网的通用语言">HTTP - 互联网的通用语言</h4>
<p>HTTP协议构建在TCP之上,经历了几个重要版本:</p>
<p>HTTP/1.1: - 持久连接:复用TCP连接 - 管道化请求:支持多个请求排队 -
但存在队头阻塞问题</p>
<p>HTTP/2: - 多路复用:在同一连接上并发处理多个请求 -
头部压缩:减少传输开销 - 服务器推送:主动推送相关资源 -
二进制分帧:更高效的数据传输</p>
<h4 id="grpc---现代分布式系统的新选择">gRPC -
现代分布式系统的新选择</h4>
<p>gRPC是基于HTTP/2构建的高性能RPC框架: - 协议层次: * 应用层:gRPC API *
中间层:HTTP/2 * 传输层:TCP * 安全层:TLS(可选)</p>
<p>四种通信模式: 1. 一元RPC(Unary): - 类似HTTP/1.1的请求-响应模式 -
适用于传统的客户端-服务器交互</p>
<ol start="2" type="1">
<li>服务器流式RPC(Server Streaming):
<ul>
<li>客户端发送一个请求,服务器返回数据流</li>
<li>适用于服务器推送场景,如订阅更新</li>
</ul></li>
<li>客户端流式RPC(Client Streaming):
<ul>
<li>客户端发送数据流,服务器返回一个响应</li>
<li>适用于数据上传场景</li>
</ul></li>
<li>双向流式RPC(Bidirectional Streaming):
<ul>
<li>客户端和服务器同时收发数据流</li>
<li>适用于实时通信场景,如在线游戏</li>
</ul></li>
</ol>
<p>gRPC的优势: - 基于HTTP/2的高性能 - Protocol Buffers的高效序列化 -
强类型接口定义 - 跨语言支持 - 内置流控和安全机制</p>
<h2 id="安全传输---tls的重要性">安全传输 - TLS的重要性</h2>
<p>TLS在传输层和应用层之间提供了安全保障: - 加密:保护数据不被窃听 -
认证:验证通信双方身份 - 完整性:确保数据不被篡改</p>
<p>现代应用如HTTPS和gRPC都默认集成了TLS,为应用提供端到端的安全保障。</p>
]]></content>
      <categories>
        <category>通信协议</category>
      </categories>
      <tags>
        <tag>网络协议</tag>
        <tag>gRPC</tag>
        <tag>TLS</tag>
        <tag>HTTP</tag>
      </tags>
  </entry>
</search>
